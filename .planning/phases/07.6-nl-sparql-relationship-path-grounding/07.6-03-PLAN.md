---
phase: 07.6-nl-sparql-relationship-path-grounding
plan: 03
type: execute
wave: 3
depends_on: ["07.6-02"]
files_modified:
  - tests/nl2sparql/eval/runner.py
  - tests/nl2sparql/eval/configs.yml
  - tests/nl2sparql/eval/run_composed_sweep.py
  - pyproject.toml
  - uv.lock
  - tests/nl2sparql/eval/baseline.json
autonomous: false
requirements: [NL-ACC-03]
user_setup:
  - service: openai-eval-harness
    why: "R5 credentialed CK25 sweep runs the live gpt-4o-mini arm; the agent never holds the key (human-run, RUN_EVAL=1 + NL2SPARQL_API_KEY)."
    env_vars:
      - name: NL2SPARQL_API_KEY
        source: "Human-held OpenAI-compatible key; never committed, never sourced by the agent"
  - service: arango-query-core-promotion
    why: "D-01 engine promotion — push the seam-8 commit to BOTH remotes (arthur, origin) and bump the pyproject.toml/uv.lock pin; human performs the git push."
    dashboard_config:
      - task: "git push seam-8 commit to arthur + origin, ls-remote-verify the pushed SHA on both"
        location: "~/dev/arango-query-core (remotes: arthur=ArthurKeen, origin=arango-solutions)"
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An additive `path_grounding:` config read in runner.py mirrors grounding:/predicate_grounding: exactly and threads path_k/path_index into the one NlPipeline(...) call — R3."
    - "The composed arm openai-gpt4o-mini-ck25-grounded-generated-fewshot-path (+ its scripted twin) builds grounding + few_shot + path indices into ONE NlPipeline; --dry-run scripted arm is 49/49 with no crash — R3 unclassified edge."
    - "Engine promotion happens ONLY after R4 recall-green (Plan 01): push to both remotes, ls-remote-verify, bump pyproject.toml + uv.lock pin, re-run R4 against the pinned (non-editable) install (D-01, 07.5-06 precedent)."
    - "The human-run paired CK25 sweep (grounded+few-shot+path vs grounded+few-shot, same session) shows a directional lift with ZERO regressions ⇒ ADOPT recorded in baseline.json (or documented-null) — R5."
    - "W3C DAWG query-eval coverage ≥ 96.4% and the SPARQL→AQL transpiler package unchanged (git diff) — R5 boundary edge."
  artifacts:
    - "tests/nl2sparql/eval/runner.py (additive path_grounding: read)"
    - "tests/nl2sparql/eval/configs.yml (openai-...-path + scripted-...-path arms)"
    - "tests/nl2sparql/eval/run_composed_sweep.py (ARMS + CONTRASTS path arm)"
    - "pyproject.toml + uv.lock (arango-query-core pin bump off 3438305)"
    - "tests/nl2sparql/eval/baseline.json (phase07_6_path_grounding_sweep key)"
  key_links:
    - "runner.py path_grounding: read → build_path_index(shared_ontology) → NlPipeline(path_index=...) → the composed arm's --dry-run smoke proves all three seams stack in one pipeline."
    - "recall-green (Plan 01) → engine promotion (this plan) → credentialed sweep against the pinned engine → baseline.json fold-in — the D-01 sequence a gap must not skip."
  prohibitions:
    - statement: "MUST NOT bump the pin before R4 recall-green holds against the real engine seam (D-01: recall-gate-green precedes the costly promotion)"
      status: resolved
      verification: "sequencing — Plan 01 R4 is green before this plan's promotion checkpoint; R4 re-run against the pinned install confirms after the bump"
    - statement: "MUST NOT dump the schema or exceed the ≤5-path budget — the zero-regression credentialed-sweep guard (c=0) is the adopt bar"
      status: resolved
      verification: "test/measurement — the composed-sweep contrast reports c (regressions); ADOPT requires a directional lift with c=0 (or documented-null)"
    - statement: "MUST NOT let the agent hold NL2SPARQL_API_KEY — the credentialed sweep is human-run"
      status: resolved
      verification: "checkpoint — R5 is gated checkpoint:human-verify; the agent never sources the key"
---

<objective>
EXPANSION + promotion — wire the eval-side `path_grounding:` config arm, prove the
three seams stack in one pipeline offline (--dry-run), then gate the two costly/
credentialed steps behind human checkpoints: the D-01 engine promotion (two-remote
push + pin bump, only AFTER Plan 01's R4 recall-green) and the human-run credentialed
CK25 paired sweep (directional lift + zero regressions ⇒ ADOPT, folded into
baseline.json).

Purpose: turn the proven+wired lever into a measurable, published capability with the
same no-regression discipline as 07.3/07.4/07.5. Output: the composed arm + scripted
twin, the pin bump, and the baseline.json adopt/kill record.

Covers R3 (eval config seam + composed arm + scripted twin) and R5 (credentialed
directional lift + non-regression + promotion). Cites D-01 (recall-green precedes the
costly pin bump). This plan is `autonomous: false` — it carries two blocking human
checkpoints (promotion push, credentialed sweep).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-SPEC.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-PATTERNS.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-CONTEXT.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-01-SUMMARY.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-02-SUMMARY.md

# Eval-harness config read + composed sweep driver + baseline
@tests/nl2sparql/eval/runner.py
@tests/nl2sparql/eval/configs.yml
@tests/nl2sparql/eval/run_composed_sweep.py
@tests/nl2sparql/eval/baseline.json
</context>

<artifacts_produced>
## Artifacts this plan produces

- `path_grounding:` additive config read in runner.py + thread into NlPipeline(...).
- Composed arms `scripted-ck25-grounded-generated-fewshot-path` + `openai-gpt4o-mini-ck25-grounded-generated-fewshot-path` in configs.yml.
- run_composed_sweep.py ARMS tuple `("g+f+path", ...)` + CONTRASTS `("g+f+path vs g+f ...", "g+f", "g+f+path")`.
- pyproject.toml + uv.lock arango-query-core pin bump (off 3438305).
- baseline.json `phase07_6_path_grounding_sweep` key (adopt/kill record).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Additive path_grounding: config read + composed arm + scripted twin + --dry-run smoke</name>
  <files>tests/nl2sparql/eval/runner.py, tests/nl2sparql/eval/configs.yml, tests/nl2sparql/eval/run_composed_sweep.py</files>
  <read_first>
    - tests/nl2sparql/eval/runner.py (lines 548-612: the grounding: and predicate_grounding: additive reads and the single NlPipeline(...) call site at 601-612 — path_grounding: mirrors predicate_grounding: exactly, gated on shared_ontology not data_ttl since the graph build is TBox-only)
    - tests/nl2sparql/eval/configs.yml (lines 307-380: scripted-/openai-gpt4o-mini-ck25-grounded-generated-fewshot and the all-levers arms — the exact composed-arm template)
    - tests/nl2sparql/eval/run_composed_sweep.py (lines 52-69: the ARMS tuple list and CONTRASTS list; the dry_run() plumbing loop at 72-84)
    - tests/nl2sparql/eval/grounding_index_builder.py (build_path_index from Plan 01 — the builder runner.py calls)
  </read_first>
  <action>
    In runner.py add an additive `path_grounding:` read directly after the predicate_grounding: block (mirror it verbatim): `path_cfg = config.get("path_grounding", {})`, `path_k = path_cfg.get("k", 0)`, `path_index = None`, and `if path_cfg and shared_ontology:` build it via a function-local `from tests.nl2sparql.eval.grounding_index_builder import build_path_index` then `path_index = build_path_index(shared_ontology)` (build-once, outside the per-case loop; gated on shared_ontology like predicate grounding, NOT on data_ttl). Thread `path_k=path_k, path_index=path_index` into the existing NlPipeline(...) call at 601-612.

    In configs.yml add two arms cloned from openai-gpt4o-mini-ck25-grounded-generated-fewshot / scripted-ck25-grounded-generated-fewshot, each PLUS a `path_grounding: {k: 5}` block (mirrors R1's ≤5 budget as the config's own k): `scripted-ck25-grounded-generated-fewshot-path` and `openai-gpt4o-mini-ck25-grounded-generated-fewshot-path`.

    In run_composed_sweep.py append `("g+f+path", "openai-gpt4o-mini-ck25-grounded-generated-fewshot-path", "scripted-ck25-grounded-generated-fewshot-path")` to ARMS and `("g+f+path vs g+f (does the path hint help?)", "g+f", "g+f+path")` to CONTRASTS. Confirm the --dry-run plumbing loop picks up the new scripted twin.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv pip install -e ~/dev/arango-query-core >/dev/null && uv run python tests/nl2sparql/eval/run_composed_sweep.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tests/nl2sparql/eval/run_composed_sweep.py --dry-run` prints the scripted `scripted-ck25-grounded-generated-fewshot-path` plumbing row and reports 49/49 (no crash) — all three seams (grounding + few_shot + path) build into ONE NlPipeline.
    - runner.py's `path_grounding:` read mirrors `predicate_grounding:` (gated on shared_ontology), builds build_path_index once, and threads path_k/path_index into the single NlPipeline(...) call.
    - configs.yml carries both `scripted-ck25-grounded-generated-fewshot-path` and `openai-gpt4o-mini-ck25-grounded-generated-fewshot-path` with `path_grounding: {k: 5}`.
    - run_composed_sweep.py ARMS + CONTRASTS carry the g+f+path arm and the g+f+path vs g+f contrast.
    - Existing scripted arms (zero/ground/fewshot/g+f/all) still 49/49 in --dry-run (no regression).
  </acceptance_criteria>
  <done>The composed path arm + scripted twin exist and the offline --dry-run proves grounding+few_shot+path stack in one NlPipeline (scripted 49/49) — R3 satisfied offline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Engine promotion — two-remote push + pin bump (D-01, costly, recall-green-gated)</name>
  <reversibility rating="costly">Undo means reverting a published engine commit on two remotes plus a pyproject.toml/uv.lock pin bump; sequenced AFTER Plan 01 R4 recall-green so a gap surfaces while still local (D-01).</reversibility>
  <what-built>
    Plans 01-02 landed seam-8 in the local-editable arango-query-core checkout: `ClassPathIndex`/`ClassPath` (pathindex.py), the `QueryLanguageAdapter` path Protocol + `NLQueryEngine` seam-8 composition (seams.py/engine.py), and the barrel export. R4 recall is GREEN (≥14/16) against this editable seam; Task 1's --dry-run proves the arm plumbs.
  </what-built>
  <how-to-verify>
    Perform the D-01 / 07.5-06 promotion sequence (human holds git push access; the pin bump must NOT precede recall-green):
    1. In ~/dev/arango-query-core: confirm the engine unit tests are green (`uv run pytest tests/test_nl_pathindex.py tests/test_nl_engine.py -x`).
    2. Commit the seam-8 engine changes; `git push` to BOTH remotes: `git push arthur <branch>` and `git push origin <branch>`.
    3. ls-remote-verify the pushed SHA is visible on BOTH: `git ls-remote arthur` and `git ls-remote origin` show the new commit (07.5-06's explicit prior lesson — verify on both BEFORE uv lock).
    4. In this repo: bump the arango-query-core git pin in pyproject.toml (both the `[nl]`/`[dense]` extras that currently pin 3438305) to the pushed SHA, then `uv lock`.
    5. Re-run R4 against the now-PINNED (non-editable) install: `uv pip install -e . && uv run pytest tests/nl2sparql/eval/test_path_recall.py -x` stays ≥14/16 — final confirmation the pinned engine matches the editable one.
    6. Confirm the SPARQL→AQL transpiler package is unchanged: `git diff --stat arango_sparql/translate/` is empty.
  </how-to-verify>
  <resume-signal>Type "promoted &lt;SHA&gt;" once both remotes show the SHA, the pin+uv.lock are bumped, and R4 is green against the pinned install; or describe any gap.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Credentialed CK25 paired sweep + baseline.json fold-in + non-regression (R5)</name>
  <what-built>
    The composed arm `openai-gpt4o-mini-ck25-grounded-generated-fewshot-path` and its `g+f+path vs g+f` contrast are wired in run_composed_sweep.py (Task 1), running against the promoted/pinned engine (Task 2). This is the terminal directional measurement — human-run because it needs the live key + network + spend; the agent never holds NL2SPARQL_API_KEY.
  </what-built>
  <how-to-verify>
    1. Run the paired live sweep in one session (both arms same session — CK25 n=49 is underpowered, so grade directionally + zero-regression, mirroring 07.3-07.5, NOT significance-gated):
       `RUN_EVAL=1 NL2SPARQL_API_KEY=… uv run python tests/nl2sparql/eval/run_composed_sweep.py --sweep`
    2. Read the `g+f+path vs g+f` contrast (b=gains, c=regressions, Δ + bootstrap CI) from the printed matrix / composed_sweep_result.json.
    3. Fold the result into tests/nl2sparql/eval/baseline.json under a new top-level key `phase07_6_path_grounding_sweep` (mirror phase07_5_generated_fewshot_sweep's structure: arms, pass_counts, contrasts, adopt/kill disposition, limitations). Record ADOPT when the arm shows a directional lift over g+f (16/49 baseline) with ZERO regressions (c=0); otherwise record a documented-null with the b/c/Δ evidence.
    4. Re-confirm non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -x` green (≥96.4%, or key-free skip when the W3C corpus isn't fetched), and `git diff --stat arango_sparql/translate/` empty (transpiler unchanged).
  </how-to-verify>
  <resume-signal>Type "adopted" (with the b/c/Δ) or "documented-null" (with evidence) once the sweep result is folded into baseline.json and W3C + transpiler non-regression are confirmed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human key → live provider | The credentialed sweep sends CK25 questions + path-hint prompts to a live model; the key is human-held, never the agent's. |
| local engine → published remotes | Promotion publishes seam-8 to arthur + origin and pins this repo's CI to it — a costly, hard-to-reverse mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-02 | Tampering (model-output correctness / schema-dump distraction) | the composed path arm's prompt | high | mitigate | The ≤5-path global-pool budget (D-03, Plan 01) + the zero-regression adopt bar (c=0) in this sweep — the sweep IS the empirical guard against a re-emergence of the 07.4 distraction regression (block-on-high). |
| T-07.6-04 | Elevation / Repudiation | NL2SPARQL_API_KEY handling | high | mitigate | R5 is gated checkpoint:human-verify; the agent never sources the key; no secret is committed (baseline.json holds only pass verdicts + stats). |
| T-07.6-05 | Tampering | engine pin bump before recall-green | high | mitigate | Sequencing gate (D-01): the promotion checkpoint follows Plan 01's R4 green and re-runs R4 against the pinned install; a gap surfaces while still local. |
| T-07.6-SC | Tampering | npm/pip/cargo installs | low | accept | No new registry package — only a git-pin bump of the already-audited internal arango-query-core (RESEARCH § Package Legitimacy Audit: N/A, same non-audit precedent as 07.3-02/07.4-06/07.5-06). |
</threat_model>

<verification>
- `uv run python tests/nl2sparql/eval/run_composed_sweep.py --dry-run` green (scripted g+f+path 49/49) — R3.
- Both remotes show the promoted seam-8 SHA; pyproject.toml + uv.lock bumped off 3438305; R4 ≥14/16 against the pinned install — D-01.
- baseline.json carries `phase07_6_path_grounding_sweep` with the paired g+f+path vs g+f result (ADOPT c=0, or documented-null) — R5.
- `uv run pytest tests/w3c/test_coverage_gate.py -x` green (≥96.4%); `git diff --stat arango_sparql/translate/` empty — R5.
</verification>

<success_criteria>
- Additive path_grounding: arm + scripted twin build all three seams into one NlPipeline; --dry-run 49/49.
- Engine promoted to both remotes + pin bumped ONLY after recall-green; R4 re-confirmed against the pinned install.
- Credentialed paired sweep folded into baseline.json: directional lift with zero regressions ⇒ ADOPT (or documented-null).
- W3C ≥96.4% and the SPARQL→AQL transpiler package unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-03-SUMMARY.md` when done.
</output>
