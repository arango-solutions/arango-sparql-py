---
phase: 07.6-nl-sparql-relationship-path-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/dev/arango-query-core/arango_query_core/nl/pathindex.py"
  - "~/dev/arango-query-core/arango_query_core/nl/__init__.py"
  - "~/dev/arango-query-core/tests/test_nl_pathindex.py"
  - tests/nl2sparql/eval/grounding_index_builder.py
  - tests/nl2sparql/eval/derive_path_golds.py
  - tests/nl2sparql/eval/path_recall_golds.yml
  - tests/nl2sparql/eval/test_path_recall.py
autonomous: true
requirements: [NL-ACC-03]
estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Given pre-resolved anchor classes + targets, ClassPathIndex.shortest_paths returns bounded shortest predicate paths (depth ≤ 3, at most 5, deterministic order length → question-token overlap → lexical) — R1, boundary/precision edges (length-4 excluded; ck25-47/48 logged as known limitation)."
    - "A subclass-aware, inverse-edged, self-revisit-bounded class-connectivity graph is built mechanically from a TBox (D-9 subclass, D-2 inverse, D-10 self-revisit) so the canonical inverse-join case is reachable — R1."
    - "Offline path-recall ≥ 14/16 on the execution-diff-derived empty-result golds; 'recovered' = the gold nav-path appears among the ≤5 returned paths (R4 edge)."
    - "The 16 empty-result 'right-entity, wrong-path' golds are derived by EXECUTION-DIFF (candidates executed vs prod-inst.ttl, 0-row set isolated), not static SPARQL-text comparison (RESEARCH Open Question 1 / A1)."
    - "The engine seam is stdlib-only and carries no pyoxigraph reference on its module path (D-08)."
    - "format_prompt_section returns '' when anchor/target unresolved or no path within depth-3 (honest no-op, mirrors seam-6/7) — R2 empty edge."
  artifacts:
    - "~/dev/arango-query-core/arango_query_core/nl/pathindex.py (ClassPathIndex, ClassPath)"
    - "tests/nl2sparql/eval/grounding_index_builder.py (build_path_index)"
    - "tests/nl2sparql/eval/derive_path_golds.py + tests/nl2sparql/eval/path_recall_golds.yml (16-case gold fixture, test-only)"
    - "tests/nl2sparql/eval/test_path_recall.py (R4 recall gate + engine-purity + no-ontology-branch scans)"
    - "~/dev/arango-query-core/tests/test_nl_pathindex.py (engine unit tests)"
  key_links:
    - "build_path_index (pyoxigraph TBox walk) → ClassPathIndex construction → shortest_paths(anchors, targets, k=5) → the R4 recall gate reads path_recall_golds.yml and asserts gold ∈ returned ≤5."
    - "The subclass link + the inverse edge are what make the canonical Department→manager inverse-join reachable — the single most likely break point (Pitfall 2)."
  prohibitions:
    - statement: "MUST NOT dump the schema or exceed the ≤5-path budget (the 07.4 distraction regression)"
      status: resolved
      verification: "test — assert the rendered path-hint block never exceeds 5 path lines; enforced in shortest_paths global pool (D-03) + format_prompt_section"
    - statement: "MUST NOT hardcode per-ontology (CK25) vocabulary — mechanical TBox derivation only"
      status: resolved
      verification: "test — static scan (mirror test_generator_no_ontology_branch, reusing _FORBIDDEN_ONTOLOGY_TERMS + _strip_comments) asserts none of those terms appear in build_path_index's comment-stripped source"
    - statement: "MUST NOT import the pyoxigraph package into the engine seam module"
      status: resolved
      verification: "test — test_module_is_pyoxigraph_free-style scan over the installed arango_query_core.nl.pathindex source (D-08)"
---

<objective>
TRACER PLAN — prove the relationship-path-grounding lever end-to-end, offline,
engine-first, BEFORE any adapter/eval/promotion wiring exists (D-01: the Step-0
recall spike already validated the hypothesis, so the committed R4 recall gate runs
against the REAL engine seam, not a throwaway prototype).

This plan lands engine seam-8 (`ClassPathIndex`), the eval-side `build_path_index`
TBox walker, the execution-diff-derived 16-case gold fixture, and the offline
path-recall gate (R4) asserting ≥ 14/16 against the local-editable engine. The
mechanical lever must be GREEN here before the pin bump (Plan 03) publishes the
engine (D-01: recall-gate-green precedes the costly promotion so a gap surfaces
while the change is still local).

Purpose: catch an architectural dead-end (e.g. a naive exact-class graph that
silently drops the canonical inverse-join case) on the executor's best early-context
tokens, after one commit, instead of after adapters/eval-arms/promotion are all built.
Output: a real `ClassPathIndex` engine class + `build_path_index` + a committed R4
gate proving the gold paths are mechanically recoverable.

Covers R1 (engine seam) and R4 (offline recall gate). Cites D-01 (engine-first
sequencing), D-03 (≤5 global-pool budget), and scope-doc design decisions D-9
(subclass-aware nodes), D-2 (inverse edges), D-10 (bounded self-revisit), D-8
(honest no-op).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-SPEC.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-PATTERNS.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-CONTEXT.md

# Engine substrate (mirror this style — seam-6/7 precedent; local checkout, NOT under this repo's git root)
@$HOME/dev/arango-query-core/arango_query_core/nl/grounding.py

# Eval-side TBox-walk precedent + recall-gate precedent
@tests/nl2sparql/eval/grounding_index_builder.py
@tests/nl2sparql/eval/test_grounding_recall.py
</context>

<artifacts_produced>
## Artifacts this phase produces (full phase list — for the drift/source-grounding pass)

New symbols created across Phase 07.6 (exclude these from any "already-exists" source-grounding check):

- Engine: `ClassPathIndex` (seam-8 class) + `ClassPath` dataclass; `ClassPathIndex.shortest_paths(anchor_classes, targets, k=5)`; `ClassPathIndex.format_prompt_section(...)`; the class-connectivity graph build inside `ClassPathIndex.__init__`/`from_items`; `path_index()` / `path_prompt_section()` on the `QueryLanguageAdapter` Protocol + `path_k` field on `NLQueryEngine` + the seam-8 composition block in `NLQueryEngine._system_prompt`.
- Project adapters: `path_index()` / `path_prompt_section()` on both `SparqlAdapter` (engine_adapter.py) and `SparqlLanguageAdapter` (adapter.py).
- Pipeline: new `path_k` / `path_index` params on `NlPipeline.__init__` + `.run()` threading (pipeline.py).
- Eval-side: `build_path_index(ontology_ttl)` in grounding_index_builder.py; `tests/nl2sparql/eval/derive_path_golds.py` + `path_recall_golds.yml`; the `path_grounding:` config block; the composed arm id `openai-gpt4o-mini-ck25-grounded-generated-fewshot-path` + its scripted twin `scripted-ck25-grounded-generated-fewshot-path`; the `run_composed_sweep.py` path arm (ARMS + CONTRASTS); `tests/nl2sparql/eval/test_path_recall.py`; the 3rd `test_grounding_prompt_parity.py` path-parity test; the new `test_module_is_pyoxigraph_free` / no-ontology-branch tests for the seam; the `baseline.json` key `phase07_6_path_grounding_sweep`.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end mechanical lever — ClassPathIndex engine seam + build_path_index, canonical inverse-join recovered from the real TBox</name>
  <files>~/dev/arango-query-core/arango_query_core/nl/pathindex.py, ~/dev/arango-query-core/arango_query_core/nl/__init__.py, ~/dev/arango-query-core/tests/test_nl_pathindex.py, tests/nl2sparql/eval/grounding_index_builder.py, tests/nl2sparql/eval/test_path_recall.py</files>
  <read_first>
    - ~/dev/arango-query-core/arango_query_core/nl/grounding.py (LabelIndex/PredicateIndex construction discipline: frozen, caller-owned, `from_items`, no memoization; `_sanitize_label`/`_local_name` at lines 66-94; `""`-on-no-match renderer contract)
    - ~/dev/arango-query-core/tests/test_nl_grounding.py (engine unit-test style: one small synthetic fixture per behavior — test_exact_substring_match / test_topk_and_ranking / test_empty_on_no_match / test_label_sanitization)
    - tests/nl2sparql/eval/grounding_index_builder.py (build_predicate_index lines 189-333: two-pass pyoxigraph TBox walk, function-local `from tests.helpers.oxi import ...`, `_local_name`/`_strip_iri` helpers; build_label_index local-name normalization at 150-152 — Pitfall 3)
    - .planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md (§ Architecture Patterns Pattern 1/2, Pitfall 3/4 — ClassPathIndex takes pre-resolved anchors/targets, NOT a question string; anchor keys on LOCAL NAMES not IRIs; the Employee-subclass-of-Agent / memberOf-inverse worked example)
  </read_first>
  <precondition>The arango-query-core engine checkout exists at ~/dev/arango-query-core (git remotes `arthur` + `origin`, HEAD 3438305 = current pin) and is installable as an editable dependency into this repo's venv (uv pip install -e ~/dev/arango-query-core).</precondition>
  <reversibility rating="reversible">Engine seam is unpushed/local-editable this plan; the costly published mutation (two-remote push + pin bump) is gated separately in Plan 03 after recall-green.</reversibility>
  <action>
    Create a new engine module `arango_query_core/nl/pathindex.py` holding a frozen `ClassPath` dataclass and a caller-owned `ClassPathIndex`, mirroring grounding.py's construction discipline (a `from_items(...)` classmethod, no file I/O, no memoization, stdlib-only). Reuse grounding.py's `_sanitize_label`/`_local_name` for every rendered label; do not fork a second scorer.

    Departure from seam-6/7 (Pitfall 4 / D-02): `ClassPathIndex` does NOT self-score a question. Its retrieval method takes PRE-RESOLVED anchor classes and targets: `shortest_paths(self, anchor_classes, targets, k=5)` returning `list[ClassPath]`. Node keys are class LOCAL NAMES (Pitfall 3 — the anchor comes from a local-name string), not full IRIs.

    Build the class-connectivity graph inside the constructor from mechanically-extracted inputs (plain tuples, no pyoxigraph in this module): forward edges from each object property's (predicate-local-name, domain-local-name, range-local-name); the matching INVERSE edge for every forward edge (scope-doc D-2); subclass-aware node linking so a subclass node inherits its superclass's incident edges and vice versa (scope-doc D-9); and bounded self-revisit so a self-referential relation (domain == range) survives one revisit of the same node type (scope-doc D-10). Enumerate paths with a bounded BFS at depth ≤ 3; pool every candidate path across ALL (anchor, target) pairs into ONE global pool, dedupe, sort by the locked deterministic ordering (path length → question-token overlap → lexical signature), and return at most 5 (D-03). `format_prompt_section(...)` renders the ≤5 paths as shared-variable JOIN/STAR patterns (scope-doc D-04 — a shared join variable, never a directed A-to-B-to-C walk) and returns the empty string when anchor/target is unresolved or no path exists within depth 3 (scope-doc D-8, mirrors seam-6/7). Keep this module free of any per-ontology term (mechanical only, D-02) and free of any top-level reference to the pyoxigraph package.

    Export `ClassPathIndex` and `ClassPath` from `arango_query_core/nl/__init__.py` alongside the existing grounding exports.

    In this repo, add a sibling `build_path_index(ontology_ttl)` to tests/nl2sparql/eval/grounding_index_builder.py mirroring build_predicate_index's two-pass, function-local pyoxigraph-import discipline (D-08 packaging boundary): a Pass-1 SELECT over declared object properties capturing domain/range as local names, PLUS an rdfs:subClassOf query producing (sub-local-name, super-local-name) pairs (build_predicate_index deliberately forbids the subclass walk for its own consumer at lines 213-215 — build_path_index is the opposite consumer and REQUIRES it). Feed those extracted edge tuples + subclass pairs into `ClassPathIndex.from_items(...)`. Never hardcode a per-schema class/predicate name here.

    Install the engine editable into this repo's venv, then add engine unit tests in ~/dev/arango-query-core/tests/test_nl_pathindex.py using SYNTHETIC class/predicate fixtures (invented names, not any real vocabulary) covering: subclass-aware reachability across a sub/super boundary, inverse-edge traversal, self-loop survival, depth-≤3 cutoff, the ≤5 global-pool cap, deterministic ordering, and the ''-on-no-match renderer. Prove the end-to-end slice with a focused assertion in tests/nl2sparql/eval/test_path_recall.py (name it test_canonical_inverse_join_recovered): build_path_index over the CK25 corpus ontology, then assert `shortest_paths` for the canonical "manager of the Data Services department" case (anchor class Department, target token manager) returns — among its ≤5 paths — the person-scoped inverse join (memberOf-inverse into an Agent/Employee, then hasManager), demonstrating subclass-aware + inverse edges working from the real TBox before any adapters exist.
  </action>
  <verify>
    <automated>cd ~/dev/arango-query-core && uv run pytest tests/test_nl_pathindex.py -x -q</automated>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv pip install -e ~/dev/arango-query-core >/dev/null && uv run pytest tests/nl2sparql/eval/test_path_recall.py -k canonical -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_nl_pathindex.py -x` (engine repo) is green: subclass-aware reachability, inverse traversal, self-loop survival, depth-≤3 cutoff, ≤5 global-pool cap, deterministic ordering, and ''-on-no-match all asserted with synthetic fixtures.
    - The canonical end-to-end assertion `tests/nl2sparql/eval/test_path_recall.py::test_canonical_inverse_join_recovered` is green: build_path_index over the CK25 corpus ontology recovers the canonical inverse-join among `shortest_paths(...)`'s ≤5 returned paths (proves D-9 subclass linking + D-2 inverse end-to-end).
    - `ClassPathIndex.shortest_paths` returns at most 5 paths, each of length ≤ 3 (asserted in the engine tests).
    - `from arango_query_core.nl import ClassPathIndex, ClassPath` imports cleanly.
  </acceptance_criteria>
  <done>The real engine seam-8 exists (stdlib-only, subclass-aware + inverse + bounded self-revisit + ≤5 global-pool bounded shortest-path + ''-no-match renderer), build_path_index feeds it from a mechanical TBox walk, and the canonical inverse-join is recovered end-to-end from the real CK25 TBox — committed against the local-editable engine.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the 16-case empty-result gold fixture by execution-diff (R4-pre gate)</name>
  <files>tests/nl2sparql/eval/derive_path_golds.py, tests/nl2sparql/eval/path_recall_golds.yml</files>
  <read_first>
    - tests/nl2sparql/eval/candidates_dump.json (shape: {arm, n, passed, cases:[{name, passed, judge_note, gold, candidate}]}; 49 cases, 31 with passed:false — the raw material)
    - tests/helpers/oxi.py (load_store_from_string / oxi_query — the pyoxigraph execution helper; form-aware SELECT/ASK row extraction)
    - tests/nl2sparql/eval/vendored/ck25/corpus.yml (data_path: raw/prod-inst.ttl — the instance graph candidates execute against)
    - .planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md (Open Question 1 / Assumptions A1: the exact 16-case list is NOT committed anywhere; the scope doc names only 6 — ck25-7/12/26/35/47/48; the other ~10 MUST be re-derived by execution, never by static SPARQL-text diff)
  </read_first>
  <action>
    Write tests/nl2sparql/eval/derive_path_golds.py: load candidates_dump.json, load raw/prod-inst.ttl once via load_store_from_string, and for each of the 31 `passed: false` cases execute the case's `candidate` SPARQL against the instance store with oxi_query, then bucket by result: (a) empty result set (0 rows / ASK false) with a correctly-grounded instance IRI present in the candidate = the "right-entity, wrong-path" target set; (b) non-empty-but-wrong (wrong-value bucket, out of scope); (c) ASK/kind mismatch; (d) empty candidate string (harness artifact). Isolate bucket (a) — expected to be the 16 empty-result golds (the root-cause doc's 16 empty-result + 10 wrong-value + 3 ASK-mismatch + 2 harness split of the 31). This mechanical execution-diff replaces the guessed static-text classification (A1). Give the script an `--emit <path>` flag that writes the fixture and a default report mode that prints the bucket counts.

    Emit tests/nl2sparql/eval/path_recall_golds.yml under a top-level `golds:` list: for each gold record the case `name`, the anchor class local-name(s) (from the grounded entity's rdf:type in the case's gold SPARQL), the target token(s) (the destination predicate/class the gold navigates to), and the gold nav-path transcribed from the case's `gold` SPARQL (the join/star triples). This fixture is TEST-ONLY: hand-transcribing gold nav-paths here does NOT violate the engine's no-hand-curation constraint (the engine derives everything mechanically from the TBox — CONTEXT specifics). Ensure the canonical inverse-join case and the three other scope-doc-named path cases (ck25-12/26/35) appear so the fixture is anchored to known ground truth.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv run python tests/nl2sparql/eval/derive_path_golds.py --emit tests/nl2sparql/eval/path_recall_golds.yml && uv run python tests/nl2sparql/eval/derive_path_golds.py --check-count</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tests/nl2sparql/eval/derive_path_golds.py` classifies every one of the 31 passed:false candidates by EXECUTION against raw/prod-inst.ttl (0-row vs non-empty vs ASK-mismatch vs empty-candidate) — never by static SPARQL-text diff.
    - `tests/nl2sparql/eval/path_recall_golds.yml` exists with a top-level `golds:` list; the empty-result-right-entity bucket has 14–16 entries (target 16), each carrying name + anchor class local-name(s) + target token(s) + gold nav-path.
    - The four scope-doc-named path cases (ck25-7, ck25-12, ck25-26, ck25-35) are present in the golds.
    - The fixture is under tests/ (test-only); no CK25 vocabulary is written into any engine module by this task.
  </acceptance_criteria>
  <done>The 16-case empty-result gold fixture is derived reproducibly by execution-diff and committed as path_recall_golds.yml, giving R4 a real ground truth instead of a guessed case list.</done>
</task>

<task type="auto">
  <name>Task 3: R4 offline path-recall gate ≥14/16 + engine-purity + no-ontology-branch static scans</name>
  <files>tests/nl2sparql/eval/test_path_recall.py, ~/dev/arango-query-core/tests/test_nl_pathindex.py</files>
  <read_first>
    - tests/nl2sparql/eval/test_grounding_recall.py (the offline, always-on, no-key recall-gate discipline: build index once, compare retrieved-vs-gold, assert a floor BELOW the measured value; NOT marked eval, no RUN_EVAL gate)
    - tests/nl2sparql/eval/test_bank_generator.py (lines 903-985: `_strip_comments` helper, `_FORBIDDEN_ONTOLOGY_TERMS`, and `test_generator_no_ontology_branch` — the static-scan precedent to mirror for build_path_index)
    - ~/dev/arango-query-core/tests/test_nl_synthbank.py (lines 85-93: test_module_is_pyoxigraph_free — the engine-purity scan precedent to mirror for the pathindex module)
    - tests/nl2sparql/eval/path_recall_golds.yml (Task 2 fixture — the 16 golds this gate reads)
    - tests/nl2sparql/eval/grounding_index_builder.py (build_path_index from Task 1 — the function the recall gate and the no-ontology-branch scan target)
  </read_first>
  <action>
    Extend tests/nl2sparql/eval/test_path_recall.py (which already holds Task 1's canonical assertion) with the full R4 recall gate `test_path_recall_meets_spike_floor`: load path_recall_golds.yml, build_path_index once over the CK25 corpus ontology, then for each of the 16 golds call `shortest_paths(anchor_classes, targets, k=5)` and count a hit when the gold nav-path appears among the ≤5 returned paths (compare on the mechanical edge sequence / join signature, not rendered prose). Assert hits/total ≥ 14/16 (the scope-doc Step-0 depth-3 floor, headroom below the measured ~16/16), and assert total is 14–16 so the guard is never vacuous. Run in the always-on tier (no pytest.mark.eval, no RUN_EVAL gate — deterministic, offline, no key).

    Add the engine-purity scan as a test in ~/dev/arango-query-core/tests/test_nl_pathindex.py mirroring test_nl_synthbank.py:85-93: read the pathindex module source via inspect.getsource and assert the anchored line-start import-pattern regex finds no match (the module must stay free of the pyoxigraph package on its import path, D-08). Guard the import path, not prose (a docstring may legitimately name the package to explain why it stays out).

    Add the no-ontology-branch static scan to tests/nl2sparql/eval/test_path_recall.py mirroring test_generator_no_ontology_branch: import build_path_index's module, strip comments via the same `_strip_comments` approach, and assert none of `_FORBIDDEN_ONTOLOGY_TERMS` appears in the comment-stripped source of build_path_index (mechanical TBox derivation only, D-02 — no per-schema class/predicate hint baked into the walker).
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv pip install -e ~/dev/arango-query-core >/dev/null && uv run pytest tests/nl2sparql/eval/test_path_recall.py -x -q</automated>
    <automated>cd ~/dev/arango-query-core && uv run pytest tests/test_nl_pathindex.py -k pyoxigraph_free -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_path_recall.py -x` is green: path-recall ≥ 14/16 on the empty-result golds at depth-3 with subclass-aware nodes + inverse edges + self-revisit; "recovered" = gold nav-path ∈ the ≤5 returned paths.
    - The recall total is 14–16 (guard is not vacuous).
    - `uv run pytest tests/test_nl_pathindex.py -k pyoxigraph_free -x` (engine repo) is green: no pyoxigraph package import on the pathindex module path.
    - The no-ontology-branch scan is green: `_FORBIDDEN_ONTOLOGY_TERMS` (comment-stripped) do not appear in build_path_index's source.
  </acceptance_criteria>
  <done>R4 is GREEN against the local-editable engine (≥14/16), the engine seam is proven pyoxigraph-free, and build_path_index is proven to carry no per-ontology vocabulary — the mechanical lever is validated before any promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBox → prompt | Class/predicate labels from a (potentially attacker-controlled) TBox are rendered into the path-hint block that reaches the LLM. |
| eval harness → engine seam | The eval-side pyoxigraph walk must not leak into the engine package's import path (D-08 packaging boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-01 | Tampering / Information Disclosure | ClassPathIndex.format_prompt_section rendering TBox class/predicate labels | medium | mitigate | Reuse `_sanitize_label`/`_local_name` (grounding.py:66-94) on every rendered label; engine unit test mirrors test_label_sanitization (control-char strip, length cap, no-op on clean input). |
| T-07.6-02 | Tampering (model-output correctness / schema-dump distraction) | path-hint block size | high | mitigate | ≤5-path GLOBAL-pool budget (D-03) enforced in shortest_paths and again in format_prompt_section; engine test asserts the rendered block never exceeds 5 path lines (block-on-high: this is the 07.4 regression's direct control). |
| T-07.6-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages this phase (RESEARCH § Package Legitimacy Audit: N/A — only a pin bump of an already-audited internal git dep in Plan 03); the editable engine install is the local sibling checkout, not a registry package. |
</threat_model>

<verification>
- `uv run pytest tests/test_nl_pathindex.py -x` (engine repo) green — R1 unit + purity.
- `uv run pytest tests/nl2sparql/eval/test_path_recall.py -x` green — R4 ≥14/16 + no-ontology-branch.
- `uv run python tests/nl2sparql/eval/derive_path_golds.py` reproduces the empty-result bucket by execution.
- W3C non-regression is not touched by this plan (engine-seam + eval-only); the SPARQL→AQL transpiler package is untouched (`git diff --stat arango_sparql/translate/` empty).
</verification>

<success_criteria>
- ClassPathIndex engine seam exists (stdlib-only, subclass-aware D-9 + inverse D-2 + bounded self-revisit D-10 + depth-≤3 ≤5 global-pool bounded shortest-path + deterministic ordering + ''-no-match renderer).
- build_path_index feeds it from a mechanical TBox walk (no per-schema hint).
- The 16-case gold fixture is derived by execution-diff (not static text).
- R4 recall gate ≥14/16 GREEN against the local-editable engine, offline, no key.
- Engine seam proven free of the pyoxigraph package import (D-08).
</success_criteria>

<output>
Create `.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-01-SUMMARY.md` when done.
</output>