---
phase: 07.6-nl-sparql-relationship-path-grounding
plan: 02
type: execute
wave: 2
depends_on: ["07.6-01"]
files_modified:
  - "~/dev/arango-query-core/arango_query_core/nl/seams.py"
  - "~/dev/arango-query-core/arango_query_core/nl/engine.py"
  - "~/dev/arango-query-core/tests/test_nl_engine.py"
  - arango_sparql/nl2sparql/engine_adapter.py
  - arango_sparql/nl2sparql/adapter.py
  - arango_sparql/nl2sparql/pipeline.py
  - tests/nl2sparql/test_grounding_prompt_parity.py
  - tests/nl2sparql/test_engine_adapter.py
autonomous: true
requirements: [NL-ACC-03]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The path-hint block composes AFTER the entity (seam-6) and predicate (seam-7) blocks in NLQueryEngine._system_prompt, following the identical index-not-None → render → append-if-non-empty shape — R2."
    - "The path block is anchored on real signals: anchor class = the seam-6 grounded entity's rdf:type (a LOCAL NAME); target = the seam-7 token scorer's retrieved hits (D-02: distinct types × top targets, not strict top-1) — R2."
    - "path_prompt_section emits the hint block when anchor+target resolve and '' (honest no-op) when either is unresolved or no path exists within depth 3 — R2 empty edge."
    - "Class nodes keyed by exact local name; target-token matching reuses the seam-7 scorer unchanged (no third scorer) — R2 encoding edge."
    - "The path-block wording is byte-identical across both SparqlAdapter and SparqlLanguageAdapter (parity), enforced by a 3rd test_grounding_prompt_parity test — R2."
    - "NlPipeline threads path_k/path_index from its constructor into SparqlAdapter(...) and NLQueryEngine(...) in .run(), so the eval harness's NlPipeline(...) call reaches the engine without a TypeError (Pitfall 5)."
  artifacts:
    - "~/dev/arango-query-core/arango_query_core/nl/seams.py (path_index / path_prompt_section on QueryLanguageAdapter)"
    - "~/dev/arango-query-core/arango_query_core/nl/engine.py (path_k field + seam-8 composition block)"
    - "arango_sparql/nl2sparql/engine_adapter.py + adapter.py (path_index / path_prompt_section on both adapters)"
    - "arango_sparql/nl2sparql/pipeline.py (path_k / path_index params + .run() threading)"
    - "tests/nl2sparql/test_grounding_prompt_parity.py (3rd path-parity test)"
  key_links:
    - "adapter.path_prompt_section resolves anchors via the injected LabelIndex.retrieve(...).type and targets via PredicateIndex.retrieve(...), then feeds them to ClassPathIndex.shortest_paths — this is the one adapter method that does MORE than delegate (D-02 / Pitfall 4)."
    - "NLQueryEngine._system_prompt seam-8 block placement (after seam-7) is the single authoritative composition point (Cypher inherits it)."
  prohibitions:
    - statement: "MUST NOT reimplement a token scorer inside the path seam — reuse seam-6/7's already-retrieved hits (D-02)"
      status: resolved
      verification: "test — the adapter path_prompt_section calls LabelIndex.retrieve / PredicateIndex.retrieve; no new scorer is introduced (code review + the '' -on-unresolved adapter test)"
    - statement: "MUST NOT modify seam-6 or seam-7 behavior — the path seam composes additively after the entity block"
      status: resolved
      verification: "test — existing seam-6/7 parity + recall guards stay green; the seam-8 block only appends"
---

<objective>
Wire the proven engine seam-8 into the prompt path: add the `path_index()`/
`path_prompt_section()` seams to the `QueryLanguageAdapter` Protocol, compose the
path block AFTER the entity/predicate blocks in `NLQueryEngine._system_prompt`,
implement both project adapters byte-identically, and thread `path_k`/`path_index`
through `NlPipeline` so the eval harness can reach the engine (Pitfall 5).

This is horizontal EXPANSION on top of Plan 01's green tracer — the mechanical lever
is already proven; this plan makes it reachable through the production orchestrator.
The adapter's `path_prompt_section` is the one place that does more than delegate: per
D-02 / Pitfall 4 it resolves anchor classes from the seam-6 grounded entity's
rdf:type and targets from the seam-7 token scorer, then feeds those PRE-RESOLVED
identifiers into `ClassPathIndex.shortest_paths(...)` — it never re-parses the raw
question and never introduces a third scorer.

Purpose: make the path hint appear in the composed prompt, after the entity block,
anchored on real seam-6/7 signals. Output: composition + both adapters + pipeline
threading + a 3rd byte-identical parity test.

Covers R1 (engine composition/Protocol) and R2 (adapter path seam + composition).
Cites D-02 (anchor/target resolution) and D-04 (join/star shape, provisional wording).

<assumption_delta_decision>no-change — seam-8 is an ADDITIVE third grounding seam composed after seams 6/7; it does not generalize or replace their identity model (CONTEXT.md constraint: "reuses seam-6/7 unchanged"), so this is additive composition, not a promote/add-alongside identity shift.</assumption_delta_decision>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-SPEC.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-PATTERNS.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-CONTEXT.md
@.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-01-SUMMARY.md

# Engine composition + Protocol (local checkout)
@$HOME/dev/arango-query-core/arango_query_core/nl/engine.py
@$HOME/dev/arango-query-core/arango_query_core/nl/seams.py

# Both project adapters (must stay byte-identical) + the orchestrator
@arango_sparql/nl2sparql/engine_adapter.py
@arango_sparql/nl2sparql/adapter.py
@arango_sparql/nl2sparql/pipeline.py
@tests/nl2sparql/test_grounding_prompt_parity.py
</context>

<artifacts_produced>
## Artifacts this plan produces

- Engine Protocol seam methods `path_index()` / `path_prompt_section()` on `QueryLanguageAdapter` (seams.py).
- `NLQueryEngine.path_k` field + the seam-8 composition block in `_system_prompt` (engine.py).
- `path_index()` / `path_prompt_section()` on `SparqlAdapter` (engine_adapter.py) and `SparqlLanguageAdapter` (adapter.py).
- `NlPipeline.__init__` `path_k` / `path_index` params + `.run()` threading (pipeline.py).
- 3rd `test_grounding_prompt_parity.py` path-parity test + a `''`-on-unresolved path test in test_engine_adapter.py.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine Protocol seam + seam-8 composition after the entity/predicate blocks</name>
  <files>~/dev/arango-query-core/arango_query_core/nl/seams.py, ~/dev/arango-query-core/arango_query_core/nl/engine.py, ~/dev/arango-query-core/tests/test_nl_engine.py</files>
  <read_first>
    - ~/dev/arango-query-core/arango_query_core/nl/seams.py (lines 109-138: grounding_index / grounding_prompt_section / predicate_index / predicate_prompt_section Protocol signatures — the exact shape to mirror for seam-8)
    - ~/dev/arango-query-core/arango_query_core/nl/engine.py (lines 51-139: NLQueryEngine dataclass fields grounding_k/predicate_k, and _system_prompt's grounding then predicate index-not-None → render → append blocks — seam-8 composes after the predicate block)
    - .planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-01-SUMMARY.md (the ClassPathIndex signature landed in Plan 01: shortest_paths(anchor_classes, targets, k) + format_prompt_section)
  </read_first>
  <behavior>
    - Given an adapter whose path_index() returns a ClassPathIndex and whose path_prompt_section returns a non-empty block, _system_prompt appends that block AFTER the seam-7 predicate block.
    - Given path_index() returns None, _system_prompt appends nothing (byte-identical to pre-seam-8 output).
    - Given path_index() non-None but path_prompt_section returns '', _system_prompt appends nothing.
  </behavior>
  <action>
    In seams.py add two Protocol members to `QueryLanguageAdapter` mirroring the seam-6/7 signature shape: `path_index(self) -> ClassPathIndex | None` (seam 8) and `path_prompt_section(self, question, index, k=5) -> str` (seam 8 renderer), with docstrings paralleling grounding_index/grounding_prompt_section. Import `ClassPathIndex` from `.grounding` or `.pathindex` per where Plan 01 placed it (Plan 01 chose pathindex.py).

    In engine.py add a `path_k: int = 5` field to `NLQueryEngine` (mirrors `grounding_k`/`predicate_k`) and, in `_system_prompt`, add the seam-8 block immediately after the seam-7 predicate block using the identical shape: fetch `self.adapter.path_index()`; if non-None, call `self.adapter.path_prompt_section(question, paths, k=self.path_k)`; append only if the returned block is truthy. Update the class docstring's per-question dynamic-content note to mention the path block composes after the predicate section.

    Add engine composition tests to ~/dev/arango-query-core/tests/test_nl_engine.py (or the existing engine-composition test module) with a fake adapter exercising the three behaviors above (path block after predicate block; None index → no block; '' render → no block). Write these tests FIRST (they fail until the composition block lands), then implement.
  </action>
  <verify>
    <automated>cd ~/dev/arango-query-core && uv run pytest tests/test_nl_engine.py -k path -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_nl_engine.py -k path -x` (engine repo) green: the seam-8 block appends after the seam-7 block when path_index resolves and a non-empty block renders.
    - `NLQueryEngine` has a `path_k: int = 5` field; `QueryLanguageAdapter` Protocol has `path_index` + `path_prompt_section`.
    - With path_index() → None OR path_prompt_section() → '', the composed system prompt is byte-identical to the pre-seam-8 output (no trailing block).
  </acceptance_criteria>
  <done>The engine Protocol carries seam-8 and _system_prompt composes the path block after the entity/predicate blocks, gated on a non-empty render — all on the local-editable engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both SPARQL adapters (byte-identical) + anchor/target resolution + parity + no-op tests</name>
  <files>arango_sparql/nl2sparql/engine_adapter.py, arango_sparql/nl2sparql/adapter.py, tests/nl2sparql/test_grounding_prompt_parity.py, tests/nl2sparql/test_engine_adapter.py</files>
  <read_first>
    - arango_sparql/nl2sparql/engine_adapter.py (lines 196-343: SparqlAdapter.__init__ injection fields + grounding_index/grounding_prompt_section/predicate_index/predicate_prompt_section; the TYPE_CHECKING-guarded PredicateIndex import at 60-70)
    - arango_sparql/nl2sparql/adapter.py (lines 75-193: SparqlLanguageAdapter dataclass fields _grounding_index/_predicate_index as field(default=None, repr=False), and the seam-6/7 methods — byte-identical twin of engine_adapter.py)
    - tests/nl2sparql/test_grounding_prompt_parity.py (the two existing parity tests + _index()/_predicate_index() fixtures — the exact pattern for a 3rd path-parity test)
    - .planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md (Pitfall 3: GroundedEntity.type is a LOCAL NAME; Pitfall 4 / D-02: resolve anchors from seam-6 .type + targets from seam-7 retrieve, feed pre-resolved ids to shortest_paths)
  </read_first>
  <behavior>
    - path_index() returns the injected ClassPathIndex (or None if never injected) on both adapters.
    - path_prompt_section(question, index, k=5): retrieves anchor classes from the injected seam-6 LabelIndex hits' .type (distinct types, D-02) and target tokens from the injected seam-7 PredicateIndex hits, calls index.shortest_paths(anchor_classes, targets, k=5), and returns index.format_prompt_section(...); returns '' when anchors or targets are empty or shortest_paths is empty.
    - SparqlAdapter.path_prompt_section(...) == SparqlLanguageAdapter.path_prompt_section(...) for identical fixtures (byte-identical parity).
  </behavior>
  <action>
    On both adapters add a `path_index` injection field (mirror the seam-7 pattern exactly — SparqlAdapter via an `__init__` kwarg `path_index: ClassPathIndex | None = None` stored as `self._path_index`; SparqlLanguageAdapter via `_path_index: ClassPathIndex | None = field(default=None, repr=False)`), and a TYPE_CHECKING-guarded `ClassPathIndex` import mirroring the existing guarded PredicateIndex import so both modules still load against the still-old engine pin between this plan and Plan 03's pin bump. Add `path_index(self)` returning the injected index (explicit-injection-only, same rationale as seam-6/7).

    Implement `path_prompt_section(self, question, index, k=5)` on BOTH adapters, byte-identical (enforced by the parity test): this method resolves the call args per D-02 / Pitfall 4 — it reads the adapter's own injected seam-6 grounding index and seam-7 predicate index, calls their `.retrieve(question, ...)`, collects the DISTINCT anchor class local names from the grounded entities' `.type` and the top target tokens from the retrieved predicates (do NOT restrict to strict top-1; the ≤5-path budget is the anti-distraction valve), then calls `index.shortest_paths(anchor_classes, targets, k=5)` and renders via `index.format_prompt_section(...)` supplying a provisional SPARQL header/instruction (D-04: join/star framing; wording provisional and tunable during the credentialed sweep, not frozen). Return '' when no anchor or no target resolves or shortest_paths is empty (honest no-op). Keep the two implementations byte-identical.

    Add a 3rd parity test to tests/nl2sparql/test_grounding_prompt_parity.py (test_path_prompt_section_byte_identical_across_adapters) mirroring the two existing ones: build both adapters against the same injected LabelIndex + PredicateIndex + ClassPathIndex + sentinel question, assert the two path_prompt_section outputs are equal and non-empty. Add a `''`-on-unresolved test to tests/nl2sparql/test_engine_adapter.py asserting path_prompt_section returns '' when the anchor or target does not resolve. Write both tests first.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv pip install -e ~/dev/arango-query-core >/dev/null && uv run pytest tests/nl2sparql/test_grounding_prompt_parity.py tests/nl2sparql/test_engine_adapter.py -k path -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/test_grounding_prompt_parity.py -k path -x` green: the 3rd parity test asserts SparqlAdapter.path_prompt_section == SparqlLanguageAdapter.path_prompt_section (equal + non-empty for the sentinel fixture).
    - `uv run pytest tests/nl2sparql/test_engine_adapter.py -k path -x` green: path_prompt_section returns '' when anchor/target unresolved.
    - Both adapters expose path_index() returning the injected ClassPathIndex (or None); anchor resolution reads GroundedEntity.type (local name) and targets read the seam-7 retrieve hits — no third scorer added.
    - The existing seam-6/7 parity tests stay green (seam-6/7 behavior unchanged).
  </acceptance_criteria>
  <done>Both SPARQL adapters carry a byte-identical path seam that resolves anchors from seam-6 and targets from seam-7 (D-02), renders the join/star hint (D-04), and no-ops honestly — parity-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Thread path_k / path_index through NlPipeline (Pitfall 5)</name>
  <files>arango_sparql/nl2sparql/pipeline.py, tests/nl2sparql/test_engine_adapter.py</files>
  <read_first>
    - arango_sparql/nl2sparql/pipeline.py (lines 128-199: NlPipeline.__init__ predicate_k/predicate_index fields at 139-163, and .run()'s SparqlAdapter(...) + NLQueryEngine(...) construction at 184-199 — the exact field-pair-in / thread-into-both-constructions-out shape to mirror)
    - .planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-RESEARCH.md (Pitfall 5: runner.py's NlPipeline(...) call raises TypeError: unexpected keyword argument if path_k/path_index are not accepted — a compile-time-visible signal, required for Plan 03's --dry-run)
  </read_first>
  <action>
    Add `path_k: int = 5` and `path_index: ClassPathIndex | None = None` to `NlPipeline.__init__` (mirroring the predicate_k/predicate_index injection branch at 139-163, including the explanatory comment that this is explicit-injection-only, honest no-op when omitted), store as `self.path_k`/`self.path_index`, and thread them into `.run()`: pass `path_index=self.path_index` into the `SparqlAdapter(...)` construction and `path_k=self.path_k` into the `NLQueryEngine(...)` construction (mirroring how predicate_index/predicate_k are threaded at 184-199). Add a TYPE_CHECKING-guarded ClassPathIndex import mirroring the existing PredicateIndex guard at lines 65-70. Extend the NlPipeline docstring's seam-history narration to mention seam-8 threads path_k/path_index the same way.

    Add a pipeline threading assertion to tests/nl2sparql/test_engine_adapter.py: construct NlPipeline with a scripted client + an injected ClassPathIndex and assert .run(...) does not raise (the seam reaches the engine); this is the offline structural proof that Plan 03's --dry-run will exercise.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/dev/arango-sparql-py && uv pip install -e ~/dev/arango-query-core >/dev/null && uv run pytest tests/nl2sparql/test_engine_adapter.py -k "path and pipeline" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - NlPipeline.__init__ accepts path_k and path_index; .run() threads path_index into SparqlAdapter(...) and path_k into NLQueryEngine(...).
    - Constructing NlPipeline with path_index= and calling .run() does NOT raise TypeError (Pitfall 5 resolved).
    - `uv run pytest tests/nl2sparql/test_engine_adapter.py -k "path and pipeline" -x` green.
  </acceptance_criteria>
  <done>NlPipeline threads path_k/path_index into both the adapter and the engine, so the eval harness's NlPipeline(...) call in Plan 03 reaches seam-8 without a TypeError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBox → prompt | The adapter renders ClassPathIndex path hints (built from TBox labels) into the system prompt reaching the LLM. |
| adapter identity | Both adapters must stay byte-identical; a silent divergence would ship different prompts for SPARQL depending on the call site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-01 | Tampering / Information Disclosure | path_prompt_section rendering TBox labels via ClassPathIndex.format_prompt_section | medium | mitigate | The renderer sanitizes every label in the engine (Plan 01, `_sanitize_label`); the adapter supplies only fixed header/instruction constants — no unsanitized label reaches the prompt. |
| T-07.6-03 | Tampering (prompt divergence) | SparqlAdapter vs SparqlLanguageAdapter path_prompt_section | medium | mitigate | 3rd byte-identical parity test in test_grounding_prompt_parity.py locks the two implementations together. |
| T-07.6-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages; the editable engine install is the local sibling checkout (pin bump deferred to Plan 03). |
</threat_model>

<verification>
- `uv run pytest tests/nl2sparql/test_grounding_prompt_parity.py -x` green (all 3 parity tests) — R2.
- `uv run pytest tests/nl2sparql/test_engine_adapter.py -k path -x` green (no-op + pipeline threading) — R2.
- `uv run pytest tests/test_nl_engine.py -k path -x` (engine) green — R1 composition.
- Existing seam-6/7 tests + tests/nl2sparql/eval/test_path_recall.py (Plan 01) stay green (no regression).
- SPARQL→AQL transpiler package untouched (`git diff --stat arango_sparql/translate/` empty).
</verification>

<success_criteria>
- Path block composes after the entity/predicate blocks (engine), anchored on seam-6 .type + seam-7 targets (D-02), rendered as a join/star hint (D-04), '' on no-op.
- Both adapters byte-identical (parity-locked); NlPipeline threads path_k/path_index end-to-end.
- Seam-6/7 behavior unchanged (additive composition).
</success_criteria>

<output>
Create `.planning/phases/07.6-nl-sparql-relationship-path-grounding/07.6-02-SUMMARY.md` when done.
</output>
