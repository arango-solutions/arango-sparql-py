---
phase: 07.6-nl-sparql-relationship-path-grounding
verified: 2026-08-10T20:51:14Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 07.6: NL→SPARQL Relationship-Path Grounding Verification Report

**Phase Goal:** Close the dominant CK25 NL→SPARQL "right-entity, wrong-path" failure mode by adding engine seam-8 (ClassPathIndex) — a mechanically-built, subclass-aware, inverse-edged TBox class-connectivity graph that retrieves the specific bounded shortest predicate path between a question's grounded anchor class (seam-6) and its target (seam-7), injected as a compact navigation hint after the entity block; mechanical/TBox-only so it transfers to CDF and Cypher inherits it; graded directionally + zero-regression on CK25 (n=49).

**Verified:** 2026-08-10T20:51:14Z
**Status:** passed
**Re-verification:** No — initial verification

## Framing Note (adjudicated per prompt instructions)

NL-ACC-03's own acceptance criteria explicitly permit closure via EITHER an adopted directional lift OR a documented-null. This phase closed via the documented-null path: the credentialed CK25 sweep's phase contrast (`g+f+path` vs `g+f`) came back b=0/c=1/p=1.0/Δ=−0.0204, so the accuracy lever did NOT adopt. Per the explicit instruction and the 07.4/07.5 precedent (NL-ACC-02, NL-FEW-02), this verification treats the honestly-measured null as goal achievement, not as a gap, and instead verifies that (a) the engineering artifact (seam-8) was actually built, tested, and promoted, and (b) the measurement was actually run with the no-regression discipline intact — i.e., the SUMMARY's documented-null claim is independently reproduced against the codebase, not merely trusted.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Engine seam-8 (`ClassPathIndex`/`ClassPath`) exists, is stdlib-only/pyoxigraph-free, subclass-aware + inverse-edged + bounded self-revisit, depth≤3, ≤5 global-pool, deterministic order, `''`-no-match renderer | ✓ VERIFIED | `python -c "import arango_query_core.nl.pathindex"` succeeds; `from arango_query_core.nl import ClassPathIndex, ClassPath` succeeds; `inspect.getsource` scan confirms no `pyoxigraph` string in module source; installed package's `direct_url.json` commit_id = `1ec67899e762dac4deef904c7324e06c61ead15b` (matches pin) |
| 2 | `build_path_index` mechanically walks TBox with no per-ontology hardcoded vocabulary | ✓ VERIFIED | `tests/nl2sparql/eval/test_path_recall.py::test_no_ontology_branch_in_build_path_index` — ran green (part of 3-test file run below) |
| 3 | Canonical inverse-join (Department→manager) recovered from real CK25 TBox before adapter wiring existed | ✓ VERIFIED | `tests/nl2sparql/eval/test_path_recall.py::test_canonical_inverse_join_recovered` green; assertion body confirms `("memberOf^-1", "hasManager")` in returned edge sequences, ≤5 paths, length ≤3 |
| 4 | 16-case empty-result gold fixture derived by execution-diff (not static text) | ✓ VERIFIED | `path_recall_golds.yml` exists under `tests/`; `test_path_recall_meets_spike_floor` asserts `14 <= len(golds) <= 16` and this passed |
| 5 | R4 offline path-recall gate ≥14/16 against local-editable/now-pinned engine, always-on, no key | ✓ VERIFIED | `uv run pytest tests/nl2sparql/eval/test_path_recall.py -x -q` → **3 passed** (canonical, recall-floor, no-ontology-branch), run independently by this verifier against the currently-pinned (non-editable) install |
| 6 | Path block composes AFTER entity(seam-6)/predicate(seam-7) blocks in `NLQueryEngine._system_prompt`, index-not-None→render→append-if-non-empty | ✓ VERIFIED | `~/dev/arango-query-core/tests/test_nl_engine.py` (25 tests) green; engine.py `path_k` field + composition block present per SUMMARY 02 and confirmed via test pass |
| 7 | `path_prompt_section` anchors on seam-6 `.type` (local name) + seam-7 retrieved predicate local names (D-02); no third scorer | ✓ VERIFIED | Direct source read of `arango_sparql/nl2sparql/adapter.py:214-259` and `engine_adapter.py:372-417`: both call `self._grounding_index.retrieve(...)` / `self._predicate_index.retrieve(...)`, feed into `index.format_prompt_section(...)`, no new scorer instantiated |
| 8 | `path_prompt_section` returns `''` on unresolved anchor/target or no path within depth-3 (honest no-op) | ✓ VERIFIED | Source shows explicit `if grounding is None or predicates is None: return ""` and `if not anchor_classes or not targets: return ""`; `tests/nl2sparql/test_engine_adapter.py` path-related tests (part of the 25 passed) exercise this |
| 9 | Path-block wording byte-identical across `SparqlAdapter` and `SparqlLanguageAdapter` | ✓ VERIFIED | Direct side-by-side Read of both files' `path_index`/`path_prompt_section` bodies (lines 205-259 vs 363-417) confirms byte-identical text; `tests/nl2sparql/test_grounding_prompt_parity.py` (3rd parity test) passed |
| 10 | `NlPipeline` threads `path_k`/`path_index` from constructor into `SparqlAdapter(...)`/`NLQueryEngine(...)` in `.run()`, no `TypeError` | ✓ VERIFIED | `pipeline.py` grep confirms `path_k: int = 5`, `path_index: ClassPathIndex | None = None` params, `self.path_k`/`self.path_index` storage, and threading at construction sites (`path_index=self.path_index`, `path_k=self.path_k`); pipeline-threading test in the 25-passed run |
| 11 | Composed `--dry-run` arm (`g+f+path`) builds all three seams into ONE `NlPipeline`, scripted 49/49, no crash, no regression on other arms | ✓ VERIFIED | `uv run python tests/nl2sparql/eval/run_composed_sweep.py --dry-run` run live by this verifier: all 6 scripted arms report 49/49 including `scripted-ck25-grounded-generated-fewshot-path` |
| 12 | Engine promoted (pushed to both remotes, ls-remote-verified, pin bumped) and the human-run credentialed sweep folded into `baseline.json` with an honest adopt/documented-null verdict; W3C ≥96.4% and transpiler package unchanged | ✓ VERIFIED | `git ls-remote arthur HEAD` and `git ls-remote origin HEAD` in `~/dev/arango-query-core` both return `1ec67899e762dac4deef904c7324e06c61ead15b`; `pyproject.toml` pins the same SHA in both `[nl]`/`[dense]` extras; `baseline.json:phase07_6_path_grounding_sweep` present with `engine_pin` matching, full `pass_counts`/`phase_contrast` (b=0,c=1,p=1.0,Δ=−0.0204) and `nl_acc_03_disposition` recording documented-null, not reframed; `uv run pytest tests/w3c/test_coverage_gate.py -x -q` → 1 passed; `git diff --stat arango_sparql/translate/` empty |

**Score:** 12/12 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `~/dev/arango-query-core/arango_query_core/nl/pathindex.py` (installed, pinned) | `ClassPathIndex`, `ClassPath` | ✓ VERIFIED | Imports cleanly from the pinned install; `direct_url.json` commit matches pin exactly |
| `tests/nl2sparql/eval/grounding_index_builder.py` (`build_path_index`) | mechanical TBox walker | ✓ VERIFIED | Function exists, exercised by green tests, no-ontology-branch scan passes |
| `tests/nl2sparql/eval/path_recall_golds.yml` | 16-case gold fixture | ✓ VERIFIED | File exists, loaded by the recall gate, count asserted 14-16 |
| `tests/nl2sparql/eval/test_path_recall.py` | R4 gate + purity/no-ontology scans | ✓ VERIFIED | 3 tests, all pass |
| `~/dev/arango-query-core/tests/test_nl_pathindex.py` | engine unit tests | ✓ VERIFIED | 14 tests collected and passing, including `test_module_is_pyoxigraph_free` |
| `arango_sparql/nl2sparql/engine_adapter.py` + `adapter.py` | `path_index`/`path_prompt_section` on both adapters | ✓ VERIFIED | Both present, byte-identical (direct comparison) |
| `arango_sparql/nl2sparql/pipeline.py` | `path_k`/`path_index` threading | ✓ VERIFIED | Params, storage, and threading confirmed by grep/read |
| `tests/nl2sparql/eval/runner.py` | additive `path_grounding:` config read | ✓ VERIFIED | `--dry-run` proves the composed arm builds via this read (indirect but functional proof) |
| `tests/nl2sparql/eval/configs.yml` | `-path` composed arm + scripted twin | ✓ VERIFIED | `--dry-run` output lists `scripted-ck25-grounded-generated-fewshot-path` at 49/49 |
| `pyproject.toml` + `uv.lock` | pin bumped off `3438305` | ✓ VERIFIED | Both `[nl]`/`[dense]` extras pin `1ec67899e762dac4deef904c7324e06c61ead15b` |
| `tests/nl2sparql/eval/baseline.json` | `phase07_6_path_grounding_sweep` key | ✓ VERIFIED | Key present with full evidence fields (arms, pass_counts, phase_contrast, disposition) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `build_path_index` (TBox walk) | `ClassPathIndex.from_items` → `shortest_paths` | R4 recall gate reads `path_recall_golds.yml` | ✓ WIRED | Green test run confirms the full chain executes and produces ≥14/16 hits |
| Adapter `path_prompt_section` | seam-6 `LabelIndex.retrieve` / seam-7 `PredicateIndex.retrieve` | anchor/target resolution, then `ClassPathIndex.shortest_paths` | ✓ WIRED | Source inspection confirms both retrieve calls precede the `shortest_paths`/`format_prompt_section` call, on both adapters |
| `NlPipeline.run()` | `SparqlAdapter(...)` / `NLQueryEngine(...)` | `path_index=self.path_index`, `path_k=self.path_k` | ✓ WIRED | Grep confirms both kwargs threaded at construction call sites |
| `runner.py path_grounding:` read | `build_path_index(shared_ontology)` → `NlPipeline(path_index=...)` | the composed arm's `--dry-run` smoke | ✓ WIRED | Live `--dry-run` run by this verifier shows the composed arm builds without crash at 49/49 |
| Plan 01 recall-green | Plan 03 engine promotion → credentialed sweep → `baseline.json` fold-in | D-01 sequencing | ✓ WIRED | R4 was green pre-promotion (SUMMARY 01/02) and re-confirmed green post-pin-bump (this verifier's own run against the now-pinned install); both remotes carry the exact promoted SHA |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Engine importable + pyoxigraph-free at the pinned SHA | `python -c "import arango_query_core.nl.pathindex"` + source scan | imports cleanly; no `pyoxigraph` string in source | ✓ PASS |
| R1 canonical inverse-join + R4 recall gate | `uv run pytest tests/nl2sparql/eval/test_path_recall.py -x -q` | 3 passed | ✓ PASS |
| R2 adapter parity + no-op + pipeline threading | `uv run pytest tests/nl2sparql/test_grounding_prompt_parity.py tests/nl2sparql/test_engine_adapter.py -x -q` | 25 passed | ✓ PASS |
| Engine-side unit tests (pathindex + engine composition) | `cd ~/dev/arango-query-core && uv run pytest tests/test_nl_pathindex.py tests/test_nl_engine.py -q` | 25 passed | ✓ PASS |
| R3 composed `--dry-run` smoke, all 6 arms | `uv run python tests/nl2sparql/eval/run_composed_sweep.py --dry-run` | all 6 scripted arms 49/49, including the new path arm | ✓ PASS |
| Both remotes carry the promoted SHA | `git ls-remote arthur HEAD` / `git ls-remote origin HEAD` (in engine repo) | both return `1ec67899e762dac4deef904c7324e06c61ead15b` | ✓ PASS |
| W3C non-regression | `uv run pytest tests/w3c/test_coverage_gate.py -x -q` | 1 passed | ✓ PASS |
| Transpiler package untouched | `git diff --stat arango_sparql/translate/` | empty | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| NL-ACC-03 | 07.6-01, 07.6-02, 07.6-03 | Relationship-path grounding (seam-8) | ✓ SATISFIED (documented-null closure, per explicit acceptance-criteria alternative) | REQUIREMENTS.md line 56: `[x]` closed, "Complete (documented-null)"; `baseline.json:phase07_6_path_grounding_sweep` carries the full honest evidence trail (b=0/c=1/p=1.0/Δ=−0.0204), matching what REQUIREMENTS.md claims verbatim |

No orphaned requirements found for Phase 07.6 in REQUIREMENTS.md.

### Anti-Patterns Found

Scanned all files modified across the three plans (`grounding_index_builder.py`, `derive_path_golds.py`, `path_recall_golds.yml`, `test_path_recall.py`, `engine_adapter.py`, `adapter.py`, `pipeline.py`, `test_grounding_prompt_parity.py`, `test_engine_adapter.py`, `runner.py`, `configs.yml`, `run_composed_sweep.py`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|not yet implemented|coming soon`.

**None found.** No debt markers, no stub returns, no empty handlers in the wired path.

### Human Verification Required

None. All must-haves are mechanically verifiable (imports, test runs, git remote state, source inspection, JSON content) and were independently re-run/re-read by this verifier rather than trusted from SUMMARY.md.

### Gaps Summary

No gaps. All 12 must-haves across the three plans (tracer, wiring, promotion+sweep) were independently verified against the live codebase and the sibling `arango-query-core` checkout/remotes:

- The engine seam (`ClassPathIndex`) exists at the exact pinned commit on both remotes, is stdlib-only/pyoxigraph-free, and its 14 unit tests plus the 3 eval-side R4 tests all pass when run fresh.
- Both SPARQL adapters carry byte-identical seam-8 implementations correctly anchored on seam-6/seam-7 signals, verified by direct source diff (not just a passing parity test).
- `NlPipeline` threading is confirmed by source read, not just SUMMARY claim.
- The composed `--dry-run` arm was re-run live and shows 49/49 with no regression on any of the other 5 arms.
- `baseline.json`'s `phase07_6_path_grounding_sweep` key contains a complete, internally consistent evidence record (engine_pin matches the actual promoted/installed SHA; phase_contrast numbers match what SUMMARY 03 and REQUIREMENTS.md both state) — the documented-null verdict is not a reframed pass; it is recorded honestly and matches the codebase's actual measured contrast.
- W3C non-regression and transpiler-untouched checks both pass independently.

Per the explicit framing given in this verification task (and the NL-ACC-02/NL-FEW-02 precedent), the documented-null outcome on the accuracy lever itself is not scored as a gap — it is the acceptance criteria's own permitted closure path, and the underlying engineering deliverable (seam-8, fully built/tested/promoted) is real and independently confirmed.

---

_Verified: 2026-08-10T20:51:14Z_
_Verifier: Claude (gsd-verifier)_
