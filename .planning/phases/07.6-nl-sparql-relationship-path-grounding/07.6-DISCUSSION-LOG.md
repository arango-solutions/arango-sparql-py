# Phase 07.6: NL→SPARQL relationship-path grounding - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-07
**Phase:** 07.6-nl-sparql-relationship-path-grounding
**Areas discussed:** Build sequencing, Anchor/target resolution, Path-surface budget, Renderer presentation

---

## Build sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Build it in the engine directly | Build the real ClassPathIndex straight into the shared engine (`arango_query_core`); prove it with the committed recall gate against the real seam; publish + pin-bump only after ≥14/16. No throwaway prototype (Step-0 already validated offline). | ✓ |
| Build a temporary copy first | Build a throwaway version in the test folder, pass the recall gate there, then port to the engine + pin-bump. Extra safety, likely redundant. | |
| You decide | Leave the order to planning/research. | |

**User's choice:** Build it in the engine directly.
**Notes:** Re-asked in plain English at the user's request. Rationale accepted: the Step-0 offline recall spike already validated the hypothesis, so 07.5's eval-prototype-first stage is redundant; recall-gate-green must still precede the (heavyweight, two-remote) pin bump.

---

## Anchor/target resolution

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct types × top targets, capped | Every distinct grounded `rdf:type` as an anchor + top few seam-7 targets as destinations; ≤5-path budget trims. Best recall for multi-hop/inverse cases. | ✓ |
| Strict top-1 start, top-1 destination | Only the single best entity's type + single top target. Tightest, but risks missing cases where the right anchor isn't top-ranked. | |
| You decide | Leave the selection policy to planning/research. | |

**User's choice:** Distinct types × top targets, capped.
**Notes:** ≤5-path budget is the safety valve against flooding, not a hard top-1 cut.

---

## Path-surface budget

| Option | Description | Selected |
|--------|-------------|----------|
| One global pool, 5 best overall | Pool all candidate paths across combos, rank by locked ordering (length → token overlap → lexical), take top 5. Simple, deterministic. | ✓ |
| Per-destination fair share | Guarantee each target ≥1 path before filling remaining slots. More balanced, more complex, can surface weaker paths. | |
| You decide | Leave allocation to planning/research. | |

**User's choice:** One global pool, 5 best overall.
**Notes:** Concrete realization of the D-5 ≤5-path anti-distraction knob across multiple anchor/target pairs.

---

## Renderer presentation

| Option | Description | Selected |
|--------|-------------|----------|
| Lock the shape, tune wording by test | Lock the shared-variable join form (not a directed walk), compact, ≤5, after the entity block; exact phrasing tunable during the human-run sweep. | ✓ |
| Freeze exact wording now | Pin the precise template string/header this phase. More predictable, against D-7. | |
| You decide | Leave renderer wording entirely to planning/research. | |

**User's choice:** Lock the shape, tune wording by test.
**Notes:** Follows D-7 ("test, don't freeze") and the 07.4 lesson that wording is itself a distraction lever.

---

## Claude's Discretion

- `path_grounding:` config-block field naming/shape (mirror `grounding:`/`predicate_grounding:`).
- Class-connectivity graph internal representation + shortest-path enumeration algorithm (within depth ≤3, ≤5, D-9 subclass-aware, D-10 self-revisit, deterministic ordering).
- Offline recall-gate fixture format + source of the 16 gold nav-paths (test-only; must not leak CK25 vocab into the engine).
- Exact `format_prompt_section` wording.

## Deferred Ideas

None — discussion stayed within phase scope. Adjacent levers already logged out-of-scope in SPEC.md (synthbank v2 / catalog expansion, fine-tuning, judge relaxations, QALD floor, depth-≥4 paths).
