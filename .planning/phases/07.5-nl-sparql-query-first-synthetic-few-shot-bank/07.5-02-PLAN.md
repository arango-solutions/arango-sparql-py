---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 02
type: execute
wave: 2
depends_on: ["07.5-01"]
files_modified:
  - tests/nl2sparql/eval/bank_generator.py
  - tests/nl2sparql/eval/test_bank_generator.py
  - tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml
  - tests/nl2sparql/eval/reports/generation_report_ck25.json
autonomous: true
requirements: [NL-GEN-01]
user_setup: []

must_haves:
  truths:
    - "The generator emits a valid CK25 bank query-first: every query parses, transpiles, and executes non-empty against prod-inst.ttl"
    - "At least 7 (target 9) distinct compositional shapes appear in the CK25 bank, all name-anchored (no hardcoded instance IRIs)"
    - "Per-shape yield is a first-class report: a hard shape yielding ~0 is a measured, logged finding — not a hidden gap"
  artifacts:
    - path: "tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml"
      provides: "generated CK25 few-shot bank (version + examples), name-anchored"
      contains: "examples:"
    - path: "tests/nl2sparql/eval/reports/generation_report_ck25.json"
      provides: "per-shape yield report (kept/dropped counts + drop reasons)"
      contains: "yield"
    - path: "tests/nl2sparql/eval/bank_generator.py"
      provides: "generate_bank pipeline: templates -> slot-fill -> data-bind -> execution non-empty filter -> strict-extremum gate"
      contains: "def generate_bank"
  key_links:
    - from: "tests/nl2sparql/eval/bank_generator.py"
      to: "tests.helpers.oxi.oxi_query + load_store_from_string"
      via: "slot sampling + execution non-empty filter (function-local import)"
      pattern: "oxi_query"
    - from: "tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml"
      to: "verify_generated_bank.py"
      via: "offline validity gate (parse+transpile+execute-non-empty)"
      pattern: "generated_fewshot_bank"
---

<objective>
Wave 1 of the Stage-1 eval prototype: build the generator core and emit the first real CK25 bank. Author the 9 name-anchored `ShapeTemplate` closures (per D-02's full shape set incl. grouped-aggregation SEPARATE from scalar COUNT), then implement `generate_bank`: slot-fill from the TBox walker + eval-side signals, data-bind by sampling the live instance store, execution-filter to keep only non-empty survivors, and apply the strict-unique-extremum gate to ranking shapes. Emit `vendored/ck25/generated_fewshot_bank.yml` and a first-class per-shape yield report. This closes REQ-1 (valid, >=7 shapes) and REQ-2 (100% non-empty) offline.

Purpose: Reproduce the VALIDATED spike's winning example form (name-anchored composition, entity-disjoint) from a schema-agnostic generator — the central bet REQ-6 later measures.
Output: generate_bank pipeline, CK25 bank, per-shape yield report, all passing the offline validity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@tests/nl2sparql/eval/bank_generator.py
@tests/nl2sparql/eval/grounding_index_builder.py
@.planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml
@.planning/spikes/001-ck25-thin-fewshot-signal/verify_bank.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the 9 name-anchored ShapeTemplate closures (applies + build_sparql + slots)</name>
  <files>tests/nl2sparql/eval/bank_generator.py, tests/nl2sparql/eval/test_bank_generator.py</files>
  <read_first>
    - .planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml (the PROVEN example form — every query is name-anchored `?x pv:name "V"`, entity-disjoint; the 3 shapes 2-hop / scalar-COUNT / top-N are exact reference SPARQL to generalize)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (the "Shape Catalog" 9-row table — each shape's SPARQL skeleton, the TBox signal it needs, construction notes, and yield risk)
    - tests/nl2sparql/eval/grounding_index_builder.py (GroundedPredicate fields kind/domain/range/shape/shape_detail + the Plan-01 PredicateSignals orderable/optional_relation the applies-gates read)
  </read_first>
  <behavior>
    - Each of the 9 catalog shapes has an `applies(pred, index, signals)` gate reading ONLY GroundedPredicate fields + eval-side signals + sampled data (never a hardcoded vocab term)
    - Each `build_sparql(binding)` emits NAME-ANCHORED SPARQL (`?x pv:name "V"` / `?x rdfs:label "..."`), never an instance-namespace IRI
    - grouped_aggregation emits GROUP BY + HAVING(?n > K) and is a DISTINCT shape from scalar_count (spike carry-forward #2)
    - top_n / offset gates require an orderable literal; negation gate requires optional_relation True
    - semantic_slots + intent_lexicon are populated per shape (top-N => superlative token; COUNT => "how many"/"number of"; negation => "without"/"no"/"lacking"; grouped-agg => per-group + threshold phrasing)
  </behavior>
  <action>
    Populate the 9 SHAPE_CATALOG entries reserved in Plan 01, generalizing the spike bank's three proven shapes and adding the six others per the RESEARCH Shape Catalog table. Each `build_sparql` closure MUST name-anchor every entity slot via the class's label predicate (reuse the `_build_label_map`/`build_label_index` concept — rdfs:label covers pv:name, verified 0 counterexamples) and MUST NOT emit a `prodi:`-style instance IRI (Pitfall 3 / spike carry-forward #1). Each `applies` gate reads only GroundedPredicate.kind/domain/range/shape/shape_detail plus the Plan-01 PredicateSignals (orderable for top_n/offset/grouped_aggregation; optional_relation for negation) — no term-name special-casing (D-02 / SPEC schema-agnostic constraint). Fill `question_template`, `semantic_slots`, and `intent_lexicon` for each shape (the D-03 faithfulness ground truth). Add test_bank_generator.py cases: for a fixed CK25 predicate index + signals, each applicable shape's build_sparql produces SPARQL that `_canonical(...) is not None` (parses) and contains no instance-namespace IRI; grouped_aggregation is present and distinct from scalar_count.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_bank_generator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -q` passes (offline, no key, no network)
    - Test asserts every shape's build_sparql output parses via `_canonical` and contains no `prodi:`/instance-namespace IRI (name-anchored only)
    - Test asserts grouped_aggregation exists and emits GROUP BY + HAVING, distinct from scalar_count (D-02 / spike carry-forward #2)
    - `grep -c "pv:name\|rdfs:label" ` is not gated on the module, but no shape closure contains a hardcoded CK25 vocabulary term (e.g. no literal `"Resistor"`/`"Engineering"` baked into a closure) — verified by test that runs the same closures against a second (synthetic) index
  </acceptance_criteria>
  <done>All 9 name-anchored shape templates exist with machine-readable slots and schema-agnostic applies-gates; parse-clean and instance-IRI-free.</done>
</task>

<task type="auto">
  <name>Task 2: Implement generate_bank pipeline; emit CK25 bank + per-shape yield report; pass the offline validity gate</name>
  <files>tests/nl2sparql/eval/bank_generator.py, tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml, tests/nl2sparql/eval/reports/generation_report_ck25.json, tests/nl2sparql/eval/test_bank_generator.py</files>
  <read_first>
    - .planning/spikes/001-ck25-thin-fewshot-signal/verify_bank.py (the execution non-empty filter lines 114-120 + the top-N strict-extremum probe lines 122-136 — the weight_g=20.0 saturation lesson: drop tied/saturated orderables)
    - tests/nl2sparql/eval/verify_generated_bank.py (the Plan-01 gate this bank must pass — its per-example `probe` field the ranking shapes must emit)
    - tests/nl2sparql/eval/grounding_index_builder.py (function-local `load_store_from_string`/`oxi_query` reuse pattern at line 237 — load the store ONCE outside the per-candidate loop, RESEARCH)
    - tests/nl2sparql/eval/vendored/ck25/corpus.yml + raw/prod-inst.ttl (the CK25 ontology + instance data the generator binds against)
  </read_first>
  <action>
    Implement `generate_bank(ontology_ttl, data_ttl, *, k_paraphrases=3, seed=0)` (leave paraphrase generation as a no-op / single templated question here — paraphrases land in Plan 03; this plan emits question + query). Pipeline: build the predicate index (build_predicate_index) + signals (build_predicate_signals); for each (shape, applicable predicate) enumerate real slot fillers by querying the instance store with a function-local `oxi_query` (e.g. all category names, department names, supplier countries), using a seeded `random.Random(seed)` bounded sampler (cap per shape) so regeneration is reproducible (Q7); prefer high-cardinality/discriminating fillers. Load the store ONCE via `load_store_from_string(data_ttl)` outside the loop. Instantiate each candidate name-anchored, execute against the store, and KEEP ONLY non-empty survivors (REQ-2). For ranking shapes run the strict-extremum probe (drop LIMIT, ORDER BY DESC/ASC, require rank1 strictly > rank2) and DROP saturated/tied orderables with a logged reason (the weight_g=20.0 lesson, Pitfall 2); emit the `probe` metadata verify_generated_bank.py consumes. Emit `vendored/ck25/generated_fewshot_bank.yml` as `version: 1` + `examples: [{question, query, probe?}]` ONLY (no `ontology:` block — Pitfall re test_bank_ontology_matches_corpus). Write `reports/generation_report_ck25.json` with per-shape kept/dropped counts + drop reasons (D-02 per-shape YIELD is first-class; Pitfall 6 — a silent 0-yield shape is forbidden). Add a test asserting the emitted CK25 bank has >=7 (target 9) distinct shapes and that generation_report_ck25.json lists every catalog shape (present or dropped-with-reason).
  </action>
  <verify>
    <automated>uv run python -c "from tests.nl2sparql.eval.bank_generator import generate_bank; from pathlib import Path; import yaml; b=generate_bank(yaml.safe_load(Path('tests/nl2sparql/eval/vendored/ck25/corpus.yml').read_text())['ontology'], Path('tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl').read_text()); print('examples', len(b['examples']))" && uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl` exits 0 — 100% parse+transpile+execute-non-empty, 0 empty examples (REQ-1 + REQ-2)
    - `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -q` passes; asserts >=7 (target 9) distinct shapes present in the CK25 bank
    - `reports/generation_report_ck25.json` exists and lists every one of the 9 catalog shapes with a kept-count or an explicit drop-reason (D-02 / Pitfall 6) — `grep -q "drop" tests/nl2sparql/eval/reports/generation_report_ck25.json` OR every shape has kept>0
    - No generated query contains an instance-namespace IRI (the verify gate's name-anchoring guard passes)
    - Regeneration with the same seed is byte-stable (re-running generate_bank yields identical examples)
  </acceptance_criteria>
  <done>A schema-agnostic generator emits a valid, name-anchored, 100%-non-empty CK25 bank with a first-class per-shape yield report; the offline validity gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time LLM call | none in this plan (paraphrase deferred to Plan 03; offline only) |
| vendored TBox/instance data -> pyoxigraph | trusted repo data; the generator executes only generator-authored SPARQL over vendored CK25 data |
| generated bank YAML | trusted-repo data injected verbatim into few-shot prompts downstream (same trust tier as the curated fewshot_bank.yml) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-03 | Tampering | pyoxigraph on engine import path | mitigate | all oxi imports function-local in bank_generator.py; grep gate |
| T-07.5-04 | Information disclosure | name-anchored emission leaking a snapshot instance IRI | mitigate | verify gate asserts no instance-namespace IRI in any generated query (Pitfall 3) |
| T-07.5-05 | Tampering | untrusted YAML | mitigate | yaml.safe_load only (existing _load_bank/_load_corpus discipline) |
| T-07.5-SC | Tampering | package installs | accept | no installs this phase (RESEARCH audit N/A) |
</threat_model>

<verification>
- Offline validity gate green on the emitted CK25 bank (parse+transpile+execute-non-empty).
- `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -q` green (>=7 shapes, yield report complete).
- W3C DAWG non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -q` still green.
- `git diff --name-only | grep -c '^arango_query_core/'` returns 0.
</verification>

<success_criteria>
- generate_bank emits a valid CK25 bank query-first: 100% non-empty, name-anchored, >=7 (target 9) shapes.
- Per-shape yield report is first-class (kept/dropped + reasons); no silent shape drop.
- Reproducible under a fixed seed; offline gate green; engine untouched; no installs; no secrets.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-02-SUMMARY.md` when done.
</output>
