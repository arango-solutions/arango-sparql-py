---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 06
type: execute
wave: 6
depends_on: ["07.5-05"]
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/nl2sparql/eval/bank_generator.py
autonomous: false
requirements: [NL-GEN-01]
user_setup: []

must_haves:
  truths:
    - "CONDITIONAL: this plan executes ONLY if the Plan 05 SPEC adopt bar cleared (b>0, c==0, delta>0 surviving overlap-exclusion, coverage>=70%, QALD non-regression). If Plan 05 closed as a documented null/kill, this plan is NOT executed"
    - "The pure construction core (template catalog + slot-filling from a PredicateIndex) is promoted to an arango_query_core seam; Cypher inherits it; pyoxigraph stays OUT of the engine (test-only)"
    - "The engine pin bump is verified fetchable via git ls-remote before uv lock; W3C DAWG >=96.4% and the transpiler package stay unchanged"
  artifacts:
    - path: "pyproject.toml"
      provides: "arango-query-core git-pin bumped to the seam commit (verified fetchable)"
      contains: "arango-query-core"
  key_links:
    - from: "arango_query_core.nl (new seam)"
      to: "the promoted pure template-catalog + slot-filling core"
      via: "engine seam (pyoxigraph-free); onboarding caller supplies pre-bound values or an execution hook (OQ-2)"
      pattern: "arango_query_core"
---

<objective>
Stage 2 (CONDITIONAL, per locked D-01): the PRODUCT deliverable — engine promotion — executed ONLY if the Plan 05 credentialed sweep cleared the SPEC adopt bar. The eval prototype validated the HYPOTHESIS; this plan ships the reusable construction core into arango_query_core.nl (so Cypher inherits it and it transfers to CDF), and bumps the arango-sparql-py pyproject.toml git pin — following the 07.3/07.4 eval-side-builder-first, engine-seam-later precedent. The exact promotion boundary is RESEARCH OQ-2, left as an explicit in-plan decision (data-binding/execution needs pyoxigraph, a test-only dep, so it CANNOT go in the engine; only template-catalog + slot-filling promote; the onboarding caller supplies pre-bound values or an execution hook). Do NOT conflate a clean CK25/QALD measurement with a working production capability (D-01's explicit warning).

Purpose: Turn the validated eval prototype into a build-time onboarding capability for real new ontologies, engine-side, Cypher-inheritable.
Output: engine seam (arango_query_core), pin bump, thin eval-side shim delegating to the promoted core.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-05-SUMMARY.md
@tests/nl2sparql/eval/bank_generator.py
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: GATE — confirm adopt bar cleared, then decide the OQ-2 promotion boundary</name>
  <read_first>
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-05-SUMMARY.md (the Plan 05 adopt/kill disposition — this plan runs ONLY if ADOPT)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (OQ-2: what exactly promotes; Stage 2 section — pyoxigraph is test-only, cannot enter the engine)
    - .planning/STATE.md (07.3-02 pin-bump precedent — verify fetchability via git ls-remote before uv lock; 07.4-06 upstream-seam precedent)
  </read_first>
  <action>
    FIRST verify the Plan 05 adopt/kill disposition from 07.5-05-SUMMARY.md. If NOT ADOPT, HALT — record that Stage 2 is not triggered and make no engine change. If ADOPT, present the OQ-2 promotion-boundary options below and record the chosen boundary (which pyoxigraph-free surface promotes; how the onboarding caller supplies data-binding) before Task 2 proceeds.
  </action>
  <decision>First: did Plan 05 clear the SPEC adopt bar (b>0, c==0, delta>0 surviving overlap-exclusion, coverage>=70%, QALD non-regression)? If NOT (documented null/kill), STOP — do not execute this plan; record that Stage 2 is not triggered. If ADOPT: decide the OQ-2 promotion boundary — exactly which pure (pyoxigraph-free) surface promotes to arango_query_core, and how the onboarding caller supplies data-binding.</decision>
  <context>D-01 gates Stage 2 strictly behind the adopt bar; OQ-2 is an explicit unresolved boundary the RESEARCH deferred to this plan. Data-binding + execution-filter depend on pyoxigraph (a test-only dep, Pitfall 5) and instance data — they CANNOT live in the engine.</context>
  <options>
    <option id="promote-template-core-only">
      <name>Promote template-catalog + slot-filling core only (RESEARCH recommendation)</name>
      <pros>Keeps pyoxigraph out of the engine; minimal engine surface; Cypher inherits the schema-agnostic construction logic; data-binding stays eval-side</pros>
      <cons>Onboarding caller must supply pre-bound slot values or an execution hook — not fully hands-free</cons>
    </option>
    <option id="promote-with-execution-hook">
      <name>Promote core + a caller-provided execution-hook Protocol</name>
      <pros>A real onboarding pipeline can wire its own SPARQL engine; still pyoxigraph-free in the engine package</pros>
      <cons>Larger seam surface; a new Protocol to design + version</cons>
    </option>
    <option id="defer-stage2">
      <name>Defer Stage 2 to a follow-up phase</name>
      <pros>Ships the validated eval prototype now; lets a dedicated phase design the onboarding caller</pros>
      <cons>The product deliverable (real-ontology onboarding) slips</cons>
    </option>
  </options>
  <resume-signal>Select: promote-template-core-only, promote-with-execution-hook, or defer-stage2 — or confirm KILL/NULL (Stage 2 not triggered).</resume-signal>
  <acceptance_criteria>
    - The Plan 05 adopt/kill disposition is confirmed from 07.5-05-SUMMARY.md before any engine change
    - If not ADOPT: this plan is explicitly not executed (recorded); no pin bump, no engine change
    - If ADOPT: the OQ-2 promotion boundary is chosen and recorded (which surface promotes; how data-binding is supplied)
  </acceptance_criteria>
  <done>Adopt confirmed (or plan halted); the OQ-2 promotion boundary is an explicit, recorded decision.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the pure construction core to arango_query_core.nl; bump the pin (git ls-remote verified) + uv lock</name>
  <files>pyproject.toml, uv.lock, tests/nl2sparql/eval/bank_generator.py</files>
  <read_first>
    - tests/nl2sparql/eval/bank_generator.py (the pure template-catalog + slot-filling surface to promote; bank_generator becomes a thin eval-side shim delegating to the engine core + retaining pyoxigraph-only data-binding)
    - .planning/STATE.md (07.3-02: pin bumped only after `git ls-remote refs/heads/main` confirmed fetchability on BOTH remotes; uv lock caught up pre-existing staleness; 07.4-06: upstream seam pushed to both remotes before the pin bump)
    - arango_query_core/nl/grounding.py (the seam-6/seam-7 style to mirror for the new seam — frozen dataclasses, no pyoxigraph)
  </read_first>
  <action>
    Per the Task-1 OQ-2 decision, port the pure (pyoxigraph-free) template-catalog + slot-filling core into a new arango_query_core.nl seam (mirror the seam-6/seam-7 addition style; schema-agnostic; Cypher inherits). Push the engine seam to origin/main (both remotes per 07.4-06) and VERIFY fetchability via `git ls-remote refs/heads/main` BEFORE bumping (07.3-02 precedent). Bump arango-sparql-py's pyproject.toml git pin to the seam commit, run `uv lock` (both extras), and refactor tests/nl2sparql/eval/bank_generator.py into a thin shim that imports the promoted core for template construction while KEEPING data-binding + execution-filter + paraphrase eval-side (pyoxigraph stays test-only — Pitfall 5 / D-08). All offline gates from Plans 01-04 must stay green post-refactor. Non-regression: W3C DAWG >=96.4% and the SPARQL->AQL transpiler package unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_bank_generator.py tests/w3c/test_coverage_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote` fetchability of the new pin was verified BEFORE `uv lock` (recorded in the SUMMARY — 07.3-02 precedent)
    - `uv run pytest tests/nl2sparql/eval/test_bank_generator.py tests/w3c/test_coverage_gate.py -q` passes (generator gates + W3C DAWG >=96.4% intact)
    - `uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl` still exits 0 (CK25 bank unchanged post-promotion)
    - `grep -rnE '^(import pyoxigraph|from pyoxigraph)' arango_query_core/` (via the installed package) returns nothing — no pyoxigraph on the engine import path (Pitfall 5)
    - The SPARQL->AQL transpiler package is unchanged (git-diff shows no transpiler-package file touched)
  </acceptance_criteria>
  <done>The pure construction core is engine-side (Cypher-inheritable), the pin is bumped fetchability-verified, pyoxigraph stays test-only, and all non-regression gates hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml git pin -> arango-query-core | already-trusted dependency; a git-pin BUMP (not a registry install), fetchability-verified before uv lock |
| eval shim -> engine seam | tests/ may import arango_query_core; pyoxigraph stays eval-side (never promoted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-14 | Tampering | supply-chain: unfetchable / wrong pin commit | mitigate | `git ls-remote refs/heads/main` on both remotes before uv lock (07.3-02 precedent); pin is a bump of an already-trusted dep, not a new registry install |
| T-07.5-15 | Tampering | pyoxigraph promoted into the engine package | mitigate | only the pure template-core promotes; grep gate asserts no pyoxigraph on the engine import path (Pitfall 5 / D-08) |
| T-07.5-16 | Denial of service | transpiler / W3C regression from the pin bump | mitigate | W3C DAWG >=96.4% gate + transpiler-package-unchanged git-diff check |
| T-07.5-SC | Tampering | package installs | accept | no NEW registry install — only a git-pin bump of the already-audited arango-query-core (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- CONDITIONAL: executed only on Plan 05 ADOPT; otherwise recorded as not-triggered.
- `uv run pytest tests/nl2sparql/eval/test_bank_generator.py tests/w3c/test_coverage_gate.py -q` green.
- CK25 bank still passes verify_generated_bank.py post-promotion.
- No pyoxigraph on the engine import path; transpiler package unchanged; git ls-remote verified before uv lock.
</verification>

<success_criteria>
- Adopt gate honored (D-01): Stage 2 runs only if the SPEC bar cleared.
- Pure construction core promoted to arango_query_core (Cypher inherits); pyoxigraph stays test-only.
- Pin bumped fetchability-verified; W3C DAWG >=96.4% and transpiler package unchanged.
- OQ-2 boundary is an explicit recorded decision.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-06-SUMMARY.md` when done.
</output>
