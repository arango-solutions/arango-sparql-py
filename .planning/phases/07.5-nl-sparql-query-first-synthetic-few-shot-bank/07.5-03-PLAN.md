---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 03
type: execute
wave: 3
depends_on: ["07.5-02"]
files_modified:
  - tests/nl2sparql/eval/bank_generator.py
  - tests/nl2sparql/eval/test_bank_generator.py
  - tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml
autonomous: true
requirements: [NL-GEN-01]
user_setup: []

must_haves:
  truths:
    - "Each generated query carries a templated question plus K>=3 paraphrases (scripted/placeholder on the offline path here; regenerated for real in Plan 05), all mapping to that same query"
    - "The PRIMARY faithfulness guard is offline and deterministic: a paraphrase that drops or flips a semantic slot / intent token is rejected"
    - "The paraphrase provider is the same human-held-key OpenAICompatibleClient; offline tests use ScriptedLLMClient (no key, no network)"
  artifacts:
    - path: "tests/nl2sparql/eval/bank_generator.py"
      provides: "paraphrase() via OpenAICompatibleClient (K=3, config knob) + offline slot-preservation guard"
      contains: "def paraphrase"
    - path: "tests/nl2sparql/eval/test_bank_generator.py"
      provides: "paraphrase_faithful offline unit test (scripted faithful pass / slot-dropping reject)"
      contains: "paraphrase_faithful"
    - path: "tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml"
      provides: "CK25 bank re-emitted with K>=3 paraphrases per query"
      contains: "paraphrases"
  key_links:
    - from: "tests/nl2sparql/eval/bank_generator.py"
      to: "arango_sparql.nl2sparql.client.OpenAICompatibleClient"
      via: "runner._client_for-style construction (provider=openai, model=gpt-4o-mini), reads NL2SPARQL_API_KEY"
      pattern: "OpenAICompatibleClient"
    - from: "the slot-preservation guard"
      to: "ShapeTemplate.semantic_slots + intent_lexicon"
      via: "set/dict comparison against the template ground truth"
      pattern: "semantic_slots"
---

<objective>
Wave 2 of the Stage-1 eval prototype (per D-03): make the generated questions natural without sacrificing faithfulness. Add K=3 LLM paraphrases per query via the same OpenAICompatibleClient provider (human-held NL2SPARQL_API_KEY, config knob), and make the PRIMARY faithfulness guard offline and deterministic: each paraphrase is checked against the paired template's semantic_slots + intent_lexicon (does it preserve the top-N-descending / COUNT / negation / filter intent and the literal fillers?). The paired template is ground truth; the LLM-judge is only a secondary sample check (folded into the Plan 05 credentialed sweep). This closes REQ-3's offline half.

Purpose: Deliver faithful, natural questions "by construction" (paired templates) with a pure/offline guard, naming the build-time LLM dependency explicitly (D-03: "auto-onboard" means auto GIVEN LLM access at build time, not hands-free).
Output: paraphrase() + offline slot-preservation guard, CK25 bank re-emitted with paraphrases, scripted offline faithfulness test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@tests/nl2sparql/eval/bank_generator.py
@arango_sparql/nl2sparql/client.py
@tests/nl2sparql/eval/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paraphrase() (K=3, OpenAICompatibleClient) + the offline primary slot-preservation guard</name>
  <files>tests/nl2sparql/eval/bank_generator.py</files>
  <read_first>
    - tests/nl2sparql/eval/runner.py (`_client_for` construction lines 196-197: OpenAICompatibleClient(provider=ptype, model=provider.get("model")); and `_wrap_sparql` lines 168-179 — the fenced-response wrapping pattern for scripted doubles)
    - arango_sparql/nl2sparql/client.py (OpenAICompatibleClient.generate(messages) -> LLMResponse reads NL2SPARQL_API_KEY at line 174; ScriptedLLMClient at line 377 — the offline double)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (the "Paraphrase + faithfulness-guard (D-03)" section — primary guard = slot preservation; secondary = LLM-judge sample)
  </read_first>
  <action>
    Add `paraphrase(question, template, binding, *, k=3, client=None) -> list[str]` to bank_generator.py. Construct the provider exactly as runner._client_for does — OpenAICompatibleClient(provider="openai", model="gpt-4o-mini") (reads NL2SPARQL_API_KEY) — but accept an injected `client` so offline tests pass a ScriptedLLMClient; keep the import function-local. K is a config knob defaulting to 3 (D-03). Add the PRIMARY faithfulness guard `slot_preserving(paraphrase, template, binding) -> bool` (pure, offline, deterministic, NO LLM): verify the paraphrase preserves every literal filler in binding (category/department/country values) AND contains at least one token from the template's intent_lexicon matching the shape intent (top-N => a superlative; scalar_count/grouped_aggregation => "how many"/"number of"; negation => "without"/"no"/"lacking"; etc.), and does NOT flip the ordering direction. A paraphrase failing the guard is rejected and regenerated (bounded retries) or dropped. Wire paraphrase() into generate_bank so each kept example emits question + a `paraphrases` list of K>=3 guard-passing paraphrases; when no client/key is available, degrade to the single templated question with an empty/absent paraphrases list (documented degrade — the offline pipeline still emits a valid bank). Name the build-time LLM dependency in the module docstring (D-03).
  </action>
  <verify>
    <automated>uv run python -c "from tests.nl2sparql.eval import bank_generator as g; assert hasattr(g,'paraphrase') and hasattr(g,'slot_preserving'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `ok` (paraphrase + slot_preserving importable offline; no key, no network)
    - slot_preserving is pure: `grep -nA30 "def slot_preserving" tests/nl2sparql/eval/bank_generator.py` shows no client/generate call
    - The OpenAICompatibleClient construction mirrors runner._client_for (provider=openai, model from config; reads NL2SPARQL_API_KEY) and is function-local
    - `grep -qi "build-time LLM" tests/nl2sparql/eval/bank_generator.py` (D-03 dependency named)
  </acceptance_criteria>
  <done>paraphrase() (K=3, config knob, human-held-key provider) and a pure offline slot-preservation guard exist; the guard is the primary faithfulness check per D-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline paraphrase_faithful test (scripted) + re-emit CK25 bank with paraphrases</name>
  <files>tests/nl2sparql/eval/test_bank_generator.py, tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml</files>
  <read_first>
    - arango_sparql/nl2sparql/client.py (ScriptedLLMClient line 377 — construct with canned paraphrase responses)
    - tests/nl2sparql/eval/runner.py (`_wrap_sparql` lines 168-179 — the scripted-response wrapping convention to mirror for scripted paraphrase doubles)
    - tests/nl2sparql/eval/verify_generated_bank.py (the gate the re-emitted bank must still pass)
  </read_first>
  <behavior>
    - A faithful scripted paraphrase (preserves the filler + intent token) passes slot_preserving
    - A slot-dropping scripted paraphrase (drops the category filler) is rejected
    - An intent-flipping scripted paraphrase (superlative -> "cheapest" on a "most expensive" template) is rejected
    - The re-emitted CK25 bank has >=3 paraphrases per query and still passes verify_generated_bank.py
  </behavior>
  <action>
    Add `test_paraphrase_faithful` (offline, pure scripted — no RUN_EVAL, no key) to test_bank_generator.py: feed slot_preserving hand-crafted faithful and unfaithful paraphrases per shape and assert accept/reject; feed paraphrase() a ScriptedLLMClient returning a mix of faithful + unfaithful candidates and assert only guard-passing ones are kept and >=3 survive (or the example is dropped/flagged). Re-run generate_bank against CK25 with the scripted client (or the degrade path) and RE-EMIT vendored/ck25/generated_fewshot_bank.yml carrying `paraphrases` per example. Confirm the re-emitted bank still passes verify_generated_bank.py (paraphrases do not change the query, so parse+transpile+execute-non-empty is unaffected — REQ-1/REQ-2 preserved). Note in the test docstring AND in this bank file's header comment that the committed CK25 bank here carries SCRIPTED/PLACEHOLDER paraphrases (offline provenance) — it is SUPERSEDED by Plan 05 Task 3's real-paraphrase regeneration (live OpenAICompatibleClient, human-held key) before any REQ-4/REQ-6 measurement; the SECONDARY >=20-pair LLM-judge audit (REQ-3's credentialed half) is also human-run in Plan 05.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k paraphrase_faithful -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k paraphrase_faithful -q` passes (offline, scripted, no key, no network)
    - Test asserts faithful paraphrases accepted and slot-dropping / intent-flipping paraphrases rejected (REQ-3 primary guard)
    - After re-emit, `uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank tests/nl2sparql/eval/vendored/ck25/generated_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl` exits 0 (REQ-1/REQ-2 preserved)
    - Every example in the re-emitted bank carries a `paraphrases` list of length >=3 (or is flagged degrade when no client) — verifiable by a yaml.safe_load count assertion in the test
  </acceptance_criteria>
  <done>The offline scripted faithfulness test is green; the CK25 bank carries K>=3 guard-passing paraphrases per query and still passes the validity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time LLM call (paraphrase) | first external call in the phase — human-held NL2SPARQL_API_KEY; offline tests use ScriptedLLMClient so no key is needed in CI |
| paraphrase provider input/output | inputs are generator-authored templated questions (not external user text); outputs are guarded by the offline slot-preservation check |
| generated bank YAML | trusted-repo data injected verbatim into few-shot prompts (same trust tier as curated fewshot_bank.yml) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-06 | Information disclosure | NL2SPARQL_API_KEY leakage | mitigate | key read only by OpenAICompatibleClient at build time; never written to the bank or any file; offline tests use ScriptedLLMClient; no-secrets grep in verification |
| T-07.5-07 | Spoofing/Tampering | prompt-injection via paraphrase output text | mitigate | offline slot-preservation guard rejects paraphrases that drop/flip slots before they enter the bank; bank is repo-reviewed trusted data |
| T-07.5-08 | Tampering | pyoxigraph / provider import on engine path | mitigate | provider + oxi imports function-local; file lives under tests/ |
| T-07.5-SC | Tampering | package installs | accept | no installs this phase (RESEARCH audit N/A) |
</threat_model>

<verification>
- `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k paraphrase_faithful -q` green (offline).
- Re-emitted CK25 bank passes verify_generated_bank.py (parse+transpile+execute-non-empty).
- No secrets committed: `git diff | grep -iE 'NL2SPARQL_API_KEY\s*=\s*sk-' ` returns nothing.
- W3C DAWG non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -q` still green.
</verification>

<success_criteria>
- K>=3 paraphrases per query; primary faithfulness guard is offline/deterministic and rejects unfaithful paraphrases.
- CK25 bank re-emitted with paraphrases, still passing the validity gate.
- Build-time LLM dependency named; secondary >=20-pair LLM-judge deferred to the Plan 05 human-run sweep.
- No secrets committed; no installs; engine untouched.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-03-SUMMARY.md` when done.
</output>
