---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 06
subsystem: nl2sparql-eval / arango-query-core engine seam
tags: [sparql, few-shot, query-first-synthetic, engine-promotion, seam, pin-bump, cypher-inheritable, stage-2]

# Dependency graph
requires:
  - phase: 07.5-05
    provides: the ADOPT disposition (NL-GEN-01 closed) that conditionally triggers this
      Stage-2 engine promotion; the validated eval-side bank_generator (ShapeTemplate +
      SHAPE_CATALOG + 9 closures) whose pure core this plan promotes
provides:
  - "arango_query_core.nl.synthbank (engine seam, commit 3438305, pushed to both remotes):
    the pure ontology-agnostic query-shape template catalog -- ShapeTemplate, SHAPE_CATALOG,
    the 9 applies/build_sparql closures, dependency-free render/index helpers -- Cypher- and
    onboarding-caller-inheritable, pyoxigraph-free"
  - "tests/nl2sparql/eval/bank_generator.py: refactored into a thin eval-side shim delegating
    the catalog to the engine seam while keeping data-binding + execution-filter + paraphrase
    test-side"
  - "pyproject.toml git pin bumped b669320 -> 3438305 (ls-remote-verified on both remotes
    BEFORE uv lock, 07.3-02 precedent); uv.lock regenerated"
affects: [arango-query-core, arango-cypher-py (future: inherits ShapeTemplate + applies gates)]

# Tech tracking
tech-stack:
  added:
    - "arango_query_core.nl.synthbank engine seam (new module in the shared engine)"
  patterns:
    - "Eval-side-builder-first, engine-seam-later (07.3/07.4 precedent): the construction
      core was validated eval-side across Stage 1, then only its PURE (pyoxigraph-free)
      surface promoted to the engine once the adopt bar cleared"
    - "Promote-verbatim + prove-by-regeneration-equivalence: the closures were ported
      byte-for-byte, and the committed CK25+QALD banks' fresh-regeneration equivalence tests
      (test_committed_ck25_bank_matches_fresh_regeneration et al.) prove behavior-identity
      mechanically rather than by assertion"
    - "Verify-consumer-against-local-editable-before-publish (arango-cypher-py package-
      extraction pattern): the shim was wired + full-gate-verified against a `uv pip install
      -e` local engine clone BEFORE the push, so nothing was published until the round-trip
      was proven; the push + ls-remote-verify + pin bump + uv lock then swapped the editable
      override for the real git pin"

key-files:
  created:
    - "arango-query-core: arango_query_core/nl/synthbank.py (the promoted catalog)"
    - "arango-query-core: tests/test_nl_synthbank.py (15 pure engine-side tests)"
  modified:
    - "arango-query-core: arango_query_core/nl/__init__.py (+SHAPE_CATALOG, ShapeTemplate exports)"
    - "pyproject.toml (git pin b669320 -> 3438305, both [nl] + [dense])"
    - "uv.lock (regenerated against the new pin)"
    - "tests/nl2sparql/eval/bank_generator.py (promoted core removed; now a thin shim, -530 lines)"

key-decisions:
  - "OQ-2 promotion boundary = promote-template-core-only (RESEARCH recommendation, user-
    confirmed): only the pure ShapeTemplate type + SHAPE_CATALOG + applies/build_sparql
    closures + render/index helpers promote. Data-binding (pyoxigraph), execution-filter, and
    paraphrase (arango_sparql LLM client + its slot_preserving guard) stay eval-side -- neither
    pyoxigraph nor the SPARQL-specific LLM client may enter the language-agnostic engine."
  - "Cypher inheritance is of the STRUCTURE, honestly scoped: the ShapeTemplate type + the
    applies gates are target-language-agnostic (they read only the shared GroundedPredicate),
    so Cypher reuses them and supplies its own build_* renderers; build_sparql here is the
    SPARQL-specific half. No premature build_query generalization (YAGNI; kept build_sparql
    to preserve byte-identical shim call sites)."
  - "Index helpers (_predicates_of/_sorted_predicates) exist in BOTH the engine seam (for its
    own applies_two_hop) and the shim (2-line locals) rather than cross-package private import
    -- avoids importing an underscore-private engine name into eval code; behavior identical."
  - "Fetchability verified via git ls-remote refs/heads/main on BOTH remotes BEFORE uv lock
    (07.3-02 precedent); the engine seam pushed to both remotes before the pin bump (07.4-06
    precedent)."

patterns-established:
  - "Stage-2 conditional engine promotion discipline: gate strictly behind the Stage-1 adopt
    bar, verify the consumer against a local-editable engine before publishing, push to all
    mirrors + ls-remote-verify before the pin bump, and prove old==new by regeneration-
    equivalence tests rather than a manual byte diff."

requirements-completed: []  # NL-GEN-01 already CLOSED via ADOPT in Plan 05; this plan ships the conditional product deliverable, no new requirement closure

# Metrics
duration: ~90min
completed: 2026-08-05
actuals:
  tasks: 2
  commits: 2  # engine 3438305 (arango-query-core), sparql 624e3c1 (this repo)
---

# Phase 07.5 Plan 06: Stage-2 Engine Promotion (synthbank Template Catalog) Summary

**Promoted the pure, ontology-agnostic query-first few-shot template catalog out of the eval-side `bank_generator` into a new `arango_query_core.nl.synthbank` engine seam (pushed to both remotes, pin-bumped fetchability-verified), turning the validated Stage-1 eval prototype into a reusable, Cypher-inheritable, pyoxigraph-free construction core -- while keeping data-binding, execution-filter, and paraphrase test-side.**

## Gate (Task 1): adopt bar + OQ-2 boundary

- **Adopt bar confirmed cleared** from `07.5-05-SUMMARY.md` (SPEC §11.2, clause by clause: shape-coverage 100% ≥70%; CK25 delta>0 all 3 runs raw+overlap-excluded; QALD non-regression; zero-regression via the documented `b>c on every run` alternative clause). NL-GEN-01 was already CLOSED via ADOPT in Plan 05 -- Stage 2 is the conditional product deliverable, user-confirmed to proceed.
- **OQ-2 boundary chosen: `promote-template-core-only`** (RESEARCH recommendation) -- minimal engine surface; the onboarding caller supplies pre-bound slot values.

## Accomplishments (Task 2)

- **New engine seam `arango_query_core/nl/synthbank.py`** (456 lines, stdlib-only, no pyoxigraph): `ShapeTemplate`, `SHAPE_CATALOG` (9 shapes × `applies`/`build_sparql`), `RDFS_LABEL_IRI`, `RELATIONAL_SHAPES`, `_lit`, `_predicates_of`/`_sorted_predicates`. Exported via `nl/__init__.py`. Closures ported **verbatim** from the eval-side generator.
- **15 pure engine tests** (`tests/test_nl_synthbank.py`): catalog integrity, no-pyoxigraph import guard, every `applies` gate, every `build_sparql` renderer, index helpers. Engine gates green: ruff + mypy clean, full engine suite 119 passed.
- **Pushed to BOTH remotes** (ArthurKeen + arango-solutions, 07.4-06 precedent), engine commit `3438305`; **fetchability `git ls-remote`-verified on both BEFORE `uv lock`** (07.3-02 precedent).
- **`bank_generator.py` refactored into a thin eval-side shim** (−530 lines) delegating the catalog to the engine seam while keeping `_candidates_*` data-binding, `_sample_labels`/`_strict_extremum_ok`/`_execution_nonempty`, `paraphrase` + `slot_preserving`, and `generate_bank*` orchestration eval-side.
- **Pin bumped** `b669320 → 3438305` (both `[nl]` + `[dense]`); `uv.lock` regenerated.

## Verification (against the REAL pinned engine, post-`uv sync`)

- `test_bank_generator.py` + `test_coverage_gate.py`: **55 passed** -- including the **regeneration-equivalence** tests (`test_committed_ck25_bank_matches_fresh_regeneration`, `test_committed_qald_bank_matches_fresh_regeneration`, `test_generation_is_byte_stable_under_fixed_seed`), the mechanical proof that the promoted catalog regenerates the committed banks identically.
- `RUN_EVAL=1` disjoint/overlap suite: 6 passed, 1 skipped (similarity-ceiling needs the sentence-transformers stack).
- `verify_generated_bank.py` on the committed CK25 bank: **ALL GREEN** (73 examples, unchanged).
- Non-regression: **W3C DAWG coverage ≥96.4%** ✓; **no pyoxigraph on the installed engine import path** ✓; **SPARQL→AQL transpiler package unchanged** ✓.

## Task Commits

1. **Engine seam (arango-query-core)** -- `3438305` `feat(nl): add synthbank query-shape template catalog (07.5 seam promotion)` (pushed to both remotes)
2. **Pin bump + shim (arango-sparql-py)** -- `624e3c1` `feat(07.5-06): promote synthbank template catalog to engine; bump pin + shim`
3. **Plan metadata** (this SUMMARY + STATE + ROADMAP) -- see below

## Deviations from Plan

None. The promotion boundary, the push-to-both-remotes + ls-remote-verify sequence, and the pyoxigraph-exclusion gate all followed the plan and the 07.3-02/07.4-06 precedents. No auto-fixed bugs; no code deviations.

## Known Stubs

None.

## Next Phase Readiness

- Phase 07.5 is COMPLETE (6/6 plans). NL-GEN-01 closed (ADOPT, Plan 05); the Stage-2 product deliverable shipped.
- The pure construction core is now engine-side and Cypher-inheritable; a future Cypher front-end reuses `ShapeTemplate` + the `applies` gates and supplies its own `build_cypher` renderers. Data-binding + execution remain a caller responsibility (RESEARCH OQ-2 boundary).
- No blockers. W3C DAWG coverage gate green; transpiler + engine grounding seams untouched.

---
*Phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank*
*Status: COMPLETE*
