---
context: phase
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
task: 1
total_tasks: 2
status: paused
last_updated: 2026-08-04T23:33:50.337Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do-not-auto-execute-07.5-06 — The sole remaining plan (07.5-06) is the CONDITIONAL Stage 2 engine promotion. It is `autonomous: false`, gated in STATE.md behind explicit user confirmation, and its Task 1 is a `checkpoint:decision` (`gate=blocking`). Running `/gsd-execute-phase 07.5` again will re-attempt it. — Structural mitigation: before dispatching any executor, first obtain the user's explicit OQ-2 promotion-boundary decision (see Next Action). Never pick the boundary or start the engine push on the user's behalf.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Barrel through a blocking checkpoint | This session's `/gsd-execute-phase 07.5` flow moved to execute the one incomplete plan; 07.5-06 opens with a blocking user decision AND is outward-facing (pushes a seam to the arango-query-core engine repo on BOTH remotes, then bumps the pyproject.toml git pin + uv lock). Auto-proceeding would push engine code and mutate a dependency pin without authorization. | blocking | Task 1 must be resolved by an explicit user choice (promote-template-core-only \| promote-with-execution-hook \| defer-stage2) BEFORE any `Agent(gsd-executor)` dispatch. The engine push + pin bump require a separate go-ahead at that step. |

<current_state>
Phase 07.5 is 5/6 plans complete. NL-GEN-01 is already CLOSED via the eval-prototype ADOPT path (Plan 05). The only remaining plan is **07.5-06** — the CONDITIONAL Stage 2 PRODUCT deliverable (engine promotion). It has not started (no partial commits; safe-resume verified clean this session). The phase is intentionally paused at the Task 1 decision gate.
</current_state>

<completed_work>

Phase-level (plans):
- 07.5-01 … 07.5-05: Done (summaries present). NL-GEN-01 CLOSED via ADOPT.

Within 07.5-06 (this session):
- Task 1 (partial): Adopt bar CONFIRMED cleared from 07.5-05-SUMMARY.md (SPEC §11.2 met clause-by-clause). The remaining half of Task 1 — the OQ-2 promotion-boundary decision — is NOT made; awaiting the user.
- No code written this session.
</completed_work>

<remaining_work>

- Task 1 (finish): user selects the OQ-2 promotion boundary (or defers Stage 2).
- Task 2 (only if a promote-* boundary is chosen): port the pure (pyoxigraph-free) template-catalog + slot-filling core into a new `arango_query_core.nl` seam (mirror seam-6/seam-7 style, frozen dataclasses, no pyoxigraph). Push the seam to origin/main on BOTH remotes; verify fetchable via `git ls-remote refs/heads/main` BEFORE bumping (07.3-02 precedent). Bump `pyproject.toml` git pin, run `uv lock`, refactor `tests/nl2sparql/eval/bank_generator.py` into a thin shim that delegates template construction to the engine core while KEEPING data-binding/execution-filter/paraphrase eval-side. Then create 07.5-06-SUMMARY.md.
</remaining_work>

<decisions_made>

- Confirmed Plan 05 disposition = ADOPT before touching any engine code (Task 1's first required gate). Evidence: 07.5-05-SUMMARY.md — shape-coverage 100% (≥70%), CK25 delta>0 on all 3 runs (raw + overlap-excluded), QALD non-regression (empty generated bank), zero-regression via the documented "b>c on every run" alternative clause (c==0 only on run 3).
- Chose NOT to autonomously start 07.5-06 or select the OQ-2 boundary — it is user-gated and outward-facing (engine push both remotes + pin bump).
</decisions_made>

<blockers>
- [Human decision — blocking]: OQ-2 promotion boundary not yet chosen. Options: `promote-template-core-only` (RESEARCH recommendation — minimal engine surface, data-binding stays eval-side), `promote-with-execution-hook` (adds a caller-provided execution-hook Protocol; larger seam), or `defer-stage2` (ship the eval prototype as-is, no engine change).
- [Human confirmation — blocking]: Explicit go-ahead required before the outward-facing engine push (both remotes) + `pyproject.toml` pin bump.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-06-PLAN.md` — the conditional plan; Task 1 is the blocking OQ-2 decision, Task 2 is the promotion + pin bump.
2. `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-05-SUMMARY.md` — the ADOPT disposition that triggers (but does not auto-start) Stage 2.
3. `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md` — OQ-2 (exactly what promotes; pyoxigraph is test-only, cannot enter the engine).
4. `.planning/STATE.md` — the pause record, and the 07.3-02 (git ls-remote before uv lock) + 07.4-06 (upstream seam pushed to both remotes) precedents.

## Infrastructure State
- No running servers/watchers. No async external jobs.
- Working tree carries only pre-existing untracked/modified files (`.gitignore`, `AGENTS.md`, `scratchpad/`, assorted `.planning/` docs, `tests/nl2sparql/eval/generated_sweep_result.json`) — none produced by this session. The pause commit touches only the two handoff files.

## Pre-Execution Critique Required
- Not applicable — the gate here is a user decision, not a design-critique gate.

<context>
The user ran `/gsd-execute-phase 07.5`, I cleared the pre-execution gates and confirmed the adopt bar, then surfaced the OQ-2 decision. The user declined the decision prompt and paused. The mental model: Stage 1 (the eval prototype) is done and validated; Stage 2 (engine promotion) is a real product ship with cross-repo, outward-facing side effects, so it correctly waits for a deliberate human go-ahead. If the user later says "defer", 07.5-06 should be recorded as not-triggered and the phase can be closed with NL-GEN-01 already met.
</context>

<next_action>
Start with: ask the user for the OQ-2 promotion-boundary decision — `promote-template-core-only` (recommended) | `promote-with-execution-hook` | `defer-stage2`. Do NOT dispatch a gsd-executor until an explicit promote-* choice is given; the engine push (both remotes) + pin bump need that go-ahead. If `defer-stage2`, record 07.5-06 as not-triggered and close the phase.
</next_action>
