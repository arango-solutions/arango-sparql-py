---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 05
type: execute
wave: 5
depends_on: ["07.5-04"]
files_modified:
  - tests/nl2sparql/eval/test_fewshot_bank_disjoint.py
  - tests/nl2sparql/eval/configs.yml
  - tests/nl2sparql/eval/run_generated_sweep.py
  - tests/nl2sparql/eval/baseline.json
  - tests/nl2sparql/eval/README.md
autonomous: false
requirements: [NL-GEN-01]
user_setup:
  - service: nl2sparql-provider
    why: "Credentialed directional lift sweep + secondary faithfulness judge (REQ-6, REQ-3 secondary) — gpt-4o-mini via the human-held key; agent never holds it"
    env_vars:
      - name: NL2SPARQL_API_KEY
        source: "Human-held OpenAI-compatible provider key (never pasted into any file)"

must_haves:
  truths:
    - "The generated bank loads via FewShotIndex/BM25 and its examples appear in the NLQueryEngine prompt's ## Examples section (offline dry-run)"
    - "The overlap audit reports bank<->held-out overlap on BOTH query-shape and entity axes; the CK25 lift is reported with AND without near-duplicates"
    - "The directional adopt/kill sweep runs a generated-bank arm vs a FRESH same-session zero arm (never a stale baseline); adopt = b>0, c==0, delta>0 surviving overlap-exclusion"
  artifacts:
    - path: "tests/nl2sparql/eval/run_generated_sweep.py"
      provides: "human-run paired sweep (dry-run + sweep modes; monkeypatch isolation; McNemar+bootstrap; overlap-excluded delta)"
      contains: "paired_mcnemar"
    - path: "tests/nl2sparql/eval/test_fewshot_bank_disjoint.py"
      provides: "shape + entity overlap audit over the generated bank"
      contains: "entity"
    - path: "tests/nl2sparql/eval/configs.yml"
      provides: "additive generated-bank arms (CK25 + QALD + scripted plumbing); optional few_shot.bank key (OQ-3)"
      contains: "generated"
  key_links:
    - from: "tests/nl2sparql/eval/run_generated_sweep.py"
      to: "runner.run + paired_mcnemar + bootstrap_paired_delta"
      via: "monkeypatch isolation (mirror run_spike.py) + fresh same-session zero arm"
      pattern: "cached_few_shot_index.cache_clear"
    - from: "tests/nl2sparql/eval/baseline.json"
      to: "the credentialed sweep results"
      via: "human-reviewed manual fold-in (never auto-regenerated)"
      pattern: "generated"
---

<objective>
Wave 4 of the Stage-1 eval prototype — the terminal measurement (REQ-4 + REQ-6, the adopt/kill decision). Build the offline scaffolding — extend the disjointness gate to a shape+entity overlap audit over the generated bank (D-05), add additive configs.yml arms pointing at the generated banks, and mirror run_spike.py into run_generated_sweep.py (dry-run drop-in proof + human-run paired sweep with the overlap-excluded delta) — then hand the credentialed CK25+QALD sweep to the human (NL2SPARQL_API_KEY is never held by an agent, mirroring 07.3-06/07.4-05). The human runs it blind + once, pastes McNemar/bootstrap + per-case verdicts + the secondary >=20-pair faithfulness judge, folds results into baseline.json, and records the adopt/kill decision.

Purpose: Measure whether the GENERATED (not hand-built) bank reproduces the spike's lift — the phase's central bet — mechanistically + directionally, overlap-audited (D-05), with zero regressions as a hard bar (spike carry-forward #3).
Output: overlap audit, config arms, sweep harness (offline dry-run green), and the human-run adopt/kill sweep folded into baseline.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@tests/nl2sparql/eval/test_fewshot_bank_disjoint.py
@tests/nl2sparql/eval/configs.yml
@.planning/spikes/001-ck25-thin-fewshot-signal/run_spike.py
@tests/nl2sparql/eval/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the disjointness gate to a shape + entity overlap audit over the generated bank (D-05)</name>
  <files>tests/nl2sparql/eval/test_fewshot_bank_disjoint.py</files>
  <read_first>
    - tests/nl2sparql/eval/test_fewshot_bank_disjoint.py (the existing 3-way check: `_normalize_question` line 71, `_canonical` from runner, `_skeleton` lines 75-99 with Literal(...)->?LIT / URIRef('...')->?URI; the `pytest.mark.eval` + RUN_EVAL skip idiom lines 48-54; BANK_PATH line 57 — point the NEW test at the GENERATED bank paths)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (the "Measurement & Overlap-Audit Design (D-05)" section — shape overlap via _skeleton; entity overlap = literal fillers/IRIs intersection; leakage-safe CK25 fillers Engineering/Procurement/Crystal/India vs held-out Data Services/Marketing/Oscillator/France)
    - .planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml (the entity-disjoint discipline the audit targets)
  </read_first>
  <action>
    Add NEW test functions to test_fewshot_bank_disjoint.py that run over the GENERATED banks (vendored/ck25/generated_fewshot_bank.yml and vendored/qald9plus/...) — do NOT reuse test_bank_ontology_matches_corpus (curated-bank specific, lines 161-168). (1) SHAPE overlap: reuse `_skeleton` (canonical algebra with literals/URIs blanked) — a generated example whose skeleton equals a held-out gold's skeleton is a near-duplicate-shape; emit the set of overlapping example indices so the sweep can exclude them. (2) ENTITY overlap (new, D-04/D-05 axis, Pitfall 4): extract literal fillers / IRIs from each generated example and each held-out gold; report the intersection — must be empty on CK25's leakage-safe disjoint fillers. Provide the overlap sets as importable helpers (e.g. `shape_overlap_indices(bank, corpus)` / `entity_overlap(bank, corpus)`) so run_generated_sweep.py (Task 2) computes the overlap-excluded delta from the same source of truth. Keep the `pytest.mark.eval` + RUN_EVAL skip idiom; point at the generated bank paths.
  </action>
  <verify>
    <automated>RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/test_fewshot_bank_disjoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/test_fewshot_bank_disjoint.py -q` passes (offline, no key, no network — RUN_EVAL gates data-file access, not a credential)
    - A shape-overlap test and an entity-overlap test both run over the GENERATED CK25 bank and assert entity overlap is empty (leakage-safe)
    - `shape_overlap_indices` / `entity_overlap` helpers are importable for reuse by the sweep harness
  </acceptance_criteria>
  <done>The overlap audit runs on both axes over the generated bank; helpers exported for the overlap-excluded delta.</done>
</task>

<task type="auto">
  <name>Task 2: Additive configs.yml arms + run_generated_sweep.py (offline dry-run drop-in proof)</name>
  <files>tests/nl2sparql/eval/configs.yml, tests/nl2sparql/eval/run_generated_sweep.py</files>
  <read_first>
    - .planning/spikes/001-ck25-thin-fewshot-signal/run_spike.py (MIRROR this whole file: monkeypatch isolation lines 63-77, arm config shape lines 46-52, fresh-same-session paired stats lines 121-133, dry-run drop-in proof lines 80-110, RUN_EVAL+key guardrails lines 114-119, result-JSON write lines 154-166)
    - tests/nl2sparql/eval/configs.yml (the existing openai-gpt4o-mini-ck25 arm lines 84-90 + the additive -grounded block pattern lines 103-142 — mirror for an additive few_shot arm)
    - tests/nl2sparql/eval/runner.py (the additive `few_shot:` config read at lines 516-528; `run()` corpus/data_path additive reads lines 494-528; paired_mcnemar line 646, bootstrap_paired_delta line 682)
  </read_first>
  <action>
    (a) Add ADDITIVE configs.yml arms (absent-key == byte-identical fallback for every existing arm): openai-gpt4o-mini-ck25-generated-fewshot (= the CK25 zero arm plus few_shot: {mode: bm25, k: 20}), a matching scripted-ck25-generated-fewshot PLUMBING arm (no network — keeps scripted the CI default), and an openai-gpt4o-mini-qald9plus-generated-fewshot arm. Recommended (OQ-3): add the additive `few_shot.bank:` config key (mirrors the corpus:/data_path: additive precedent) so an arm names its generated bank file without monkeypatching; keep it additive/default-preserving. (b) Create run_generated_sweep.py mirroring run_spike.py: `--dry-run` (default, NO key) loads the generated bank via cached_few_shot_index(path, "bm25"), asserts BM25Retriever, retrieves for held-out questions, and asserts format_prompt_section(q, k=3, language="sparql") starts with "## Examples" (REQ-4 drop-in proof); `--sweep` (human-run: RUN_EVAL=1 + NL2SPARQL_API_KEY, else exit 2) runs the generated-bank arm then a FRESH same-session zero arm (cache_clear between), computes paired_mcnemar (b=gains, c=regressions) + bootstrap_paired_delta, AND the overlap-EXCLUDED delta using Task 1's helpers (report lift with AND without near-duplicate shape/entity cases — REQ-6), across BOTH CK25 and QALD; adopt bar in code = b>0 and c==0 and delta>0 surviving overlap-exclusion; run once, blind; write results JSON next to the script. Touch NO production file (monkeypatch isolation) OR use the additive few_shot.bank arm — either way existing arms stay byte-identical.
  </action>
  <verify>
    <automated>uv run python tests/nl2sparql/eval/run_generated_sweep.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tests/nl2sparql/eval/run_generated_sweep.py --dry-run` exits 0 (offline, no key): bank loads via BM25 FewShotIndex, retrieves same-shape examples, injects a `## Examples` block (REQ-4)
    - `--sweep` without RUN_EVAL/NL2SPARQL_API_KEY exits 2 (human-run guardrail; agent never runs it)
    - New configs.yml arms are additive: `RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/test_eval.py -q` still green (existing arms byte-identical)
    - The sweep computes both the raw delta and the overlap-excluded delta using Task 1's helpers (REQ-6)
  </acceptance_criteria>
  <done>Offline drop-in proof green; additive config arms in place; the human-run sweep harness computes the overlap-audited paired delta on CK25 + QALD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human-run credentialed CK25+QALD adopt/kill sweep + secondary faithfulness judge + baseline.json fold-in</name>
  <files>tests/nl2sparql/eval/baseline.json, tests/nl2sparql/eval/README.md</files>
  <read_first>
    - tests/nl2sparql/eval/run_generated_sweep.py (the harness the human runs)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md (the adopt/kill bar — REQ-6: shape-coverage >=70%, CK25 delta>0 x3 runs, 0 regressions, survives overlap-exclusion; QALD non-regression; KILL if delta<=0 post-exclusion or single-ontology-only)
    - tests/nl2sparql/eval/README.md (the existing human-run credentialed-sweep runbook sections — mirror the 07.3-06/07.4-05 fold-in discipline: fresh same-session zero arm, never a stale committed number, human-reviewed baseline.json fold-in)
  </read_first>
  <action>
    BEFORE the checkpoint: write a README.md runbook section documenting the exact human-run commands (mirror the existing 07.3-06/07.4-05 credentialed-sweep sections). Then PAUSE for the human to run the credentialed sweep (agent never holds NL2SPARQL_API_KEY). AFTER the human pastes results: fold them into baseline.json under a NEW sibling key (aggregate + per-case where captured; do not overwrite the existing openai-gpt4o-mini-ck25 zero entry — mirror the phase07_dense_few_shot_sweep sibling-key precedent), record the adopt/kill decision per the SPEC REQ-6 bar, and update REQUIREMENTS.md NL-GEN-01 disposition. Never write the key to any file.
  </action>
  <what-built>
    Everything up to the credentialed call is automated: the generator, both banks, the offline validity + faithfulness gates, the overlap audit, the additive config arms, and the run_generated_sweep.py harness (dry-run proven). The agent has NOT run the live sweep (NL2SPARQL_API_KEY is human-held — 07.3-06/07.4-05 precedent). A README runbook section documents the exact commands.
  </what-built>
  <how-to-verify>
    1. Run the CK25 directional sweep 3x (REQ-6 "delta>0 across 3 runs"): `RUN_EVAL=1 NL2SPARQL_API_KEY=... uv run python tests/nl2sparql/eval/run_generated_sweep.py --sweep` (gpt-4o-mini @ temp 0.1; generated-bank arm vs a FRESH same-session zero arm each run). Capture per-case verdicts + paired McNemar (b=gains, c=regressions) + bootstrap delta + the OVERLAP-EXCLUDED delta.
    2. Run the QALD arm once for non-regression (expect a muted/flat result at the floor — that is a PASS per OQ-4).
    3. Run the SECONDARY faithfulness judge on >=20 random (question, query) pairs (REQ-3 secondary): confirm >=95% match.
    4. Compute shape-coverage of held-out failing cases' gold shapes (REQ-6 >=70%).
    5. Never paste NL2SPARQL_API_KEY into any file. Confirm results are secret-free.
    6. Paste back: aggregate + per-case verdicts + McNemar/bootstrap (raw AND overlap-excluded) + coverage% + faithfulness% + corpus_sha.
    7. Reply "approved" (with the numbers) or describe issues. The continuation folds results into baseline.json (a NEW sibling key, aggregate + per-case where captured; the existing openai-gpt4o-mini-ck25 zero entry is NOT overwritten — mirror the phase07_dense_few_shot_sweep sibling-key precedent), records the adopt/kill decision, updates REQUIREMENTS.md NL-GEN-01, and writes the SUMMARY.
  </how-to-verify>
  <resume-signal>Reply "approved" with the pasted aggregate + per-case + McNemar/bootstrap (raw and overlap-excluded) + coverage% + faithfulness% + corpus_sha, or describe issues.</resume-signal>
  <acceptance_criteria>
    - The credentialed sweep was human-run (agent never held the key); results pasted back with per-case verdicts, raw + overlap-excluded McNemar/bootstrap, shape-coverage%, faithfulness%, and corpus_sha
    - baseline.json fold-in is manual + human-reviewed; the fresh same-session zero arm (never a stale committed number) is the McNemar comparator; existing entries not overwritten (new sibling key)
    - No secret written to any file: `git diff | grep -c 'sk-'` returns 0
    - The adopt/kill decision is recorded per the SPEC REQ-6 bar (adopt: b>0/c==0/delta>0 surviving overlap-exclusion + coverage>=70% + QALD non-regression; else documented kill/null — NL-FEW-02/07.4 precedent)
  </acceptance_criteria>
  <done>The directional adopt/kill measurement is complete, overlap-audited, folded into baseline.json (human-reviewed), and NL-GEN-01's disposition is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credentialed sweep -> provider | the phase's only live external call; NL2SPARQL_API_KEY human-held, never held by an agent, never written to a file |
| generated bank -> few-shot prompt | trusted-repo data injected verbatim into the ## Examples block (same trust tier as curated fewshot_bank.yml) |
| baseline.json fold-in | human-reviewed manual edit; never auto-regenerated in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-11 | Information disclosure | NL2SPARQL_API_KEY / secret in committed files | mitigate | human-run sweep; key never pasted into any file; `git diff | grep -c 'sk-'` == 0 gate; results are aggregate + per-case verdicts only |
| T-07.5-12 | Repudiation | confounded delta vs a stale baseline (Pitfall 7) | mitigate | fresh same-session zero arm is the McNemar comparator; never read baseline.json pass-rate as the comparator |
| T-07.5-13 | Information disclosure | entity-level leakage inflating the lift (D-04/D-05) | mitigate | overlap audit reports lift with AND without near-duplicate shape+entity cases; adopt requires survival post-exclusion |
| T-07.5-SC | Tampering | package installs | accept | no installs this phase (RESEARCH audit N/A) |
</threat_model>

<verification>
- `uv run python tests/nl2sparql/eval/run_generated_sweep.py --dry-run` exits 0 (REQ-4).
- `RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/test_fewshot_bank_disjoint.py -q` green (overlap audit).
- `RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/test_eval.py -q` green (additive arms — existing behavior byte-identical).
- W3C DAWG non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -q` still green.
- Human-run sweep results folded into baseline.json (human-reviewed); no secret committed.
</verification>

<success_criteria>
- Generated bank loads via BM25 and appears in the prompt ## Examples block (REQ-4).
- Overlap audit reports both axes; lift reported with and without near-duplicates (REQ-6/D-05).
- Credentialed CK25+QALD sweep human-run, blind, once; adopt/kill decision recorded; baseline.json folded in (human-reviewed).
- No secrets committed; scripted CI default preserved; engine untouched.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-05-SUMMARY.md` when done.
</output>
