---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/nl2sparql/eval/grounding_index_builder.py
  - tests/nl2sparql/eval/test_grounding_index_builder.py
  - tests/nl2sparql/eval/bank_generator.py
  - tests/nl2sparql/eval/verify_generated_bank.py
autonomous: true
requirements: [NL-GEN-01]
user_setup: []

must_haves:
  truths:
    - "The walker exposes orderable-literal and expected-but-optional-relation signals derived mechanically from the TBox (+ instance data), with zero per-schema vocabulary hardcoding"
    - "A typed ShapeTemplate catalog exists eval-side with first-class machine-readable semantic slots"
    - "An ontology-parameterized offline validity gate runs green with no key and no network"
  artifacts:
    - path: "tests/nl2sparql/eval/grounding_index_builder.py"
      provides: "orderable-literal + optional-relation signals (eval-side sibling struct; GroundedPredicate untouched)"
      contains: "orderable"
    - path: "tests/nl2sparql/eval/bank_generator.py"
      provides: "ShapeTemplate frozen dataclass + ordered catalog registry + generate_bank() signature"
      min_lines: 40
      contains: "ShapeTemplate"
    - path: "tests/nl2sparql/eval/verify_generated_bank.py"
      provides: "parameterized parse->transpile->execute-non-empty (+ TBox-only structural mode) validity gate"
      contains: "def verify_bank"
  key_links:
    - from: "tests/nl2sparql/eval/grounding_index_builder.py"
      to: "range_iri (Pass 1)"
      via: "ordered-XSD-type membership test"
      pattern: "range_iri"
    - from: "tests/nl2sparql/eval/verify_generated_bank.py"
      to: "runner._canonical + arango_sparql.api.translate + tests.helpers.oxi.oxi_query"
      via: "per-example validity triad"
      pattern: "oxi_query"
---

<objective>
Wave 0 of the Stage-1 eval prototype (per locked D-01): lay the three eval-side foundations the generator is built on, all offline (no key, no network). (1) Extend the 07.4 TBox walker with the two new mechanical signals the hard compositional shapes need (per D-02): orderable-literal detection (ranking -> top-N / OFFSET / grouped-HAVING) and expected-but-optional-relation detection (negation -> FILTER NOT EXISTS). (2) Scaffold `bank_generator.py` with the typed `ShapeTemplate` dataclass catalog whose first-class semantic slots make D-03's faithfulness-by-slot check mechanical. (3) Promote the VALIDATED spike gate `verify_bank.py` into a reusable, ontology-parameterized `verify_generated_bank.py`.

Purpose: De-risk the generator build by landing its raw materials (walker signals), its type backbone (catalog), and its acceptance gate (validity verifier) before any generation logic — mirroring the 07.4 eval-side-builder-first discipline (D-01 Stage 1).
Output: extended walker + tests, generator scaffold, promoted offline validity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@tests/nl2sparql/eval/grounding_index_builder.py
@.planning/spikes/001-ck25-thin-fewshot-signal/verify_bank.py
@.planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the TBox walker with orderable-literal + optional-relation signals (eval-side sibling struct)</name>
  <files>tests/nl2sparql/eval/grounding_index_builder.py, tests/nl2sparql/eval/test_grounding_index_builder.py</files>
  <read_first>
    - tests/nl2sparql/eval/grounding_index_builder.py (the `build_predicate_index` function, lines 187-331 — the two-pass classifier, `range_iri` capture at line 274, the `for iri, p in declared.items()` shape loop at lines 306-329, and the module docstring's "no per-schema hints" hard rule at lines 194-199)
    - tests/nl2sparql/eval/test_grounding_index_builder.py (existing test cases for the 4-way shape classification — extend in lockstep)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (the "The 07.4-walker extension (D-02)" section — the confirmed CK25 orderables: amount/weight_g/height_mm/width_mm/depth_mm/reliabilityIndex/quantity are xsd:decimal; pv:name/addressCountry are xsd:string NOT orderable)
  </read_first>
  <behavior>
    - orderable is True for a datatype predicate whose range IRI is in {xsd:decimal, xsd:integer, xsd:float, xsd:double, xsd:dateTime, xsd:date}; False for xsd:string
    - On the CK25 TBox: exactly the 7 decimals flag orderable=True; pv:name / addressCountry flag orderable=False
    - optional_relation(P) is True iff the instance store contains BOTH a domain-C instance that has P and a domain-C instance that lacks P (data-driven); on a TBox-only ontology (data_ttl is None) the signal is unavailable/False, never a crash
    - The signals live in an eval-side sibling structure keyed by predicate IRI; the engine-side GroundedPredicate dataclass is byte-unchanged (no field added to arango_query_core)
  </behavior>
  <action>
    Add the two signals eval-side WITHOUT touching the engine's frozen GroundedPredicate (per D-01 — no engine change / no pin bump in Stage 1; SPEC non-regression: transpiler package unchanged). Introduce an eval-side sibling: a frozen dataclass `PredicateSignals(iri, orderable, optional_relation)` plus a builder `build_predicate_signals(ontology_ttl, data_ttl=None) -> dict[str, PredicateSignals]` so 07.4's existing runner.py consumer (line 586) of build_predicate_index stays byte-identical. Derive `orderable` purely from the `range_iri` already captured in Pass 1 (line 274) against an `_ORDERABLE_XSD` frozenset of the six ordered XSD type IRIs — NO term-name special-casing (the docstring hard-forbids it). Derive `optional_relation` data-driven: reuse the existing function-local `from tests.helpers.oxi import load_store_from_string, oxi_query` discipline (line 237) to run two probes per object-property P (a domain-C instance WITH P; a domain-C instance WITHOUT P via FILTER NOT EXISTS) and flag True only when both are non-empty; when `data_ttl is None`, short-circuit to unavailable. Keep every pyoxigraph import function-local (CLAUDE.md rule 5 / Pitfall 5). Extend test_grounding_index_builder.py with cases asserting the CK25 orderable set and the optional_relation behavior on prod-inst.ttl, plus the TBox-only-unavailable case.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -q` passes (offline, no key, no network) — the VALIDATION.md offline sampling command
    - Test asserts orderable=True for all seven CK25 xsd:decimal predicates and orderable=False for pv:name/addressCountry
    - Test asserts optional_relation is data-driven on prod-inst.ttl and unavailable/False when data_ttl is None
    - Stage-1 makes NO engine change: `git diff -- pyproject.toml uv.lock` is EMPTY (arango-query-core is an external git-pinned dep; the pin is bumped only in Stage-2 Plan 06) and no local shadow `arango_query_core/` directory is created (`test -d arango_query_core || echo ok`) — GroundedPredicate is reached read-only via the installed package
    - `grep -nE '^(import pyoxigraph|from tests[.]helpers[.]oxi)' tests/nl2sparql/eval/grounding_index_builder.py` returns nothing (function-local only)
  </acceptance_criteria>
  <done>The walker exposes both new signals eval-side, mechanically and schema-agnostically, engine untouched, all tests green offline.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold bank_generator.py — ShapeTemplate dataclass + ordered catalog + generate_bank signature</name>
  <files>tests/nl2sparql/eval/bank_generator.py, tests/nl2sparql/eval/test_grounding_index_builder.py</files>
  <read_first>
    - tests/nl2sparql/eval/grounding_index_builder.py (module docstring lines 24-31 — copy the packaging-boundary paragraph verbatim; function-local oxi import pattern at lines 118 & 237)
    - arango_query_core/nl/grounding.py (the GroundedPredicate frozen dataclass, lines 224-246 — mirror this frozen-dataclass style)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md (the bank_generator.py section — the ShapeTemplate shape: name / applies / build_sparql / question_template / semantic_slots / intent_lexicon)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md ("Template representation — typed dataclass catalog" and the 9-shape "Shape Catalog" table)
  </read_first>
  <action>
    Create tests/nl2sparql/eval/bank_generator.py per D-01's Stage-1 home (pure functions under tests/nl2sparql/eval/, mirroring grounding_index_builder.py). Copy the packaging-boundary docstring paragraph verbatim (file lives under tests/, MUST NOT be imported by arango_query_core/arango_sparql, every pyoxigraph-touching import function-local). Define `@dataclass(frozen=True) class ShapeTemplate` with fields `name: str`, `applies: Callable`, `build_sparql: Callable`, `question_template: str`, `semantic_slots: tuple[str, ...]`, `intent_lexicon: tuple[str, ...]` (per D-02 per-shape structure + D-03 machine-readable slots). Create an ordered catalog registry (module-level `SHAPE_CATALOG` list plus a registration helper) reserving the 9 shape names from the RESEARCH Shape Catalog: lookup, value_object, category_filter, scalar_count, grouped_aggregation, top_n, offset, negation, two_hop (grouped_aggregation SEPARATE from scalar_count per D-02 + spike carry-forward #2). Add the `generate_bank(ontology_ttl: str, data_ttl: str | None = None, *, k_paraphrases: int = 3, seed: int = 0) -> dict` SIGNATURE with a stubbed body raising NotImplementedError (implemented in Plans 02/03/04). Type applies/build_sparql to read only GroundedPredicate fields + eval-side signals + sampled data — never a hardcoded vocab term (SPEC schema-agnostic constraint / D-02). Add a unit test asserting the catalog registers 9 distinct names and ShapeTemplate is frozen.
  </action>
  <verify>
    <automated>uv run python -c "from tests.nl2sparql.eval.bank_generator import ShapeTemplate, SHAPE_CATALOG, generate_bank; assert len({t.name for t in SHAPE_CATALOG})==9, SHAPE_CATALOG"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from tests.nl2sparql.eval.bank_generator import ShapeTemplate, SHAPE_CATALOG, generate_bank"` imports clean (offline, no key, no network)
    - SHAPE_CATALOG registers exactly 9 distinct shape names covering the D-02 set (incl. grouped_aggregation SEPARATE from scalar_count)
    - `grep -q "MUST NOT be imported" tests/nl2sparql/eval/bank_generator.py` (packaging boundary present — Pitfall 5)
    - `grep -nE '^(import pyoxigraph|from tests[.]helpers[.]oxi)' tests/nl2sparql/eval/bank_generator.py` returns nothing (function-local only)
    - `uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -q` passes (catalog test included)
  </acceptance_criteria>
  <done>The generator's type backbone exists: a frozen ShapeTemplate, a 9-name ordered catalog, and a generate_bank signature — no generation logic yet.</done>
</task>

<task type="auto">
  <name>Task 3: Promote spike verify_bank.py to ontology-parameterized verify_generated_bank.py</name>
  <files>tests/nl2sparql/eval/verify_generated_bank.py</files>
  <read_first>
    - .planning/spikes/001-ck25-thin-fewshot-signal/verify_bank.py (the ENTIRE file — the reference to generalize: store setup + resolver-from-corpus lines 66-74, per-example triad lines 91-120, leakage guard lines 76-99, top-N strict-extremum probe lines 37-62 and 122-136, exit convention line 141)
    - tests/nl2sparql/eval/test_gold_transpilable.py (the parse+transpile idiom + vendored-corpus auto-discovery)
    - .planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml (a known-green bank to self-test the promoted gate against)
  </read_first>
  <action>
    Create tests/nl2sparql/eval/verify_generated_bank.py by generalizing the VALIDATED spike gate from three hardcoded CK25 module constants (verify_bank.py lines 28-31) to a `verify_bank(bank_path, corpus_path, data_path=None) -> int` function returning `0 if ok else 1` (exit convention line 141) plus an argparse CLI (`--bank --corpus --data`) so it runs as `uv run python tests/nl2sparql/eval/verify_generated_bank.py ...` and can wrap in pytest. Keep the per-example validity triad in spirit: (a) `_canonical(query) is not None` (parses — REQ-1); (b) `translate(query, resolver=SchemaResolver.from_turtle(corpus["ontology"])).aql` truthy (transpiles — REQ-1); (c) `len(oxi_query(store, query).rows or []) >= 1` (executes non-empty — REQ-2). Add a TBox-ONLY structural mode (per D-04): when `data_path is None`, skip step (c) and the strict-extremum probe and validate parse+transpile only (QALD structural validity — used by Plan 04). Carry the leakage guard (each example's `_canonical` must NOT equal any held-out gold's canonical algebra). Generalize the top-N strict-extremum probe so ranking-shape examples carry an optional per-example `probe` field (drop LIMIT / ORDER BY DESC / read top-2 / require rank1 > rank2) rather than a hardcoded TOPN_PROBES dict. Add a name-anchoring guard: assert no generated query contains an instance-namespace IRI (`prodi:` / a bare instance IRI) — only vocab predicates plus pv:name/rdfs:label anchors (Pitfall 3, spike carry-forward #1). Keep all pyoxigraph imports function-local.
  </action>
  <verify>
    <automated>uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank .planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 (all-green) against the known-good spike bank — proves the promoted gate works offline (no key, no network)
    - Running with `--data` OMITTED exits 0 in structural-only mode (parse+transpile only, per D-04) — verifiable against the same bank
    - `grep -q "def verify_bank" tests/nl2sparql/eval/verify_generated_bank.py` and the CLI accepts `--bank/--corpus/--data`
    - `grep -nE '^(import pyoxigraph|from tests[.]helpers[.]oxi)' tests/nl2sparql/eval/verify_generated_bank.py` returns nothing (function-local only)
  </acceptance_criteria>
  <done>A reusable, ontology-parameterized offline validity gate (with a TBox-only structural mode) runs green against a known-good bank.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time LLM call | none in this plan (offline only — no key held, no network) |
| vendored TBox/instance data -> pyoxigraph | trusted repo data (CK25 vendored corpus); no untrusted input |
| eval module -> engine package | one-directional: tests/ may import arango_query_core, never the reverse (Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-01 | Tampering | pyoxigraph on engine import path (rule 5 / D-08) | mitigate | all pyoxigraph imports function-local; grep gate in each task's acceptance; engine-diff gate in Task 1 |
| T-07.5-02 | Information disclosure | secret leakage | mitigate | no key used or referenced in this plan; nothing written to any file needs a secret |
| T-07.5-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (RESEARCH Package Legitimacy Audit: N/A — every dep already present, green spike); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -q` green (walker + catalog).
- `uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank .planning/spikes/001-ck25-thin-fewshot-signal/ck25_thin_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/ck25/corpus.yml --data tests/nl2sparql/eval/vendored/ck25/raw/prod-inst.ttl` exits 0.
- W3C DAWG non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -q` still green (>=96.4%).
- Stage-1 makes no engine change: `git diff -- pyproject.toml uv.lock` is empty (pin bumped only in Stage-2) and no local `arango_query_core/` shadow dir exists.
</verification>

<success_criteria>
- Walker exposes orderable + optional-relation signals eval-side, engine untouched, tests green.
- ShapeTemplate + 9-name SHAPE_CATALOG + generate_bank signature importable offline.
- verify_generated_bank.py runs green against the known-good spike bank in both data-bound and TBox-only modes.
- No new package installs; no secrets; no pyoxigraph on the engine import path.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-01-SUMMARY.md` when done.
</output>
