---
phase: 07.5-nl-sparql-query-first-synthetic-few-shot-bank
plan: 04
type: execute
wave: 4
depends_on: ["07.5-03"]
files_modified:
  - tests/nl2sparql/eval/vendored/qald9plus/generated_fewshot_bank.yml
  - tests/nl2sparql/eval/reports/generation_report_qald9plus.json
  - tests/nl2sparql/eval/test_bank_generator.py
autonomous: true
requirements: [NL-GEN-01]
user_setup: []

must_haves:
  truths:
    - "The SAME generator runs unmodified on the DBpedia (QALD) TBox and emits structurally-valid, faithful examples — no CK25/DBpedia-specific code branch"
    - "On TBox-only QALD the data-driven shapes (negation, strict-extremum top-N/OFFSET) drop with a logged reason — an honest measured yield, not a hidden gap"
    - "The QALD bank passes the structural-only validity gate (parse+transpile); execution non-empty is correctly skipped (no instances, per D-04)"
  artifacts:
    - path: "tests/nl2sparql/eval/vendored/qald9plus/generated_fewshot_bank.yml"
      provides: "generated QALD bank (structurally-valid, TBox-only)"
      contains: "examples:"
    - path: "tests/nl2sparql/eval/reports/generation_report_qald9plus.json"
      provides: "per-shape yield for QALD (data-driven shapes dropped-with-reason)"
      contains: "drop"
    - path: "tests/nl2sparql/eval/test_bank_generator.py"
      provides: "generator_no_ontology_branch static-inspection test"
      contains: "no_ontology_branch"
  key_links:
    - from: "tests/nl2sparql/eval/bank_generator.py::generate_bank"
      to: "tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl"
      via: "same code path, data_ttl=None (TBox-only, D-04)"
      pattern: "dbpedia_subset"
    - from: "test_generator_no_ontology_branch"
      to: "bank_generator.py source"
      via: "static scan for hardcoded CK25/DBpedia vocab terms"
      pattern: "no_ontology_branch"
---

<objective>
Wave 3 of the Stage-1 eval prototype (per D-04, REQ-5): prove cross-ontology generalization by running the SAME generator, unmodified, on the DBpedia (QALD) TBox. Emit vendored/qald9plus/generated_fewshot_bank.yml TBox-only (no DBpedia instance snapshot — that would leak test entities, D-04), and honestly report the expected thin yield: data-driven shapes (negation; strict-extremum top-N/OFFSET) drop with a logged reason on a TBox with 0 instances. Add a static-inspection test proving one code path emits both banks with no CK25/DBpedia-specific branch. The generalization claim is "the same generator RUNS and emits structurally-valid, faithful examples," not "lifts QALD" (whose bottleneck is orthogonal entity grounding).

Purpose: Demonstrate the generator is schema-/language-agnostic (so Cypher inherits it and it transfers to CDF) — REQ-5, the generalization signal.
Output: QALD bank (structurally valid), QALD per-shape yield report, no-ontology-branch static test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-SPEC.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md
@.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-PATTERNS.md
@tests/nl2sparql/eval/bank_generator.py
@tests/nl2sparql/eval/verify_generated_bank.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the same generator on the QALD TBox; emit the structurally-valid bank + honest yield report</name>
  <files>tests/nl2sparql/eval/vendored/qald9plus/generated_fewshot_bank.yml, tests/nl2sparql/eval/reports/generation_report_qald9plus.json</files>
  <read_first>
    - tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl (the shallow QALD TBox — 75 classes, 250 properties, 0 instance triples; per 07.4-03 the walker degrades object-property ranges to category_instance, 0 value_object)
    - tests/nl2sparql/eval/vendored/qald9plus/corpus.yml (the QALD corpus whose `ontology:` block feeds generate_bank)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-RESEARCH.md (the "QALD expected yield (D-04)" note — data-driven shapes yield ~0; structurally-valid-but-thin is an accepted, documented limitation)
    - tests/nl2sparql/eval/bank_generator.py (generate_bank — call it UNMODIFIED with data_ttl=None; the negation/optional_relation and strict-extremum shapes must self-drop when instances are absent)
  </read_first>
  <action>
    Call generate_bank(qald_ontology_ttl, data_ttl=None) — the SAME function, no QALD-specific argument or branch (REQ-5). Because QALD is TBox-only (D-04: do NOT build a DBpedia instance snapshot from gold-query entities), data-driven shapes MUST self-drop: negation (needs optional_relation, unavailable without instances) and strict-extremum top_n/offset (need real ordered values) — each logged with a drop-reason in reports/generation_report_qald9plus.json (D-02 per-shape YIELD; Pitfall 6 — no silent drop). Structural shapes (lookup, value_object, category_filter, scalar_count, two_hop, grouped_aggregation where a groupable category exists) emit name-anchored examples that parse+transpile. Emit vendored/qald9plus/generated_fewshot_bank.yml as version + examples only (paraphrases optional/degraded — the credentialed key is human-held; the structural claim does not require them). Validate with the STRUCTURAL-ONLY gate (verify_generated_bank.py without --data, per Plan 01's D-04 mode) since execution non-empty is impossible with 0 instances.
  </action>
  <verify>
    <automated>uv run python tests/nl2sparql/eval/verify_generated_bank.py --bank tests/nl2sparql/eval/vendored/qald9plus/generated_fewshot_bank.yml --corpus tests/nl2sparql/eval/vendored/qald9plus/corpus.yml</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 (structural-only mode: every QALD example parses + transpiles; execution non-empty correctly skipped, D-04)
    - `grep -q "drop" tests/nl2sparql/eval/reports/generation_report_qald9plus.json` — negation and the strict-extremum ranking shapes carry a drop-reason on TBox-only QALD
    - The QALD bank was produced by calling generate_bank with NO ontology-specific argument/branch (same signature as the CK25 call in Plan 02)
    - No QALD example contains a hardcoded instance IRI (name-anchoring guard passes)
  </acceptance_criteria>
  <done>The same generator emits a structurally-valid, honestly-thin QALD bank; data-driven shapes drop with logged reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Add the generator_no_ontology_branch static-inspection test (REQ-5)</name>
  <files>tests/nl2sparql/eval/test_bank_generator.py</files>
  <read_first>
    - tests/nl2sparql/eval/bank_generator.py (the full module — the target of the static scan; assert no CK25/DBpedia vocabulary literal appears in generator logic)
    - .planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-VALIDATION.md (REQ-5 row — `pytest -k generator_no_ontology_branch`)
    - tests/nl2sparql/eval/grounding_index_builder.py (the docstring's "must never special-case any single vocabulary term" rule — the same discipline the test enforces on bank_generator.py)
  </read_first>
  <action>
    Add test_generator_no_ontology_branch to test_bank_generator.py enforcing REQ-5's "no ontology-specific branches": (1) statically scan bank_generator.py source for forbidden hardcoded vocabulary terms — no CK25 term literals (e.g. "Resistor", "Engineering", "hasCategory" baked as a constant) and no DBpedia term literals in the generator/template logic (name-anchoring uses the label predicate resolved from the TBox, not a baked term); (2) assert both banks came from the identical generate_bank entry point (call it on both the CK25 and QALD ontology blocks within the test and assert both return the version+examples dict shape). Allow generic SPARQL keywords / XSD type IRIs and the label-predicate concept (rdfs:label/pv:name resolved from corpus config declaratively — mirrors the configs.yml label_predicates precedent, not a hardcode). Mark the test offline (no RUN_EVAL, no key).
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k no_ontology_branch -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k no_ontology_branch -q` passes (offline, no key, no network)
    - Test fails if a CK25 or DBpedia vocabulary term literal is introduced into generator/template logic (REQ-5 / D-02 schema-agnostic constraint)
    - Test proves one generate_bank entry point emits both banks (same code path)
  </acceptance_criteria>
  <done>A committed static-inspection test proves one code path, no ontology-specific branch — REQ-5 satisfied offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time LLM call | none required in this plan (QALD structural claim needs no paraphrase; offline) |
| vendored QALD TBox -> pyoxigraph/rdflib | trusted repo data (dbpedia_subset.ttl); TBox-only, 0 instances |
| generated QALD bank YAML | trusted-repo data; structurally-valid-but-thin (documented D-04 limitation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-09 | Information disclosure | DBpedia test-entity leakage into QALD examples (D-04/D-05) | mitigate | TBox-only generation (data_ttl=None); no instance snapshot built from gold-query entities; name-anchoring guard; entity-overlap axis audited in Plan 05 |
| T-07.5-10 | Tampering | ontology-specific branch smuggled into the generator | mitigate | test_generator_no_ontology_branch static scan (REQ-5) |
| T-07.5-SC | Tampering | package installs | accept | no installs this phase (RESEARCH audit N/A) |
</threat_model>

<verification>
- QALD bank passes the structural-only validity gate (parse+transpile).
- `uv run pytest tests/nl2sparql/eval/test_bank_generator.py -k no_ontology_branch -q` green.
- QALD yield report logs drop-reasons for data-driven shapes (D-02).
- W3C DAWG non-regression: `uv run pytest tests/w3c/test_coverage_gate.py -q` still green.
</verification>

<success_criteria>
- The same generator runs unmodified on QALD; emits a structurally-valid, honestly-thin bank.
- Data-driven shapes drop with logged reasons; no silent gap.
- No-ontology-branch static test green (REQ-5); no test-entity leakage; no installs.
</success_criteria>

<output>
Create `.planning/phases/07.5-nl-sparql-query-first-synthetic-few-shot-bank/07.5-04-SUMMARY.md` when done.
</output>
