---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 02
type: execute
wave: 2
depends_on: [07.3-01]
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: false
requirements: [NL-ACC-01]

must_haves:
  truths:
    - "The engine-side grounding commit is fetchable from the git remote the pyproject pin references (github.com/ArthurKeen/arango-query-core)"
    - "pyproject.toml's arango-query-core pin (both the `nl` and `nl[dense]` extra lines) points at the new commit SHA"
    - "uv.lock resolves the new SHA and the new grounding symbols import from the installed package"
  artifacts:
    - path: "pyproject.toml"
      provides: "Bumped arango-query-core git pin to the grounding-seam commit"
      contains: "arango-query-core @ git+"
    - path: "uv.lock"
      provides: "Locked resolution of the new SHA"
      contains: "arango-query-core"
  key_links:
    - from: "pyproject.toml pin"
      to: "installed arango_query_core.nl.grounding"
      via: "uv lock + reinstall"
      pattern: "arango-query-core @ git\\+"
---

<objective>
Publish the Plan 01 engine-side grounding commit to the git remote(s) and bump this repo's
`arango-query-core` pin so downstream plans (03/04/05) can import + verify
`arango_query_core.nl.grounding.LabelIndex` end-to-end. This is the cross-repo coordination
gate for NL-ACC-01's "language-agnostic seam in `arango_query_core`" clause. It follows the
07-01 / 07-03 pin-bump playbook (STATE.md) plus RESEARCH Pitfall 4's dual-remote verification.

Purpose: The pin currently references `github.com/ArthurKeen/arango-query-core@acb60ae...`
while the local checkout's origin is `github.com/arango-solutions/arango-query-core`; both
resolve to the same commit today but the new grounding commit must be pushed to and
verified fetchable from the pinned remote BEFORE `uv lock`, or the lock silently resolves
stale content.
Output: pushed commit on the pinned remote, bumped `pyproject.toml` pin (both extra lines),
regenerated `uv.lock`, verified `from arango_query_core.nl.grounding import LabelIndex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-RESEARCH.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-01-SUMMARY.md
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Push the engine grounding commit to the pinned remote</name>
  <read_first>
    - 07.3-01-SUMMARY.md — the new local commit SHA in the arango-query-core checkout.
    - 07.3-RESEARCH.md Common Pitfalls Pitfall 4 (git-pin remote mismatch: pyproject pins ArthurKeen/, local origin is arango-solutions/; verify BOTH via git ls-remote before uv lock).
    - pyproject.toml lines 62 + 72 — the two `arango-query-core @ git+https://github.com/ArthurKeen/arango-query-core@<sha>` pin sites.
  </read_first>
  <what-built>
    Plan 01 committed the grounding seam locally in `/Users/plosiewicz/Desktop/arango-query-core`
    (branch main) but did NOT push. The pyproject pin references the `ArthurKeen/arango-query-core`
    remote, which is human-controlled and may require credentials the agent does not hold.
  </what-built>
  <how-to-verify>
    1. In `/Users/plosiewicz/Desktop/arango-query-core`, confirm `git log -1 --format=%H` matches
       the SHA recorded in 07.3-01-SUMMARY.md and the working tree is clean (`git status -s`).
    2. Push the commit to BOTH remotes that resolve the pin: `origin`
       (arango-solutions/arango-query-core) AND the ArthurKeen/arango-query-core remote the
       pyproject pin references (add it as a named remote if absent). If you lack push rights to
       one, push what you can and report which remote now carries the commit.
    3. Verify fetchability from the PINNED remote:
       `git ls-remote https://github.com/ArthurKeen/arango-query-core <SHA>` returns a line, AND
       `git ls-remote https://github.com/arango-solutions/arango-query-core <SHA>` returns a line.
    4. Paste back: the SHA, and the two `git ls-remote` outputs.
  </how-to-verify>
  <resume-signal>Type "pushed" with the SHA + both git ls-remote outputs, or describe the blocker (e.g. missing push rights to ArthurKeen).</resume-signal>
  <acceptance_criteria>
    - `git ls-remote https://github.com/ArthurKeen/arango-query-core <SHA>` returns the SHA (the pin target is fetchable).
    - The pasted SHA equals the local commit SHA from 07.3-01-SUMMARY.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bump the pyproject pin (both extras) + uv lock + verify import</name>
  <read_first>
    - pyproject.toml lines ~62 (nl extra) and ~72 (nl[dense] extra) — the two pin sites; both currently `@acb60aeeeec2082338a9386d6aa9fd38bb903bdb`.
    - 07.3-RESEARCH.md Standard Stack (the "commit engine-side → push → bump pin → uv lock" workflow established by 07-01, verified via git ls-remote before uv lock).
  </read_first>
  <action>
    Replace `acb60aeeeec2082338a9386d6aa9fd38bb903bdb` with the new grounding SHA at BOTH pin
    sites in `pyproject.toml` (the `nl` extra line and the `nl[dense]` extra line) — leave the
    `github.com/ArthurKeen/arango-query-core` host unchanged. Run `uv lock` to regenerate
    `uv.lock` against the new SHA (only after Task 1 confirmed the SHA is fetchable from the
    pinned remote). Reinstall the `nl` extra into the working environment
    (`uv sync --extra nl` or the repo's established install command) so downstream plans import
    the new module.
  </action>
  <verify>
    <automated>test "$(grep -c acb60aeeeec2082338a9386d6aa9fd38bb903bdb pyproject.toml)" = "0" && python -c "from arango_query_core.nl.grounding import GroundedEntity, LabelIndex; from arango_query_core.nl.engine import NLQueryEngine; assert 'grounding_k' in NLQueryEngine.__dataclass_fields__; print('grounding seam installed')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c acb60aeeeec2082338a9386d6aa9fd38bb903bdb pyproject.toml` returns 0 (both pin sites bumped).
    - `python -c "from arango_query_core.nl.grounding import GroundedEntity, LabelIndex"` succeeds against the installed package.
    - `NLQueryEngine.__dataclass_fields__` contains `grounding_k`.
    - `uv.lock` diff shows the arango-query-core rev updated to the new SHA (no unrelated dependency churn).
    - NON-REGRESSION: `git diff --stat -- arango_sparql/translate/` is empty (transpiler untouched).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external git remote → this repo's locked dependency | The pinned `arango-query-core` content is pulled from a human-controlled GitHub repo into this repo's build via `uv lock`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-SC | Tampering | arango-query-core git pin (pyproject.toml + uv.lock) | mitigate | Verify the exact new SHA is fetchable from BOTH the pinned remote (ArthurKeen/) and origin (arango-solutions/) via `git ls-remote <SHA>` BEFORE `uv lock` (Pitfall 4), preventing a stale/divergent-remote resolution. The push to the human-controlled pinned remote is a blocking human checkpoint (Task 1) — the agent never silently trusts an unpushed/unverified SHA. No new PyPI packages are added (git-pin content change only), so the package-legitimacy checkpoint protocol does not apply. |
| T-07.3-SC2 | Elevation of Privilege | pin host string | accept | The pin host (`github.com/ArthurKeen/...`) is unchanged from the committed baseline; only the `@<sha>` fragment moves. A host change would be out-of-scope and is explicitly not performed here. |
</threat_model>

<verification>
- `grep -c acb60aeeeec2082338a9386d6aa9fd38bb903bdb pyproject.toml` == 0.
- `python -c "from arango_query_core.nl.grounding import LabelIndex"` succeeds.
- `git diff --stat -- arango_sparql/translate/` empty (non-regression).
- New SHA confirmed fetchable from the pinned remote via `git ls-remote` (Task 1 evidence).
</verification>

<success_criteria>
- Engine grounding commit pushed + verified fetchable from the pinned remote.
- pyproject pin bumped at both extra sites; `uv.lock` regenerated; grounding symbols import from the installed package.
- Transpiler package untouched.
</success_criteria>

<output>
Create `.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-02-SUMMARY.md` when done — record the published SHA + both git ls-remote confirmations.
</output>
