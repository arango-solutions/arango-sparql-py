---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 02
subsystem: nl2sparql
tags: [nl-to-sparql, entity-grounding, arango-query-core, dependency-pin, uv]

# Dependency graph
requires:
  - phase: 07.3-01 (engine-side entity/instance grounding seam)
    provides: "arango_query_core.nl.grounding.{GroundedEntity, LabelIndex}, QueryLanguageAdapter.grounding_index() seam 6, NLQueryEngine.grounding_k, committed locally in arango-query-core as 11b71d5 (later superseded)"
provides:
  - "arango-sparql-py's arango-query-core git pin bumped (both nl and nl[dense] extras) to the published, CI-clean grounding-seam commit 3ff0d53d0eb39aca80b4ec01a93deae7939569e9"
  - "uv.lock regenerated and reinstalled; arango_query_core.nl.grounding.{GroundedEntity, LabelIndex} and NLQueryEngine.grounding_k now importable from the installed package in this repo"
affects: [07.3-03 (SparqlAdapter seam-6 wiring + grounding_prompt_section wording), 07.3-04, 07.3-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-repo pin-bump playbook (07-01/07-03 precedent) extended: commit engine-side -> human pushes to pinned remote -> verify BOTH remotes via git ls-remote refs/heads/main -> bump pyproject.toml pin -> uv lock -> uv sync --extra nl -> verify import"

key-files:
  created: []
  modified:
    - pyproject.toml
    - uv.lock

key-decisions:
  - "Pinned SHA is 3ff0d53d0eb39aca80b4ec01a93deae7939569e9, NOT the 11b71d5 recorded in 07.3-01-SUMMARY.md — the human's push surfaced mypy+ruff CI failures on 11b71d5 which were fixed in a follow-up commit on the arango-query-core main branch; 3ff0d53 is the CI-clean HEAD that both remotes now carry, confirmed via git ls-remote refs/heads/main against both github.com/ArthurKeen/arango-query-core (pin target) and github.com/arango-solutions/arango-query-core (origin)"
  - "git ls-remote <sha> (exact commit) returns nothing since ls-remote only enumerates refs, not arbitrary commits; verification used git ls-remote refs/heads/main and compared the returned SHA to 3ff0d53 instead — documented here so future plans don't repeat the same dead-end command"
  - "uv lock surfaced pre-existing, unrelated lock staleness (mypy>=1.11, ruff==0.15.22, arangodb-schema-analyzer[anthropic] extra) that had been added to pyproject.toml in a prior commit (c2fafe7) but never re-locked; this repair was mechanically necessary for uv lock to produce a lock file consistent with pyproject.toml and is not scope creep introduced by this plan's SHA bump"
  - "Prose comment near pyproject.toml's nl extra (previously referencing acb60ae) updated to name 3ff0d53 and the Phase 7.3 grounding seam, keeping the pin rationale comment accurate for future readers"

patterns-established: []

requirements-completed: []  # NL-ACC-01 spans Plans 01-05; not marked complete until the full seam is adopted + proven (Plan 06)

# Metrics
duration: ~10min
completed: 2026-07-23
---

# Phase 07.3 Plan 02: Publish + pin-bump the entity/instance grounding engine seam Summary

**Bumped arango-sparql-py's arango-query-core git pin (both extras) from acb60ae to the published, CI-clean grounding-seam commit 3ff0d53, verified fetchable from both remotes before `uv lock`, and confirmed `arango_query_core.nl.grounding.{GroundedEntity,LabelIndex}` + `NLQueryEngine.grounding_k` import from the installed package.**

## Performance

- **Duration:** ~10 min (Task 2 only; Task 1's human-action checkpoint was resolved out-of-band before this continuation started)
- **Completed:** 2026-07-23
- **Tasks:** 2/2 completed (Task 1: human-action checkpoint, resolved prior to this continuation; Task 2: executed this session)
- **Files modified:** 2 (pyproject.toml, uv.lock)

## Accomplishments
- Confirmed the published SHA `3ff0d53d0eb39aca80b4ec01a93deae7939569e9` is fetchable from BOTH remotes via `git ls-remote <remote> refs/heads/main` (exact-SHA `ls-remote` returns nothing since it only enumerates refs, not arbitrary commits — used the branch-ref form instead):
  - `https://github.com/ArthurKeen/arango-query-core.git refs/heads/main` -> `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`
  - `https://github.com/arango-solutions/arango-query-core.git refs/heads/main` -> `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`
- Bumped both `pyproject.toml` pin sites (`nl` extra line, `nl[dense]` extra line) from `acb60aeeeec2082338a9386d6aa9fd38bb903bdb` to `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`; host (`github.com/ArthurKeen/arango-query-core`) left unchanged per T-07.3-SC2.
- Updated the stale prose comment above the `nl` extra to name `3ff0d53` and describe the Phase 7.3 grounding seam instead of the old `acb60ae`/DenseRetriever wording.
- Regenerated `uv.lock` via `uv lock`; `arango-query-core` package source now resolves to `git = "https://github.com/ArthurKeen/arango-query-core?rev=3ff0d53d0eb39aca80b4ec01a93deae7939569e9#3ff0d53d0eb39aca80b4ec01a93deae7939569e9"`.
- Reinstalled via `uv sync --extra nl`; verified `from arango_query_core.nl.grounding import GroundedEntity, LabelIndex; from arango_query_core.nl.engine import NLQueryEngine; assert 'grounding_k' in NLQueryEngine.__dataclass_fields__` succeeds against the installed package.
- Verified non-regression: `git diff --stat -- arango_sparql/translate/` is empty (transpiler untouched).

## Task Commits

1. **Task 1: Push the engine grounding commit to the pinned remote** - resolved out-of-band by the human before this continuation (checkpoint:human-action; no commit in this repo). Published SHA: `3ff0d53d0eb39aca80b4ec01a93deae7939569e9` (supersedes the `11b71d5` originally recorded in 07.3-01-SUMMARY.md after upstream fixed mypy+ruff CI findings).
2. **Task 2: Bump the pyproject pin (both extras) + uv lock + verify import** - `c3f8765` (feat)

**Plan metadata:** committed via the final metadata commit below.

## Files Created/Modified
- `pyproject.toml` - both `arango-query-core` git pin sites bumped to `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`; prose comment updated to reference the new SHA and the grounding seam
- `uv.lock` - regenerated; `arango-query-core` source now points at the ArthurKeen host + new SHA; incidentally caught up pre-existing staleness (mypy, ruff==0.15.22, arangodb-schema-analyzer[anthropic] extra) that had been added to pyproject.toml in commit c2fafe7 but never re-locked

## Decisions Made
- Pin bumped to `3ff0d53` (the CI-clean commit), not `11b71d5` (the originally-committed SHA that failed CI on mypy+ruff) — see key-decisions above for full rationale and both `git ls-remote` confirmations.
- `git ls-remote <remote> <sha>` (exact commit, no ref) is a dead end for this verification step since `ls-remote` only lists refs; used `git ls-remote <remote> refs/heads/main` and compared the returned SHA instead. Documented here for the next plan that needs the same dual-remote verification pattern.
- The `uv lock` run also resolved unrelated, pre-existing lock/pyproject.toml drift (mypy/ruff/anthropic-extra) dating to commit c2fafe7. This was mechanically necessary for `uv lock` to succeed with a lock file consistent with the current `pyproject.toml`, not new churn caused by this plan's SHA bump.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pinned SHA changed from the plan's expected 11b71d5 to the CI-clean 3ff0d53**
- **Found during:** Continuation start (checkpoint context provided by the orchestrator) — the original Task 1 push of `11b71d5` to the pinned remote failed CI (mypy + ruff findings in arango-query-core); a follow-up commit fixed both and the human pushed the fixed HEAD, `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`, to both remotes instead.
- **Issue:** 07.3-01-SUMMARY.md and 07.3-02-PLAN.md both reference `11b71d5` as the SHA to pin; that commit was never the final, CI-verified state of arango-query-core's main branch.
- **Fix:** Used the corrected, CI-clean SHA (`3ff0d53`) for both `pyproject.toml` pin sites and for the `git ls-remote refs/heads/main` fetchability verification, since it is what both remotes' `main` branches actually carry.
- **Files modified:** `pyproject.toml`
- **Verification:** `git ls-remote https://github.com/ArthurKeen/arango-query-core.git refs/heads/main` and the `arango-solutions` equivalent both return `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`; `uv lock` + `uv sync --extra nl` + the grounding-symbol import all succeed against this SHA.
- **Committed in:** `c3f8765` (Task 2 commit)

**2. [Rule 3 - Blocking] uv.lock was already stale relative to pyproject.toml before this plan touched it**
- **Found during:** Task 2 — `uv lock` output showed unrelated diffs (mypy added to arango-query-core's own `dev` extra, `ruff>=0.6.0` -> `ruff==0.15.22`, `arangodb-schema-analyzer` gaining the `anthropic` extra) beyond the expected `arango-query-core` rev bump.
- **Issue:** `git log` confirmed commit `c2fafe7` ("ruff format the repo + pin ruff==0.15.22") had already updated `pyproject.toml`'s `dev` extra (adding `mypy>=1.11`, changing the ruff pin) without a corresponding `uv lock` re-run, leaving `uv.lock` inconsistent with `pyproject.toml` since before this plan started.
- **Fix:** Let `uv lock` catch the lock file up to the current `pyproject.toml` state in the same run as the SHA bump, since `uv lock` always resolves against the full current `pyproject.toml`, not just the changed lines. No `pyproject.toml` content was altered beyond the two pin sites and the one comment.
- **Files modified:** `uv.lock`
- **Verification:** `git diff uv.lock` reviewed line-by-line; every change traces either to the `arango-query-core` rev bump or to pre-existing `pyproject.toml` content (`ruff==0.15.22`, `mypy>=1.11`, `arangodb-schema-analyzer[anthropic]`) that was already committed but unlocked. No new top-level dependency was added to `pyproject.toml` itself by this plan.
- **Committed in:** `c3f8765` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 1 SHA-supersession bug, 1 Rule 3 pre-existing lock-staleness blocker)
**Impact on plan:** Both were mechanically necessary — the first ensures the pin references content that actually passed CI and is genuinely present on both remotes; the second was required for `uv lock` to produce a consistent lock file. Neither touched `arango_sparql/translate/` or altered the plan's design/scope.

## Issues Encountered
None beyond the two deviations documented above.

## User Setup Required
None - Task 1 (the only step requiring human action, pushing to the human-controlled `ArthurKeen/arango-query-core` remote) was resolved before this continuation started; see `<checkpoint_already_resolved>` context. No further external service configuration is required.

## Next Phase Readiness
- `arango_query_core.nl.grounding.{GroundedEntity, LabelIndex}`, `QueryLanguageAdapter.grounding_index()` (seam 6), and `NLQueryEngine.grounding_k` are now importable from the installed `arango-query-core` package in this repo (pinned to `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`, verified fetchable from both remotes).
- Plan 03 can now wire `SparqlAdapter`'s seam-6 implementation (`grounding_index()` + `grounding_prompt_section()` with the spike's exact verbatim wording) against the installed grounding symbols without any further pin work.
- `arango_sparql/translate/` (the W3C transpiler) is untouched; no regression risk introduced by this plan.
- Full engine-side test suite (90 tests) was already verified green in 07.3-01 against the pre-CI-fix commit; the CI fix itself (mypy+ruff) was applied and verified upstream in `arango-query-core` before the human pushed `3ff0d53` (per `<checkpoint_already_resolved>`), not re-verified in this repo (no test runner for the engine's own suite lives here).

## Self-Check: PASSED

- FOUND: `pyproject.toml` contains `3ff0d53d0eb39aca80b4ec01a93deae7939569e9` (2 occurrences), 0 occurrences of `acb60aeeeec2082338a9386d6aa9fd38bb903bdb`
- FOUND: `uv.lock` contains `3ff0d53d0eb39aca80b4ec01a93deae7939569e9`
- FOUND: commit `c3f8765` (Task 2) in arango-sparql-py
- FOUND: `git diff --stat -- arango_sparql/translate/` empty (non-regression holds)
- FOUND: `python -c "from arango_query_core.nl.grounding import GroundedEntity, LabelIndex; from arango_query_core.nl.engine import NLQueryEngine; assert 'grounding_k' in NLQueryEngine.__dataclass_fields__"` succeeds against the installed package
