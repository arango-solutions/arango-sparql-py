---
phase: 07.3-nl-to-sparql-entity-instance-grounding
verified: 2026-07-24T15:10:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 07.3: NL-to-SPARQL Entity/Instance Grounding Verification Report

**Phase Goal:** Productionize entity/instance grounding as a language-agnostic seam (seam 6) in
the shared `arango_query_core` NL engine (Cypher inherits it); retrieve candidate instance
IRIs+labels from the target instance data via a schema-agnostic index, inject them into the
prompt so the model binds to real IRIs; prove a statistically significant, execution-graded
accuracy lift on the CK25 corporate anchor over the zero-shot baseline.

**Verified:** 2026-07-24T15:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Seam 6 (`grounding_index()`/`grounding_prompt_section()`) is a real, language-agnostic member of `QueryLanguageAdapter` in the shared `arango_query_core` engine, not a SPARQL-only hack | VERIFIED | Installed package check: `from arango_query_core.nl.grounding import GroundedEntity, LabelIndex; from arango_query_core.nl.seams import QueryLanguageAdapter; assert hasattr(QueryLanguageAdapter,'grounding_index') and hasattr(QueryLanguageAdapter,'grounding_prompt_section')` → `seam ok`. Sibling checkout `/Users/plosiewicz/Desktop/arango-query-core` is at commit `c6ae5e14360699618ba4a38c109d54d8173633ab`, clean working tree, matching the pin in `pyproject.toml` (2 pin sites, `nl` + `nl[dense]` extras) exactly. |
| 2 | Instance IRIs+labels are retrieved from a schema-agnostic index and injected into the LLM prompt (SPARQL adapter, the first consumer) | VERIFIED | `arango_sparql/nl2sparql/engine_adapter.py::SparqlAdapter.grounding_index/grounding_prompt_section` and the legacy `adapter.py::SparqlLanguageAdapter` (Rule-3 fix, Plan 03) both implement seam 6. `NlPipeline` threads `grounding_k`/`grounding_index` through (`pipeline.py`). SC-gate `tests/nl2sparql/test_grounding_engine_prompt.py` (2 passed) proves the "## Known entities" block lands in the engine-composed `_system_prompt` and never leaks into the cacheable `grammar_prompt_section`. Cross-adapter parity enforced by `tests/nl2sparql/test_grounding_prompt_parity.py` (WR-02 fix, 3 passed). |
| 3 | Retrieval mechanism proven intact offline (deterministic recall guard) and precision/purity-checked (post-review) | VERIFIED | `tests/nl2sparql/eval/test_grounding_recall.py` runs in the always-on CI tier (no `RUN_EVAL`), asserts gold-IRI recall ≥0.90 (measured 0.96, matches spike). Post-review addition `tests/nl2sparql/eval/test_grounding_index_builder.py` (WR-03 fix, 3 passed) directly unit-tests `build_label_index()` for schema-term exclusion (CR-01) and clean label normalization (CR-02). All ran green in this session. |
| 4 | Code-review BLOCKER findings (CR-01 schema-IRI leak, CR-02 label-corruption) are actually fixed in the shipped builder, not just narrated | VERIFIED | `git log` shows `f932360` (CR-01) and `6c72b37` (CR-02) as real diffs to `tests/nl2sparql/eval/grounding_index_builder.py`. Read current file content: CR-01's `FILTER NOT EXISTS` schema-type/structural exclusions and CR-02's `split('"^^')[0].strip('"').split('"@')[0]` correct-order stripping are both present in the query/normalization code, matching the fix commit diffs exactly (not just comments). Also WR-01 (`1264da3`), WR-02 (`a45a30e`), WR-03 (`0f1e442`) fix commits all exist and their corresponding tests pass. |
| 5 | Additive `grounding:` config is default-preserving; scripted stays the CI-reachable default (no live LLM calls in CI) | VERIFIED | `tests/nl2sparql/eval/test_eval.py -k grounding_default` passes; `test_ci_gate_only_ever_runs_scripted` passes both plain (`1 skipped` — RUN_EVAL-gated, correctly not running live) and under `RUN_EVAL=1` (`1 passed`). `configs.yml` has `scripted-ck25-grounded` (scripted, no network) + `openai-gpt4o-mini-ck25-grounded` (live, human-run only). |
| 6 | Statistically significant, execution-graded accuracy lift proven on CK25, same-session grounded-vs-fresh-zero, on the (post-review) corrected builder | VERIFIED | `tests/nl2sparql/eval/baseline.json:configs.openai-gpt4o-mini-ck25-grounded` — pass_rate 14/49 (0.2857) vs `confirmatory_test.fresh_zero_arm_pass_rate` 5/49 (0.1020); `b=9, c=0, p_value=0.0039`; bootstrap `paired_delta=0.1837`, `ci_95=[0.0816, 0.3061]` (excludes 0); `regressed_1_to_0=[]` (zero regressions). `confirmatory_test.clean_builder_reconfirmation` records the full re-run on the CR-01/CR-02-corrected builder producing an **identical** aggregate result (only the specific gained-case identities shifted) — the significant lift is not an artifact of the pre-fix bug. README §9 documents the full reproduction runbook. |
| 7 | Non-regression invariants hold: W3C DAWG QUERY_EVAL ≥96.4%, SPARQL→AQL transpiler untouched, pyoxigraph confined to tests/ | VERIFIED | `pytest tests/w3c/test_coverage_gate.py -q` → 1 passed. `git diff --stat a7903e9..HEAD -- arango_sparql/translate/` → empty. `grep -rn "^import pyoxigraph\|^from pyoxigraph" arango_sparql/` → empty; `grounding_index_builder.py`'s only pyoxigraph-touching import (`from tests.helpers.oxi import ...`) is function-local inside `build_label_index`, confirmed by direct file read. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `arango_query_core/nl/grounding.py` (sibling repo) | `GroundedEntity`/`LabelIndex` + generic renderer | VERIFIED | Importable from installed package at pin `c6ae5e1`; sibling checkout clean at same SHA. |
| `arango_query_core/nl/seams.py` (sibling repo) | Seam 6 protocol member | VERIFIED | `hasattr(QueryLanguageAdapter, 'grounding_index')` true. |
| `arango_sparql/nl2sparql/engine_adapter.py` | `SparqlAdapter` seam-6 impl + verbatim wording | VERIFIED | `grep -c "def grounding_index"` = 1; tests pass. |
| `arango_sparql/nl2sparql/adapter.py` | Legacy adapter seam-6 (Rule-3 fix) | VERIFIED | `test_adapter.py` 10/10 pass (was 7/10 broken pre-fix per 03-SUMMARY). |
| `arango_sparql/nl2sparql/pipeline.py` | `NlPipeline` grounding passthrough | VERIFIED | `grounding_index=`/`grounding_k=` call sites present; `test_pipeline.py` green. |
| `tests/nl2sparql/eval/grounding_index_builder.py` | pyoxigraph-confined builder, CR-01/CR-02 fixed | VERIFIED | Read file; schema-exclusion filters + correct-order label stripping both present; pyoxigraph import function-local. |
| `tests/nl2sparql/eval/test_grounding_recall.py` | Deterministic recall guard ≥0.90 | VERIFIED | 2 passed (with `test_eval.py -k recall`), always-on tier. |
| `tests/nl2sparql/eval/test_grounding_index_builder.py` | WR-03 precision/purity unit tests | VERIFIED | 3 passed. |
| `tests/nl2sparql/test_grounding_engine_prompt.py` | SC-gate (block engine-composed, not in grammar prefix) | VERIFIED | 2 passed. |
| `tests/nl2sparql/test_grounding_prompt_parity.py` | WR-02 cross-adapter parity | VERIFIED | 3 passed. |
| `tests/nl2sparql/eval/runner.py` + `configs.yml` | Additive `grounding:` config, build-once, CK25-grounded configs | VERIFIED | `grep -c "pv:name" runner.py` = 1 pre-existing unrelated docstring line (net-new 0, per 05-SUMMARY, confirmed acceptable); config entries present. |
| `tests/nl2sparql/eval/baseline.json` | CK25-grounded confirmatory entry | VERIFIED | Full `confirmatory_test` block present with McNemar/bootstrap/clean-builder reconfirmation. |
| `tests/nl2sparql/eval/README.md` §9 | Reproduction runbook | VERIFIED | §9.1–9.6 present including post-review correction section. |
| `pyproject.toml` / `uv.lock` | Pin bumped to CI-green grounding commit | VERIFIED | Both pin sites at `c6ae5e1...`; sibling repo confirmed at same SHA, clean tree. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `NLQueryEngine._system_prompt` | `adapter.grounding_index()` + `adapter.grounding_prompt_section()` | seam-6 splice, after few-shot | WIRED | `test_engine_composes_grounding_block` (engine repo) + `test_grounding_engine_prompt.py` (this repo) both assert block presence/ordering. |
| `NlPipeline.run()` | `SparqlAdapter(grounding_index=...)` + `NLQueryEngine(grounding_k=...)` | constructor passthrough | WIRED | Grep-confirmed call sites; `test_pipeline.py` green with defaults unchanged. |
| `runner.py::run()` | `build_label_index(data_ttl, label_predicates)` → `NlPipeline(grounding_index=...)` | build-once before per-case loop | WIRED | Build call precedes `for case in corpus["cases"]:` loop (05-SUMMARY line-order check); `scripted-ck25-grounded` plumbing gate green (pass_rate 1.0, per 05-SUMMARY). |
| `grounding_index_builder.build_label_index` | `arango_query_core.nl.grounding.LabelIndex` + `tests.helpers.oxi` | function-local import | WIRED | Confirmed via direct file read; `grep` for module-level pyoxigraph import returns nothing. |
| human-run live sweep | `baseline.json` CK25-grounded entry | manual human-reviewed fold-in | WIRED | Entry present with full provenance; not CI-auto-regenerated (no code path writes it). |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| NL-ACC-01 | 01, 02, 03, 04, 05, 06 (all 6 plans declare it) | Productionize entity/instance grounding as a language-agnostic seam; prove significant execution-graded CK25 lift | SATISFIED | REQUIREMENTS.md line 53 marked `[x]`, closure text matches `baseline.json` numbers exactly (14/49 vs 5/49, p=0.0039, 0 regressions); traceability table (line 119) shows `Phase 07.3 | Complete`. No orphaned requirements found for Phase 07.3 (NL-ACC-01 is the only ID mapped). |

### Anti-Patterns Found

None. Scanned all phase-modified files (`engine_adapter.py`, `pipeline.py`, `adapter.py`,
`grounding_index_builder.py`, `runner.py`, `test_grounding_recall.py`,
`test_grounding_index_builder.py`, `test_grounding_engine_prompt.py`,
`test_grounding_prompt_parity.py`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and
"not yet implemented"/"coming soon" phrasing — zero matches.

### Code Review Closure Verification

The phase's own `07.3-REVIEW.md` found 2 blockers + 3 warnings. All 5 have real fix commits,
independently confirmed (not just narrated in SUMMARY.md):

| Finding | Fix commit | Verified |
|---------|-----------|----------|
| CR-01 (schema-IRI leak) | `f932360` | Diff + current file content confirm `FILTER NOT EXISTS` schema-type + structural exclusions present |
| CR-02 (label-strip order bug) | `6c72b37` | Diff + current file content confirm correct `split('"^^')[0].strip('"').split('"@')[0]` order |
| WR-01 (hardcoded prefix map) | `1264da3` | `prefixes` parameter added, config-extensible |
| WR-02 (no cross-adapter parity test) | `a45a30e` | `tests/nl2sparql/test_grounding_prompt_parity.py` exists, 3 passed |
| WR-03 (no precision/purity unit test) | `0f1e442` | `tests/nl2sparql/eval/test_grounding_index_builder.py` exists, 3 passed |

The full grounded-vs-fresh-zero sweep was re-run on the corrected builder
(`confirmatory_test.clean_builder_reconfirmation` in `baseline.json`) and produced an
**identical** aggregate result (14/49 vs 5/49, p=0.0039) — the significant lift predates and
survives the bug fixes, addressing the review's core concern that the headline number might
have been produced by noisy/corrupted evidence.

### Test Execution Summary (this session)

| Suite | Command | Result |
|-------|---------|--------|
| Seam import (installed package) | `python -c "from arango_query_core.nl.grounding import ...; assert hasattr(...)"` | `seam ok` |
| Grounding recall + config-default | `pytest tests/nl2sparql/eval/test_grounding_recall.py tests/nl2sparql/eval/test_eval.py -k "grounding or recall"` | 2 passed |
| All grounding-tagged tests | `pytest tests/nl2sparql/ -k grounding` | 8 passed |
| Builder precision/purity + parity | `pytest tests/nl2sparql/eval/test_grounding_index_builder.py tests/nl2sparql/test_grounding_prompt_parity.py` | 3 passed |
| Full offline nl2sparql suite | `pytest tests/nl2sparql/` | 171 passed, 603 skipped |
| W3C DAWG coverage gate | `pytest tests/w3c/test_coverage_gate.py` | 1 passed |
| CI-default gate (plain + RUN_EVAL=1) | `pytest tests/nl2sparql/eval/test_eval.py -k ci_gate_only_ever_runs_scripted` (both modes) | 1 skipped (plain, correct) / 1 passed (RUN_EVAL=1) |
| Transpiler non-regression | `git diff --stat a7903e9..HEAD -- arango_sparql/translate/` | empty |
| pyoxigraph confinement | `grep -rn "^import pyoxigraph\|^from pyoxigraph" arango_sparql/` | empty |

### Human Verification Required

None. All must-haves were verifiable programmatically: the engine-side seam is directly
importable and inspectable, the eval-harness tests are deterministic and were re-run in this
session, and the confirmatory live-sweep statistics are recorded with full provenance
(McNemar b/c/p, bootstrap CI, corpus_sha, temperature) that was independently checked against
the file content rather than trusted from SUMMARY.md narrative alone.

### Gaps Summary

No gaps. All 7 derived observable truths verified against the actual codebase (not SUMMARY.md
claims): the seam exists and is language-agnostic (sibling repo, pin-verified), the SPARQL
adapter and pipeline wire it end-to-end with a passing SC-gate proving the cache-boundary
invariant, the eval-only builder's two code-review blockers are genuinely fixed (verified via
git diff + current file content, not just commit messages), the deterministic recall and
precision/purity guards pass in this session, the additive config is default-preserving with
scripted remaining the CI default, and the confirmatory statistically-significant CK25 lift
(McNemar p=0.0039, 0 regressions) is recorded with full provenance and was independently
re-confirmed on the corrected builder with an identical result. All non-regression invariants
(W3C ≥96.4%, transpiler untouched, pyoxigraph confined to tests/) hold.

---

_Verified: 2026-07-24T15:10:00Z_
_Verifier: Claude (gsd-verifier)_
