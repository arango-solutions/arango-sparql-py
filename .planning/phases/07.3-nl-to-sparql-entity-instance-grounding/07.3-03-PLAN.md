---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 03
type: execute
wave: 3
depends_on: [07.3-02]
files_modified:
  - arango_sparql/nl2sparql/engine_adapter.py
  - arango_sparql/nl2sparql/pipeline.py
  - tests/nl2sparql/test_grounding_engine_prompt.py
  - tests/nl2sparql/test_engine_adapter.py
autonomous: true
requirements: [NL-ACC-01]

must_haves:
  truths:
    - "SparqlAdapter accepts an injected grounding_index and returns it from grounding_index() (seam 6), else None"
    - "SparqlAdapter.grounding_prompt_section() renders the SPARQL-specific 'use these EXACT IRIs' wording (verbatim from the spike) via the engine-side generic renderer"
    - "The '## Known entities' block appears in NLQueryEngine._system_prompt but NEVER in SparqlAdapter.grammar_prompt_section() (Pitfall 1 cache-boundary invariant)"
    - "NlPipeline threads grounding_k + grounding_index through to SparqlAdapter and NLQueryEngine"
  artifacts:
    - path: "arango_sparql/nl2sparql/engine_adapter.py"
      provides: "SparqlAdapter seam-6 impl + grounding_prompt_section wording"
      contains: "def grounding_index"
    - path: "arango_sparql/nl2sparql/pipeline.py"
      provides: "NlPipeline grounding_k/grounding_index passthrough"
      contains: "grounding_index"
    - path: "tests/nl2sparql/test_grounding_engine_prompt.py"
      provides: "SC-gate: block in engine prompt, not in grammar section"
      contains: "Known entities"
  key_links:
    - from: "NlPipeline.run"
      to: "SparqlAdapter(grounding_index=...) + NLQueryEngine(grounding_k=...)"
      via: "constructor passthrough"
      pattern: "grounding_index="
    - from: "SparqlAdapter.grounding_prompt_section"
      to: "LabelIndex.format_prompt_section"
      via: "adapter-owned header/instruction wording"
      pattern: "format_prompt_section"
---

<objective>
Wire the SPARQL adapter as the first consumer of the new engine seam 6: `SparqlAdapter`
implements `grounding_index()` (explicit-injection-only, no production default this phase)
and owns the SPARQL-specific `grounding_prompt_section()` wording; `NlPipeline` threads
`grounding_k`/`grounding_index` through. Adds the SC-gate proving the "## Known entities"
block lands in the engine-composed prompt and never leaks into the static schema prefix.
Implements the adapter-side half of NL-ACC-01's seam productionization.

Purpose: Keep the engine renderer language-agnostic (Plan 01) while the exact instruction
wording that produced the measured +12.2pt lift stays adapter-owned and verbatim.
Output: SparqlAdapter seam impl + wording, NlPipeline passthrough, SC-gate test, adapter
unit tests — all verified against the now-installed grounding seam (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-RESEARCH.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SparqlAdapter seam 6 + grounding_prompt_section (SPARQL wording)</name>
  <read_first>
    - arango_sparql/nl2sparql/engine_adapter.py — the `few_shot_index()` seam (lines ~183-202) is the template for `grounding_index()` (but grounding has NO cached-default fallback — return the injected index or None); `grammar_prompt_section()` (lines ~178-181) is the template for keeping language-specific wording adapter-side; the constructor (lines ~165-176) + class docstring seam table (lines ~142-151) + import block (lines ~39-40).
    - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py — LabelIndex.format_prompt_section signature (header/instruction/id_prefix/id_suffix params) to call correctly.
    - scratchpad/nl-grounding-spike/grounding_spike.py::entity_block (lines ~126-139) — the EXACT header + instruction wording. Copy VERBATIM — RESEARCH warns rephrasing invalidates the measured +12.2pt.
    - 07.3-RESEARCH.md Pattern 1 (adapter-side wording block) + 07.3-PATTERNS.md engine_adapter.py section.
  </read_first>
  <behavior>
    - SparqlAdapter(grounding_index=index).grounding_index() returns that exact index object.
    - SparqlAdapter(...) with no grounding_index → grounding_index() returns None.
    - grounding_prompt_section(question, index, k) delegates to index.format_prompt_section with the verbatim spike header + instruction and id_prefix/id_suffix so IRIs render as `<IRI> — "label" (type)`.
  </behavior>
  <action>
    Extend the SparqlAdapter import block with `from arango_query_core.nl.grounding import
    LabelIndex`. Add a keyword-only `grounding_index: LabelIndex | None = None` constructor
    param stored as `self._grounding_index`. Implement `def grounding_index(self) -> LabelIndex
    | None:` (seam 6) returning `self._grounding_index` (explicit-injection-only; comment that
    no production default source exists this phase per RESEARCH Open Question 2). Implement `def
    grounding_prompt_section(self, question, index, k=20) -> str:` that calls
    `index.format_prompt_section(...)` passing the VERBATIM spike header "## Known entities (use
    these EXACT IRIs)" + the verbatim spike instruction, with `id_prefix="<"` and
    `id_suffix=">"`. Add a `grounding_index` / `LabelIndex` row to the class docstring seam
    table. Do NOT touch `grammar_prompt_section` or `schema_context` (Pitfall 1). Do NOT modify
    anything under `arango_sparql/translate/`.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/test_engine_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def grounding_index" arango_sparql/nl2sparql/engine_adapter.py` returns 1.
    - The header/instruction strings in `grounding_prompt_section` are byte-identical to `entity_block`'s in `grounding_spike.py`.
    - `SparqlAdapter(...).grounding_index()` returns the injected index, and None when none injected (proven by Task 3 unit tests).
    - `git diff --stat -- arango_sparql/translate/` is empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: NlPipeline grounding_k / grounding_index passthrough</name>
  <read_first>
    - arango_sparql/nl2sparql/pipeline.py — the `few_shot_k`/`few_shot_index` constructor fields (lines ~104-122) + the `SparqlAdapter(...)`/`NLQueryEngine(...)` construction in `run()` (lines ~143-154) + import line (~61-62) + class docstring "deferred hooks" note (lines ~95-101).
    - 07.3-PATTERNS.md pipeline.py section (literal copy-paste-rename of the few_shot plumbing).
  </read_first>
  <action>
    Add `from arango_query_core.nl.grounding import LabelIndex` to the import block. Add
    keyword-only `grounding_k: int = 20` and `grounding_index: LabelIndex | None = None`
    constructor params, stored as `self.grounding_k` / `self.grounding_index`. In `run()`, pass
    `grounding_index=self.grounding_index` into the `SparqlAdapter(...)` call and
    `grounding_k=self.grounding_k` into the `NLQueryEngine(...)` call — mirroring the existing
    few_shot args exactly. Update the docstring "deferred hooks" note: grounding now lands
    (eval-harness-injected path), but production `db`-handle wiring stays deferred (RESEARCH
    Open Question 2); production paths keep grounding_index=None (honest degraded no-op).
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.signature(NlPipeline.__init__)` contains params `grounding_index` and `grounding_k`.
    - `run()` passes `grounding_index` to SparqlAdapter and `grounding_k` to NLQueryEngine (grep the two call sites).
    - Existing test_pipeline.py stays green (default behavior byte-identical when grounding_index omitted).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC-gate (test_grounding_engine_prompt.py) + adapter unit tests</name>
  <read_first>
    - tests/nl2sparql/test_fewshot_engine_prompt.py — the FULL analog SC-gate file: sentinel-string trick, assert block in `engine._system_prompt`, assert block absent from `adapter.grammar_prompt_section`. Mirror exactly for "## Known entities".
    - tests/nl2sparql/test_engine_adapter.py — `TestSparqlAdapterSeams.test_few_shot_index_returns_populated_index` (lines ~191-195); add sibling tests for grounding_index injected + default-None.
    - 07.3-PATTERNS.md sections for test_grounding_engine_prompt.py + test_engine_adapter.py (target test bodies).
  </read_first>
  <behavior>
    - test_entities_render_in_engine_prompt_not_standalone_builder: a sentinel-labeled GroundedEntity injected into SparqlAdapter → "## Known entities" + sentinel label appear in engine._system_prompt(question,"") but NOT in adapter.grammar_prompt_section("").
    - test_grounding_index_returns_injected_index: adapter.grounding_index() is the injected object.
    - test_grounding_index_defaults_to_none: with no injection, adapter.grounding_index() is None (encodes the no-production-default design).
  </behavior>
  <action>
    Create `tests/nl2sparql/test_grounding_engine_prompt.py` mirroring
    `test_fewshot_engine_prompt.py`: build a SparqlAdapter with an injected LabelIndex holding
    one sentinel-labeled GroundedEntity, construct NLQueryEngine via EngineProviderBridge +
    ScriptedLLMClient with grounding_k=20, call `_system_prompt(f"find {_SENTINEL_LABEL}", "")`,
    assert "## Known entities" and the sentinel appear; then assert both are ABSENT from
    `adapter.grammar_prompt_section("")` (Pitfall 1 gate). Add the two sibling unit tests to
    `TestSparqlAdapterSeams` in `tests/nl2sparql/test_engine_adapter.py`. Import GroundedEntity
    + LabelIndex from `arango_query_core.nl.grounding`.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/test_grounding_engine_prompt.py tests/nl2sparql/test_engine_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/nl2sparql/ -k grounding -q` exits 0.
    - The SC-gate asserts the block is present in `_system_prompt` AND absent from `grammar_prompt_section` (Pitfall 1 invariant enforced).
    - A sentinel label string is used so matches cannot be incidental.
    - Full `python -m pytest tests/nl2sparql/ -q` (offline) stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instance-graph label data → SPARQL system prompt | The adapter renders retrieved labels into the "## Known entities" prompt block via the engine renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-01 | Tampering / Injection | SparqlAdapter.grounding_prompt_section → LabelIndex.format_prompt_section | mitigate | Label sanitization lives in the engine renderer (Plan 01, T-07.3-01); the adapter only supplies fixed header/instruction constant strings + the delimited "## Known entities" section boundary — no attacker-controlled text originates adapter-side. The downstream (unchanged) rdflib `validate` seam + SchemaResolver still gate any AQL emission. |
| T-07.3-02 | Information Disclosure | grammar_prompt_section cache boundary | mitigate | The SC-gate test asserts grounding text never leaks into the static/cacheable `grammar_prompt_section` (Pitfall 1), keeping the per-question dynamic block out of the shared cache prefix. |
</threat_model>

<verification>
- `python -m pytest tests/nl2sparql/ -k grounding -q` exits 0.
- `python -m pytest tests/nl2sparql/ -q` (full offline nl2sparql suite) green.
- `git diff --stat -- arango_sparql/translate/` empty (transpiler untouched).
</verification>

<success_criteria>
- SparqlAdapter implements seam 6 (injection-only) + verbatim SPARQL grounding wording.
- NlPipeline threads grounding_k/grounding_index; default behavior unchanged when omitted.
- SC-gate proves the block is engine-composed and never in the static schema prefix.
</success_criteria>

<output>
Create `.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-03-SUMMARY.md` when done.
</output>
