---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 04
type: execute
wave: 3
depends_on: [07.3-02]
files_modified:
  - tests/nl2sparql/eval/grounding_index_builder.py
  - tests/nl2sparql/eval/test_grounding_recall.py
  - tests/nl2sparql/eval/test_eval.py
autonomous: true
requirements: [NL-ACC-01]

must_haves:
  truths:
    - "An eval-only pyoxigraph builder turns CK25's vendored Turtle + a label_predicates list into a LabelIndex, living under tests/ (pyoxigraph never leaks into shipped packages)"
    - "A deterministic, offline, CI-visible (NOT RUN_EVAL-gated) test proves gold instance-IRI retrieval recall over CK25 stays at/above the spike floor (>=0.90; spike measured 0.96)"
    - "A structural test proves the additive grounding: config key is default-preserving (absent == today's zero-shot behavior)"
  artifacts:
    - path: "tests/nl2sparql/eval/grounding_index_builder.py"
      provides: "build_label_index(data_ttl, label_predicates) -> LabelIndex (pyoxigraph, tests-only)"
      contains: "def build_label_index"
    - path: "tests/nl2sparql/eval/test_grounding_recall.py"
      provides: "Deterministic gold-IRI retrieval-recall regression guard"
      contains: "recall"
    - path: "tests/nl2sparql/eval/test_eval.py"
      provides: "grounding_default absent-safe structural test"
      contains: "grounding_default"
  key_links:
    - from: "tests/nl2sparql/eval/grounding_index_builder.py"
      to: "arango_query_core.nl.grounding.LabelIndex + tests.helpers.oxi"
      via: "function-local pyoxigraph query, module GroundedEntity construction"
      pattern: "from tests.helpers.oxi import"
    - from: "tests/nl2sparql/eval/test_grounding_recall.py"
      to: "build_label_index over vendored/ck25/raw/prod-inst.ttl"
      via: "retrieve(case nl, k=20) vs gold prodi: IRIs"
      pattern: "prod-instances"
---

<objective>
Build the eval-only bridge from CK25's vendored RDF instance graph to the engine's
LabelIndex, and lock in the two offline guards NL-ACC-01 depends on: the deterministic
gold-IRI retrieval-recall regression test (the highest-value new CI-visible, non-LLM
evidence that grounding keeps working) and the additive-config default-preservation
structural test. Depends only on the installed grounding seam (Plan 02) — independent of the
adapter/pipeline wiring (Plan 03), so it runs in parallel with Plan 03 in Wave 3.

Purpose: The retrieval mechanism must be provably intact offline in CI (recall >= spike
floor) even though the accuracy proof itself is a human-run live sweep. The builder keeps
pyoxigraph a tests-only dependency (CLAUDE.md hard rule 5 / packaging boundary).
Output: `grounding_index_builder.py` (eval-only), `test_grounding_recall.py` (deterministic),
`test_eval.py` grounding_default structural test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-SPIKE-FINDINGS.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-RESEARCH.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval-only pyoxigraph → LabelIndex builder</name>
  <read_first>
    - tests/nl2sparql/eval/runner.py::_build_label_map (lines ~355-377) — the analog rdfs:label query + IRI/literal normalization; note its function-local `from tests.helpers.oxi import oxi_query` (line ~362) which keeps pyoxigraph off the module import path.
    - scratchpad/nl-grounding-spike/grounding_spike.py::build_entity_index (lines ~75-96) — the exact per-entity aggregation (dict by IRI, labels set, type local-name); PREFIX preamble for pv:/rdfs: (lines ~77-79).
    - tests/helpers/oxi.py — load_store_from_string + oxi_query signatures; the lazy pyoxigraph-optional pattern to preserve.
    - tests/nl2sparql/eval/vendored/ck25/corpus.yml (ontology prefix header — prodi: = http://ld.company.org/prod-instances/, pv: = http://ld.company.org/prod-vocab/, rdfs:) + data_path `raw/prod-inst.ttl`.
    - 07.3-RESEARCH.md Pattern 2 (build_label_index target code) + 07.3-PATTERNS.md grounding_index_builder.py section.
  </read_first>
  <action>
    Create `tests/nl2sparql/eval/grounding_index_builder.py` with `def build_label_index(
    data_ttl: str, label_predicates: list[str]) -> LabelIndex`. Import
    load_store_from_string + oxi_query function-locally from `tests.helpers.oxi` (never at
    module top level). Resolve the prefixed `label_predicates` (e.g. "rdfs:label", "pv:name")
    to full IRIs — either via a PREFIX preamble matching the spike's, or by expanding against
    the known ontology prefix map — and build a `SELECT ?s ?label ?type` query over the
    predicate union with `OPTIONAL { ?s a ?type }`. Aggregate rows by subject IRI into
    `{labels: set, type: local-name}`, then return `LabelIndex.from_items([GroundedEntity(...)])`
    importing GroundedEntity + LabelIndex from `arango_query_core.nl.grounding`. The file MUST
    live under tests/ and MUST NOT be importable by `arango_query_core`/`arango_sparql` proper.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/eval/test_grounding_recall.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_label_index" tests/nl2sparql/eval/grounding_index_builder.py` returns 1.
    - No module-level pyoxigraph import: `grep -n "^import pyoxigraph\|^from pyoxigraph\|^from tests.helpers.oxi" tests/nl2sparql/eval/grounding_index_builder.py` returns nothing (imports are function-local).
    - Building over CK25's prod-inst.ttl with ["rdfs:label","pv:name"] yields a non-empty index (~2600+ instances, per spike) — proven by Task 2's recall test consuming it.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic gold-IRI retrieval-recall guard</name>
  <read_first>
    - scratchpad/nl-grounding-spike/grounding_spike.py recall loop (lines ~176-190) — the recall arithmetic (per-case gold IRIs ∩ retrieved, summed) that measured 24/25 = 0.96.
    - tests/nl2sparql/eval/runner.py::_load_corpus (line ~148) + CORPUS_PATH — how to load the CK25 corpus dict; corpus["data_path"] resolution relative to the corpus file dir.
    - tests/nl2sparql/eval/test_eval.py (lines ~1-60) — the always-on (NOT `pytestmark = pytest.mark.eval`) tier; this recall test must be in that tier so it runs in CI without RUN_EVAL.
    - 07.3-PATTERNS.md test_grounding_recall.py section (target test body + IRI regex) + 07.3-VALIDATION.md per-task map (recall >= spike 0.96 floor).
  </read_first>
  <behavior>
    - Load vendored/ck25/corpus.yml, read raw/prod-inst.ttl, build the index with ["rdfs:label","pv:name"].
    - For each case with gold prodi: instance IRIs in `expected`, compute recall = (gold ∩ retrieved(nl, k=20)) / gold, summed across cases.
    - Assert recall >= 0.90 with a message citing the spike's 24/25 = 0.96.
  </behavior>
  <action>
    Create `tests/nl2sparql/eval/test_grounding_recall.py` (deterministic, offline, no LLM,
    NOT under `pytestmark = pytest.mark.eval`). Use IRI regex
    `<http://ld\.company\.org/prod-instances/[^>]+>` to extract gold instance IRIs from each
    case's `expected` gold SPARQL (skip cases with no instance IRI). Build the index via
    build_label_index, retrieve top-k=20 per case's `nl`, sum hits/total, assert `recall >=
    0.90` with a message referencing the spike floor (24/25 = 0.96). Do NOT gate on RUN_EVAL —
    this is the always-runs CI evidence.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/eval/test_grounding_recall.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/nl2sparql/eval/test_grounding_recall.py -q` exits 0 with NO RUN_EVAL env var set (runs in the default CI tier).
    - The test collects OUTSIDE the `pytest.mark.eval` marker (confirm: `python -m pytest tests/nl2sparql/eval/test_grounding_recall.py --collect-only -q` lists it without RUN_EVAL).
    - The measured recall printed/asserted is >= 0.90 (expected ~0.96 matching the spike).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: grounding: config-default structural test</name>
  <read_first>
    - tests/nl2sparql/eval/test_eval.py::test_corpus_path_default (lines ~32-60) — the exact "config key absent ⇒ byte-identical default" structural shape to mirror; note it is in the always-on tier (not `@pytest.mark.eval`).
    - 07.3-PATTERNS.md test_eval.py section (target test body).
  </read_first>
  <action>
    Add `test_grounding_default_absent_is_noop` directly after `test_corpus_path_default` in
    `tests/nl2sparql/eval/test_eval.py`, mirroring its dict-default discipline: a config dict
    with no `grounding:` key yields `config.get("grounding", {}) == {}` and
    `.get("k", 0) == 0` (proving the additive key is default-preserving so every existing
    config stays byte-identical). Keep it in the always-on tier (not under the eval marker) —
    it only exercises dict-default logic, no provider/pipeline.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/eval/test_eval.py -k grounding_default -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/nl2sparql/eval/test_eval.py -k grounding_default -q` exits 0 without RUN_EVAL.
    - The test asserts absent `grounding:` ⇒ `{}` and `k` default 0 (default-preserving invariant).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored CK25 Turtle → LabelIndex → (eventual) LLM prompt | The eval builder reads labels from the vendored instance graph; these become the retrieved grounding entities. |
| pyoxigraph (dev-only) → shipped packages | Packaging boundary: the builder must not leak pyoxigraph into `arango_query_core`/`arango_sparql`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-01 | Tampering / Injection | build_label_index label values | mitigate | Labels are sanitized at render time by the engine renderer (Plan 01, T-07.3-01); the builder passes them through as data. The recall test uses only the trusted vendored CK25 graph, so no adversarial label is introduced here. |
| T-07.3-03 | Tampering | packaging boundary (pyoxigraph leak) | mitigate | pyoxigraph-touching imports are function-local and the builder lives under tests/; asserted by a grep gate so `arango_query_core`/`arango_sparql` stay pyoxigraph-free at runtime (CLAUDE.md hard rule 5). |
</threat_model>

<verification>
- `python -m pytest tests/nl2sparql/eval/test_grounding_recall.py tests/nl2sparql/eval/test_eval.py -k "grounding or grounding_default or recall" -q` green with NO RUN_EVAL set.
- `grep -n "pyoxigraph" tests/nl2sparql/eval/grounding_index_builder.py` shows only function-local usage (packaging boundary held).
- `git diff --stat -- arango_sparql/translate/` empty.
</verification>

<success_criteria>
- Eval-only builder produces a non-empty LabelIndex from CK25's vendored Turtle, pyoxigraph confined to tests/.
- Deterministic recall guard runs in the default CI tier and asserts recall >= 0.90 (spike 0.96).
- Additive grounding: config key proven default-preserving.
</success_criteria>

<output>
Create `.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-04-SUMMARY.md` when done — record the measured recall number.
</output>
