---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 06
type: execute
wave: 5
depends_on: [07.3-05]
files_modified:
  - tests/nl2sparql/eval/baseline.json
  - tests/nl2sparql/eval/README.md
autonomous: false
requirements: [NL-ACC-01]

must_haves:
  truths:
    - "A human runs the credentialed live CK25 grounded-vs-freshly-run-zero sweep in the SAME session and reports aggregate pass-rates + per-case verdicts + McNemar b/c/p + bootstrap delta CI"
    - "The reported (not-gated) grounded number is folded into baseline.json via manual human review (never auto-regenerated in CI), kept as a SEPARATE CK25-grounded entry (never blended)"
    - "README documents the reproduction runbook mirroring the §7 dense-sweep discipline (grounded-vs-fresh-zero, temperature recorded, McNemar-primary)"
    - "Non-regression re-confirmed: W3C DAWG QUERY_EVAL >= 96.4% and the transpiler package untouched"
  artifacts:
    - path: "tests/nl2sparql/eval/baseline.json"
      provides: "Folded-in CK25-grounded live number (reported, not gated)"
      contains: "grounded"
    - path: "tests/nl2sparql/eval/README.md"
      provides: "CK25 grounding reproduction runbook"
      contains: "grounded"
  key_links:
    - from: "human-run live sweep"
      to: "baseline.json CK25-grounded entry"
      via: "manual human-reviewed fold-in (not CI auto-regen)"
      pattern: "grounded"
---

<objective>
Close NL-ACC-01's confirmatory evidence: a credentialed human runs the live CK25
grounded-vs-freshly-run-zero McNemar sweep (never in CI), the reported number is
manually folded into baseline.json as a separate CK25-grounded entry, the README §7 runbook
is written, and the hard non-regression gates (W3C >= 96.4%, transpiler untouched) are
re-confirmed. Follows the exact 07.2-04 / NL-FEW-02 human-run-reported-not-gated precedent.

Purpose: The accuracy proof requires a real LLM call + a human API key the agent must never
hold; scripted configs prove only plumbing (Pitfall 5). The confirmatory comparison must be
grounded-vs-a-fresh-zero-arm in the same session (never vs the already-committed baseline —
model drift is a real confound, RESEARCH Pitfall / 07-04 M2).
Output: baseline.json CK25-grounded entry, README runbook, non-regression re-confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-SPIKE-FINDINGS.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-RESEARCH.md
@tests/nl2sparql/eval/README.md
@tests/nl2sparql/eval/baseline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the live-config temperature + record provenance plan</name>
  <read_first>
    - arango_sparql/nl2sparql/client.py — OpenAICompatibleClient temperature handling (the spike used temperature=0 explicitly; the live configs may differ — RESEARCH Open Question 1 / Assumption A3; note 07-04 recorded gpt-4o-mini at 0.1 and gpt-5-family omits temperature).
    - tests/nl2sparql/eval/baseline.json — the existing BaselineConfig provenance fields (model/temperature/corpus_sha) + the phase07 sweep entry shape (aggregate-only sibling key precedent from 07-04).
    - 07.3-RESEARCH.md Open Question 1 + Code Examples reproduction recipe.
  </read_first>
  <action>
    Read OpenAICompatibleClient to determine the exact temperature the
    `openai-gpt4o-mini-ck25`/`-grounded` configs will use, and whether an override is possible.
    Record the value to be captured in baseline.json provenance (mirroring the existing
    temperature field convention). Prepare the exact human-run command string (Task 2) with the
    resolved temperature noted, so a non-reproduction of the spike's exact numbers can be
    correctly attributed (temperature mismatch vs. genuine effect) rather than misread as
    grounding failing. Do NOT invent a number — record what the client actually uses.
  </action>
  <verify>
    <automated>python -c "import inspect; from arango_sparql.nl2sparql.client import OpenAICompatibleClient; src=inspect.getsource(OpenAICompatibleClient); print('temperature' in src)"</automated>
  </verify>
  <acceptance_criteria>
    - The temperature value (or its omission) the live grounded config uses is stated explicitly in the SUMMARY, sourced from client.py (not assumed).
    - The Task 2 human-run command embeds/notes that temperature for provenance capture.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Credentialed live CK25 grounded-vs-fresh-zero McNemar sweep</name>
  <read_first>
    - 07.3-RESEARCH.md Code Examples (the exact `RUN_EVAL=1 NL2SPARQL_API_KEY=... python -c "..."` recipe: run BOTH openai-gpt4o-mini-ck25 (fresh zero) AND openai-gpt4o-mini-ck25-grounded THIS session, then paired_mcnemar + bootstrap_paired_delta).
    - tests/nl2sparql/eval/README.md §7 — the established grounded-vs-fresh-zero same-session discipline + the manual fold-in review process.
    - 07.3-SPIKE-FINDINGS.md — the target reproduction numbers (6/49 → 12/49, b=6/c=0, p=0.031; a materially different number is not automatically a failure per Open Question 1).
  </read_first>
  <what-built>
    The full grounding path (engine seam + adapter + eval wiring + configs) is in place and the
    scripted plumbing gate is green. The only remaining evidence is a real LLM measurement,
    which requires an NL2SPARQL_API_KEY the agent must never hold and live calls that never run
    in CI (07.2-04 / NL-FEW-02 precedent).
  </what-built>
  <how-to-verify>
    1. Run, in ONE session with a valid NL2SPARQL_API_KEY (never OPENAI_API_KEY — 06.2-04
       Pitfall 1), the RESEARCH Code Examples recipe: `zero = run('openai-gpt4o-mini-ck25')`
       (freshly this session), `grounded = run('openai-gpt4o-mini-ck25-grounded')`, write both
       reports, then `paired_mcnemar(zero.cases, grounded.cases)` and
       `bootstrap_paired_delta(zero.cases, grounded.cases)`.
    2. Also capture the corpus_sha: `git log -1 --format=%h -- tests/nl2sparql/eval/vendored/ck25/corpus.yml`.
    3. Paste back: zero pass_rate, grounded pass_rate, per-case verdicts for both arms, McNemar
       b/c/p, bootstrap delta + 95% CI, the temperature used (Task 1), and corpus_sha.
  </how-to-verify>
  <resume-signal>Paste the two pass-rates + per-case verdicts + McNemar b/c/p + bootstrap CI + temperature + corpus_sha (or report a null/inconclusive result for the documented-null path).</resume-signal>
  <acceptance_criteria>
    - Both arms run in the SAME session (grounded-vs-fresh-zero, never vs the committed baseline number).
    - McNemar b/c/p and bootstrap delta CI are reported with per-case verdicts.
    - The result is either a significant positive lift (p<0.05, 0 regressions) OR an explicitly-documented null/inconclusive result (following the NL-FEW-02 human-accepted-documented path) — never a fabricated or scripted number substituted for the live measurement (Pitfall 5).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fold result into baseline.json + write README runbook + non-regression re-check</name>
  <read_first>
    - tests/nl2sparql/eval/baseline.json — the 07-04 aggregate-only sibling-key precedent (phase07_dense_few_shot_sweep) for how to record a sweep whose full per-case dicts may not all be captured; keep CK25-grounded as a SEPARATE entry (never blended).
    - tests/nl2sparql/eval/README.md §5/§7 — the manual-review fold-in discipline + the sweep runbook shape to mirror for grounding.
    - tests/w3c/test_coverage_gate.py — the committed W3C >= 96.4% gate to re-run.
  </read_first>
  <action>
    Manually fold the human-reported grounded number (from Task 2) into `baseline.json` as a
    SEPARATE CK25-grounded entry/sibling key (reported, NOT gated; never blended with the zero
    baseline or other sets), including provenance (model, temperature from Task 1, corpus_sha,
    McNemar b/c/p, bootstrap delta CI). Write a README §7-style reproduction runbook for the
    CK25 grounding sweep: the grounded-vs-fresh-zero same-session command, the NL2SPARQL_API_KEY
    (not OPENAI_API_KEY) note, the temperature-provenance capture, and the manual (never
    CI-auto-regenerated) fold-in step. Re-confirm the two hard non-regression gates. If Task 2
    returned a null, record it via the documented-null path (mirroring NL-FEW-02) rather than
    claiming a passed confirmatory test.
  </action>
  <verify>
    <automated>python -c "import json; b=json.load(open('tests/nl2sparql/eval/baseline.json')); print('ck25-grounded entry present:', any('grounded' in str(k).lower() for k in (list(b.keys())+list(b.get('configs',{}).keys()))))"; python -m pytest tests/w3c/test_coverage_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - baseline.json contains a SEPARATE CK25-grounded entry (never blended) with provenance (model, temperature, corpus_sha, McNemar b/c/p, bootstrap CI).
    - README documents the grounded-vs-fresh-zero same-session runbook + manual fold-in discipline (grep "grounded" in README §7).
    - `pytest tests/w3c/test_coverage_gate.py -q` green (QUERY_EVAL >= 96.4%) OR skips key-free when the W3C corpus isn't fetched locally.
    - `git diff --stat -- arango_sparql/translate/` across ALL phase commits is empty (transpiler untouched).
    - If null: baseline.json + SUMMARY record the documented-null path, not a passed confirmatory claim.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human API key → live LLM sweep | The credentialed sweep uses a secret the agent must never hold; results are pasted back by the human. |
| reported number → committed baseline.json | A human-reviewed value enters the tracked regression artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-06 | Spoofing / Repudiation | baseline.json folded number | mitigate | The number is human-run + human-reviewed, recorded with full provenance (model, temperature, corpus_sha, McNemar b/c/p, bootstrap CI); never auto-regenerated in CI (06.2-04 Pitfall 2). A scripted/plumbing number is never substituted for the live measurement (Pitfall 5). |
| T-07.3-07 | Information Disclosure | secrets in committed artifacts | mitigate | NL2SPARQL_API_KEY is never committed; only aggregate/per-case verdicts + provenance land in baseline.json (no raw prompts/keys), matching NL-BENCH-06 / 06.2-04 discipline. |
| T-07.3-04 | Tampering | CI default (no live calls) | mitigate | The live config is human-run only; `test_ci_gate_only_ever_runs_scripted` (Plan 05) keeps scripted the sole CI-reachable config. |
</threat_model>

<verification>
- baseline.json has a separate CK25-grounded entry with full provenance (reported, not gated, never blended).
- README §7 runbook documents grounded-vs-fresh-zero same-session sweep + manual fold-in.
- `pytest tests/w3c/test_coverage_gate.py -q` green/skips; W3C QUERY_EVAL >= 96.4%.
- `git diff --stat -- arango_sparql/translate/` empty across all phase commits.
</verification>

<success_criteria>
- Confirmatory CK25 grounded-vs-fresh-zero McNemar result (significant lift OR documented null) recorded with provenance, human-run, never in CI.
- README runbook + non-regression gates re-confirmed; transpiler untouched; scripted stays CI default.
</success_criteria>

<output>
Create `.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-06-SUMMARY.md` when done — record the McNemar b/c/p, both pass-rates, temperature, corpus_sha, and whether NL-ACC-01 closed via a significant lift or the documented-null path.
</output>
