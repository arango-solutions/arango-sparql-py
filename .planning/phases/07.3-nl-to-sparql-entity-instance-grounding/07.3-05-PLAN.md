---
phase: 07.3-nl-to-sparql-entity-instance-grounding
plan: 05
type: execute
wave: 4
depends_on: [07.3-03, 07.3-04]
files_modified:
  - tests/nl2sparql/eval/runner.py
  - tests/nl2sparql/eval/configs.yml
autonomous: true
requirements: [NL-ACC-01]

must_haves:
  truths:
    - "runner.py::run() reads an additive grounding: config key, builds the LabelIndex ONCE per run() from data_ttl (never per-case), and threads grounding_k/grounding_index into every case's NlPipeline"
    - "The global default label_predicates is ['rdfs:label']; pv:name appears ONLY in CK25's own config entry (never hardcoded in code)"
    - "New scripted-ck25-grounded + openai-gpt4o-mini-ck25-grounded configs exist; scripted-ck25-grounded is a green plumbing gate under RUN_EVAL"
    - "Every pre-existing config's behavior stays byte-identical (absent grounding: == no-op); scripted stays the CI default (no live calls in CI)"
  artifacts:
    - path: "tests/nl2sparql/eval/runner.py"
      provides: "Additive grounding: read + build-once + NlPipeline passthrough"
      contains: "grounding_index"
    - path: "tests/nl2sparql/eval/configs.yml"
      provides: "scripted-ck25-grounded + openai-gpt4o-mini-ck25-grounded entries"
      contains: "ck25-grounded"
  key_links:
    - from: "runner.py::run()"
      to: "build_label_index(data_ttl, label_predicates) then NlPipeline(grounding_index=...)"
      via: "build once outside the per-case loop, pass same object per case"
      pattern: "build_label_index"
    - from: "configs.yml scripted-ck25-grounded"
      to: "grounding: {k: 20, label_predicates: [...]}"
      via: "additive config block mirroring few_shot:"
      pattern: "grounding:"
---

<objective>
Wire the additive `grounding:` config path through the eval harness: `runner.py::run()`
reads the config key, builds the LabelIndex once per run from the corpus `data_ttl`, and
threads it into each case's NlPipeline; `configs.yml` gains the CK25-grounded config entries.
This is the integration point that makes NL-ACC-01's grounded eval runnable end-to-end. It
depends on both Plan 03 (NlPipeline grounding param) and Plan 04 (build_label_index).

Purpose: Mirror the already-shipped `few_shot:` additive-config precedent exactly so every
existing config stays byte-identical and scripted remains the no-network CI default. Keep the
global label-predicate default schema-agnostic (`rdfs:label`) so grounding transfers to CDF;
CK25's dataset-specific `pv:name` lives only in its own config entry.
Output: runner.py grounding wiring (build-once), configs.yml new entries, and a green
`scripted-ck25-grounded` plumbing gate under RUN_EVAL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-RESEARCH.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runner.py additive grounding: read + build-once + passthrough</name>
  <read_first>
    - tests/nl2sparql/eval/runner.py — the `data_path`/`data_ttl` read (lines ~504-514), the `few_shot_cfg`/`few_shot_mode`/`few_shot_k` additive block + build-once index (lines ~516-538 incl. the "Build the index ONCE per arm, outside the per-case loop (Pitfall 1)" comment), and the per-case `NlPipeline(...)` construction (lines ~540-552). Imports at line ~24.
    - tests/nl2sparql/eval/grounding_index_builder.py (Plan 04) — the build_label_index signature to import function-locally.
    - 07.3-RESEARCH.md Pattern 3 (additive grounding read code) + 07.3-PATTERNS.md runner.py section (build BEFORE the per-case loop, guard on data_ttl, default label_predicates=["rdfs:label"]).
  </read_first>
  <action>
    In `runner.py::run()`, after the existing few_shot build-once block and before the `cases:
    list[CaseResult] = []` loop, add an additive read: `grounding_cfg = config.get("grounding",
    {})`, `grounding_k = grounding_cfg.get("k", 0)`, `grounding_index = None`; if
    `grounding_cfg and data_ttl:` set `label_predicates =
    grounding_cfg.get("label_predicates", ["rdfs:label"])` and
    `grounding_index = build_label_index(data_ttl, label_predicates)` (build ONCE here, never
    per-case — Pitfall 3). Import build_label_index function-locally (mirroring the existing
    lazy `from tests.helpers.oxi import ...` pattern) so pyoxigraph stays off runner's module
    import path. Extend the per-case `NlPipeline(...)` call with `grounding_k=grounding_k,
    grounding_index=grounding_index` (mirroring the few_shot args). The global default MUST be
    `["rdfs:label"]`; do NOT hardcode `pv:name` anywhere in runner.py (Pitfall 2). Keep
    `run(config_name) -> Report` signature + Report shape byte-identical; absent `grounding:`
    == today's behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/nl2sparql/eval/test_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pv:name" tests/nl2sparql/eval/runner.py` returns 0 (dataset-specific predicate never in code).
    - The build_label_index call appears BEFORE the `for case in corpus["cases"]:` loop (grep line order), proving build-once (Pitfall 3).
    - `python -m pytest tests/nl2sparql/eval/test_eval.py -q` green — every existing config byte-identical.
    - `RUN_EVAL=1 pytest tests/nl2sparql/eval/test_eval.py -k ci_gate_only_ever_runs_scripted -q` still green (CI default unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: configs.yml CK25-grounded entries + scripted plumbing gate</name>
  <read_first>
    - tests/nl2sparql/eval/configs.yml — the existing `scripted-ck25` / `openai-gpt4o-mini-ck25` entries (lines ~56-82) and the `few_shot:` block shape under the dense/bm25 entries (lines ~92-110); the top-of-file comment block (lines ~1-17).
    - 07.3-PATTERNS.md configs.yml section (place new entries beside the ck25 entries, NOT in the few-shot matrix; pv:name only here).
    - 07.3-RESEARCH.md Pitfall 5 (scripted gate is a plumbing gate, NOT accuracy evidence).
  </read_first>
  <action>
    Add two entries to `configs.yml` directly after the existing ck25 entries:
    `scripted-ck25-grounded` (provider scripted, judge execution, max_repairs 2, corpus
    vendored/ck25/corpus.yml, `grounding: {k: 20, label_predicates: ["rdfs:label", "pv:name"]}`)
    and `openai-gpt4o-mini-ck25-grounded` (same but provider openai/gpt-4o-mini). `pv:name`
    appears ONLY in these `label_predicates:` lists (declarative config, reviewable), never in
    code. Update the top-of-file comment to document the `grounding:` key the same way
    `few_shot:` is documented. Confirm `scripted-ck25-grounded` runs green as a plumbing gate
    (config → build_label_index → seam → prompt → judge does not crash) — this proves plumbing,
    NOT accuracy (Pitfall 5).
  </action>
  <verify>
    <automated>python -c "import yaml,io; c=yaml.safe_load(open('tests/nl2sparql/eval/configs.yml')); assert 'scripted-ck25-grounded' in c and 'openai-gpt4o-mini-ck25-grounded' in c and c['scripted-ck25-grounded']['grounding']['label_predicates']==['rdfs:label','pv:name']; print('configs ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripted-ck25-grounded` and `openai-gpt4o-mini-ck25-grounded` exist in configs.yml with `grounding: {k: 20, label_predicates: ["rdfs:label","pv:name"]}`.
    - `RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-ck25-grounded'); print('scripted-ck25-grounded pass_rate', r.pass_rate)"` completes without error (plumbing gate green; treated as plumbing evidence only, NOT accuracy — Pitfall 5).
    - `RUN_EVAL=1 pytest tests/nl2sparql/eval/test_eval.py -k ci_gate_only_ever_runs_scripted -q` green (no new live config reachable in CI).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configs.yml label_predicates → build_label_index → prompt | Declarative config controls which predicates seed the grounding index. |
| CK25 instance graph → per-run LabelIndex → LLM prompt | Retrieved labels are injected into the system prompt for the grounded configs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-01 | Tampering / Injection | grounded config → retrieved labels → prompt | mitigate | Engine renderer sanitizes labels (Plan 01, T-07.3-01); the recall/plumbing path only uses the trusted vendored CK25 graph. Downstream rdflib validate + SchemaResolver still gate AQL. |
| T-07.3-04 | Tampering | CI default drift (live calls in CI) | mitigate | `test_ci_gate_only_ever_runs_scripted` stays green — the new live `openai-gpt4o-mini-ck25-grounded` config is never reachable from CI; scripted-only remains the enforced CI default. |
| T-07.3-05 | Information Disclosure | dataset-specific vocabulary hardcoded in code | mitigate | `pv:name` lives only in the CK25 config entry's `label_predicates:` (declarative), never in runner.py (grep gate == 0), so the mechanism stays schema-agnostic and transfers to CDF unchanged. |
</threat_model>

<verification>
- `python -m pytest tests/nl2sparql/eval/test_eval.py -q` green (existing configs byte-identical).
- `RUN_EVAL=1 pytest -m eval -q` green (scripted gate, offline) + `RUN_EVAL=1 pytest tests/nl2sparql/eval/test_eval.py -k ci_gate_only_ever_runs_scripted -q` green.
- `grep -c "pv:name" tests/nl2sparql/eval/runner.py` == 0.
- `git diff --stat -- arango_sparql/translate/` empty.
</verification>

<success_criteria>
- runner.py reads grounding: additively, builds the index once per run, threads it per case; default label_predicates=["rdfs:label"].
- configs.yml has the two CK25-grounded entries with pv:name confined to config.
- scripted-ck25-grounded green (plumbing); every existing config unchanged; scripted stays CI default.
</success_criteria>

<output>
Create `.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-05-SUMMARY.md` when done.
</output>
