---
phase: 04-interoperability-performance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/perf/__init__.py
  - tests/perf/conftest.py
  - tests/fixtures/cosmic_coffee.rdf
  - tests/fixtures/cosmic_coffee.NOTICE.md
  - docs/howto/index.md
autonomous: true
requirements: [REQ-performance-slos, REQ-thirdparty-tool-compat]
must_haves:
  truths:
    - "pytest recognises the `perf` marker without an unregistered-marker warning"
    - "SPARQLWrapper is an installable dev dependency"
    - "A perf test can import the fake ArangoDB double and a p95 helper from tests/perf/conftest.py"
    - "tests/fixtures/cosmic_coffee.rdf is vendored with a provenance NOTICE"
    - "docs/howto/ exists as a directory with an index"
  artifacts:
    - path: "pyproject.toml"
      provides: "perf marker registration + SPARQLWrapper dev dep"
      contains: "perf:"
    - path: "tests/perf/conftest.py"
      provides: "reused _FakeArangoClient double + p95/report/baseline helpers"
      min_lines: 60
    - path: "tests/perf/__init__.py"
      provides: "package marker for tests/perf"
    - path: "tests/fixtures/cosmic_coffee.rdf"
      provides: "MIT-licensed RDF/XML roundtrip fixture (349 triples)"
    - path: "tests/fixtures/cosmic_coffee.NOTICE.md"
      provides: "vendored-fixture provenance (source URL, commit SHA, MIT license)"
    - path: "docs/howto/index.md"
      provides: "docs/howto directory anchor + recipe index"
  key_links:
    - from: "tests/perf/conftest.py"
      to: "tests/test_service_sparql_routes.py"
      via: "reuse of _FakeArangoClient / fake_client_factory"
      pattern: "_FakeArangoClient"
    - from: "pyproject.toml"
      to: "SPARQLWrapper"
      via: "dev optional-dependency"
      pattern: "SPARQLWrapper"
---

<objective>
Land the Wave-0 foundation every downstream Phase-4 harness depends on: register the
`perf` pytest marker and add `SPARQLWrapper` as a dev dependency, scaffold the
`tests/perf/` package (reusing the proven `_FakeArangoClient` double plus a stdlib p95
helper), vendor the MIT-licensed `cosmic_coffee.rdf` Ontology-Playground fixture with a
provenance NOTICE, and create the `docs/howto/` directory with an index.

Purpose: The perf suite (Plans 06/07), the SPARQLWrapper smoke test (Plan 05), the
Ontology-Playground roundtrip (Plan 05), and the documented recipes (Plan 08) are all
blocked until these scaffolding pieces exist. This plan does the from-scratch setup so
those plans only write test/doc bodies.
Output: `pyproject.toml` amendment, `tests/perf/{__init__,conftest}.py`,
`tests/fixtures/cosmic_coffee.rdf` + NOTICE, `docs/howto/index.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the perf marker and add SPARQLWrapper dev dependency</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the file being modified; `[tool.pytest.ini_options] markers` at line 97, `[project.optional-dependencies].dev` at line 77 — the exact analog blocks)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (the `pyproject.toml (config) — MODIFY` section: exact marker string + dep-comment style)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pitfall 4: do NOT add `legacy_roundtrip` or `bench` markers)
  </read_first>
  <action>
    Append exactly one new marker to the `markers` list in `[tool.pytest.ini_options]`:
    `"perf: performance budget enforcement (see PRD §9.4); CI-blocking on >25% p95 regression for the fast in-process rows"`.
    Do NOT add `legacy_roundtrip` (Foxx harness retired per D-01/D-02) or `bench` (out of
    scope — translator-only gauge). Add `"SPARQLWrapper>=2.0.0"` to the `dev` list under
    `[project.optional-dependencies]`, preceded by a comment mirroring the existing
    `pyoxigraph` comment style noting it is the exact client PRD §11.1 names as the
    automated third-party-tool compat target (D-06). Run `uv lock` to refresh the lockfile
    after adding the dependency (per CLAUDE.md off-limits rule on new deps).
  </action>
  <verify>
    <automated>python -c "import tomllib; m=tomllib.load(open('pyproject.toml','rb'))['tool']['pytest']['ini_options']['markers']; assert any(x.startswith('perf:') for x in m), 'perf marker missing'; assert not any(x.startswith('legacy_roundtrip') or x.startswith('bench') for x in m), 'forbidden marker added'; d=tomllib.load(open('pyproject.toml','rb'))['project']['optional-dependencies']['dev']; assert any('SPARQLWrapper' in x for x in d), 'SPARQLWrapper dep missing'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `markers` list contains an entry beginning `perf:` and does NOT contain `legacy_roundtrip` or `bench`.
    - `pyproject.toml` `dev` optional-dependencies contains `SPARQLWrapper>=2.0.0`.
    - `pytest -m perf --collect-only -q` emits no `PytestUnknownMarkWarning`.
    - `uv.lock` updated (contains a `sparqlwrapper` entry).
  </acceptance_criteria>
  <done>Marker registered, dev dep added, lockfile refreshed, no unregistered-marker warning.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/perf package with reused fake double and stdlib helpers</name>
  <files>tests/perf/__init__.py, tests/perf/conftest.py</files>
  <read_first>
    - tests/test_service_sparql_routes.py (lines 96-243: `_FakeCursor`, `_FakeDb`, `_FakeArangoClient`, `fake_client_factory`, `_connect_session` — the double + connect helper to reuse; and line 242 `_connect_session`)
    - tests/cross/__init__.py (the empty-package-init analog)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/perf/conftest.py` section: the verbatim double excerpt + `statistics.quantiles(samples, n=100)[93]` p95 helper)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pattern 3: the in-process perf harness reusing the fake double)
  </read_first>
  <action>
    Create empty `tests/perf/__init__.py` (package marker, mirror `tests/cross/__init__.py`).
    Create `tests/perf/conftest.py` that makes the CI-gated perf rows dependency-free:
    re-expose the `_FakeArangoClient`/`_FakeDb`/`_FakeCursor` double and `fake_client_factory`
    fixture by importing them from `tests.test_service_sparql_routes` if that import path
    resolves cleanly, else copy the ~140-line block verbatim (per PATTERNS.md guidance).
    Also provide three stdlib-only helper functions: `p95(samples: list[float]) -> float`
    returning `statistics.quantiles(sorted_samples, n=100)[93]`; `load_baseline() -> dict`
    reading `tests/perf/baseline.json` (returns `{}` if absent so a first-run capture does
    not crash); and `append_report(row_name: str, p95_ms: float, budget_ms: float | None)`
    appending a Markdown table row to `tests/perf/LATENCY_REPORT.md`. Add a `_connect_session`
    helper mirroring `tests/test_service_sparql_routes.py:242` so perf tests can obtain a
    bearer token. No `pytest-benchmark`, no numpy/scipy (stdlib `statistics` only).
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('tests/perf/conftest.py').read(); t=ast.parse(src); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,))}; assert {'p95','load_baseline','append_report'} <= names, names; assert 'pytest_benchmark' not in src and 'import numpy' not in src and 'import scipy' not in src; print('ok')" && python -c "import ast; open('tests/perf/__init__.py')" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/perf/__init__.py` exists and is a valid (empty) Python module.
    - `tests/perf/conftest.py` defines functions `p95`, `load_baseline`, `append_report` and exposes `fake_client_factory` (imported or copied) plus a `_connect_session` helper.
    - `tests/perf/conftest.py` contains no `pytest_benchmark`, `numpy`, or `scipy` import.
    - `p95` is implemented via `statistics.quantiles(..., n=100)[93]`.
    - `pytest tests/perf --collect-only -q` succeeds (no collection error).
  </acceptance_criteria>
  <done>tests/perf is an importable package whose conftest supplies the fake double + p95/report/baseline helpers with zero new third-party dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor cosmic_coffee.rdf with NOTICE and create docs/howto index</name>
  <files>tests/fixtures/cosmic_coffee.rdf, tests/fixtures/cosmic_coffee.NOTICE.md, docs/howto/index.md</files>
  <read_first>
    - tests/nl2sparql/eval/vendored/ck25/NOTICE.md (the provenance-notice analog: Title / Source / Commit / License / Files vendored / Downstream use sections)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/fixtures/cosmic_coffee.rdf + provenance note` section — the NOTICE template and the supply-chain pin-to-SHA requirement)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Code Examples: the exact raw.githubusercontent.com fetch URL; Known Threat Patterns: pin to a commit SHA, do not fetch live at test time)
  </read_first>
  <action>
    Fetch `cosmic-coffee.rdf` from
    `https://raw.githubusercontent.com/microsoft/Ontology-Playground/main/catalogue/official/cosmic-coffee/cosmic-coffee.rdf`
    and save it verbatim to `tests/fixtures/cosmic_coffee.rdf`. Capture the exact commit SHA
    that `main` resolves to at fetch time (via the GitHub API `commits` endpoint for that
    path) and record it in `tests/fixtures/cosmic_coffee.NOTICE.md` using the ck25 NOTICE
    section structure: Title, Source (github.com/microsoft/Ontology-Playground), Commit
    (pinned SHA), License (MIT — verified via GitHub API `license.spdx_id`), Files vendored
    (`cosmic_coffee.rdf`, byte count + triple count), Downstream use
    (`tests/integration/test_ontology_playground_roundtrip.py` roundtrips it via
    `/mapping/import-owl` → `/mapping/export-owl`, D-06). Verify the file parses as RDF/XML
    with rdflib and record the parsed triple count. Create `docs/howto/index.md` as the
    directory anchor: a short intro plus a table listing the planned recipes (protege.md,
    yasgui.md, arq.md, sparqlwrapper.md, ontology-playground.md) with a one-line purpose each,
    referencing PRD §11.2–§11.4.
  </action>
  <verify>
    <automated>python -c "from rdflib import Graph; g=Graph(); g.parse('tests/fixtures/cosmic_coffee.rdf', format='xml'); n=len(g); assert n>300, n; print('triples',n)" && python -c "import os,re; s=open('tests/fixtures/cosmic_coffee.NOTICE.md').read(); assert 'MIT' in s and re.search(r'[0-9a-f]{7,40}', s), 'NOTICE missing license or commit SHA'; assert os.path.exists('docs/howto/index.md'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/cosmic_coffee.rdf` parses under rdflib `format="xml"` and yields > 300 triples.
    - `tests/fixtures/cosmic_coffee.NOTICE.md` names the source repo, a pinned commit SHA (7–40 hex chars), and `MIT` license.
    - `docs/howto/index.md` exists and lists the five planned recipe files.
    - The fixture is a static checked-in file (no test fetches it live from GitHub).
  </acceptance_criteria>
  <done>Fixture vendored with pinned-SHA provenance, RDF/XML-parseable; docs/howto directory anchored with an index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub → repo | vendored `cosmic_coffee.rdf` crosses from an external repo into our test corpus |
| dev tooling → CI | new `SPARQLWrapper` dep enters the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | vendored `cosmic_coffee.rdf` fetched from GitHub | mitigate | Pin the exact commit SHA in `cosmic_coffee.NOTICE.md`; check the file in statically (never fetch live at test time); verify it parses to the expected triple count before committing. |
| T-04-02 | Tampering | `SPARQLWrapper` supply chain | mitigate | RESEARCH.md Package Legitimacy Audit dispositions `SPARQLWrapper` as Approved (`slopcheck OK`, RDFLib-org, `github.com/RDFLib/sparqlwrapper`); pin `>=2.0.0`; `uv lock` records the resolved hash. |
| T-04-SC | Tampering | pip/uv install of SPARQLWrapper | mitigate | Package is `[CITED]` (official docs + RDFLib GitHub) — not `[ASSUMED]`/`[SUS]`; no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest -m perf --collect-only -q` emits no unregistered-marker warning.
- `pytest tests/perf --collect-only -q` collects cleanly.
- `tests/fixtures/cosmic_coffee.rdf` parses as RDF/XML; NOTICE pins a SHA + MIT.
- Non-regression: `pytest -m "not integration and not w3c and not eval and not perf" -q` stays green (this plan adds no import-time cost to the default suite).
</verification>

<success_criteria>
- `perf` marker registered; `SPARQLWrapper>=2.0.0` in dev deps; `uv.lock` refreshed.
- `tests/perf/` is an importable package with fake-double + p95/report/baseline helpers.
- `cosmic_coffee.rdf` vendored with pinned-SHA MIT NOTICE.
- `docs/howto/index.md` created.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-01-SUMMARY.md` when done.
</output>
