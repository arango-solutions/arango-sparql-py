---
phase: 04-interoperability-performance-verification
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - tests/integration/test_aoe_roundtrip.py
autonomous: true
requirements: [REQ-ontoextract-integration]
must_haves:
  truths:
    - "A phys:-annotated OWL ontology round-trips /mapping/export-owl → /mapping/import-owl with triple-bag equality for Turtle AND RDF/XML"
    - "After SchemaCache.put activation, an ASK via /sparql returns a boolean and a SELECT returns well-formed bindings against the docker-compose ArangoDB"
    - "The test skip-gates cleanly when Docker/ArangoDB is unavailable"
  artifacts:
    - path: "tests/integration/test_aoe_roundtrip.py"
      provides: "AOE own-half contract test (import/export isomorphism + ASK/SELECT via /sparql)"
      min_lines: 80
      contains: "isomorphic"
  key_links:
    - from: "tests/integration/test_aoe_roundtrip.py"
      to: "arango_sparql/service/routes/mapping.py"
      via: "POST /mapping/export-owl and /mapping/import-owl (Turtle + RDF/XML)"
      pattern: "mapping/import-owl"
    - from: "tests/integration/test_aoe_roundtrip.py"
      to: "arango_sparql.schema.cache.SchemaCache.put"
      via: "deterministic schema activation for /sparql"
      pattern: "put\\("
    - from: "tests/integration/test_aoe_roundtrip.py"
      to: "tests.integration.conftest"
      via: "shared Docker boot/skip helpers"
      pattern: "try_boot_arangodb_via_compose"
---

<objective>
Prove REQ-ontoextract-integration by testing OUR half of the AOE contract only (D-03/D-04):
`POST /mapping/export-owl` → `POST /mapping/import-owl` triple-bag equality (modulo blank-node
renaming via `rdflib.Graph.isomorphic()`) for Turtle AND RDF/XML, then `SchemaCache.put()` the
imported bundle and run ASK/SELECT via `/sparql` against the docker-compose ArangoDB (host
8532, DB `sparql-to-aql`). NO external `arango-ontoextract` service is cloned or run.

Purpose: AOE's own integration is "one env var" (§12.2); the substance worth testing is our
endpoints' OWL fidelity + queryability. RDF/XML rows depend on Plan 02's format fix.
Output: `tests/integration/test_aoe_roundtrip.py` (Docker-gated, `integration` marker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@.planning/phases/04-interoperability-performance-verification/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turtle + RDF/XML export→import triple-bag isomorphism</name>
  <files>tests/integration/test_aoe_roundtrip.py</files>
  <read_first>
    - tests/integration/test_execute_endpoint.py (the exact analog: `_PERSON_ONTOLOGY_TTL` phys:-annotated fixture at lines 55-73, `_seeded_collection` at 132-170, `_connect_session` at 173-191)
    - tests/integration/conftest.py (shared boot/skip helpers to import: `ensure_test_database`, `integration_enabled`, `arangodb_reachable`, `try_boot_arangodb_via_compose`, `DEFAULT_ARANGO_*` constants at lines 29-129)
    - arango_sparql/service/routes/mapping.py (the `/mapping/import-owl` at line 222 and `/mapping/export-owl` at line 329 route contract, incl. the RDF/XML negotiation added by Plan 02)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/integration/test_aoe_roundtrip.py` section: import/export call shape + `rdflib.Graph.isomorphic()` sketch; Pitfall 3 — keep this fixture separate from cosmic_coffee.rdf)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pattern 1 isomorphism; Pattern 2 SchemaCache.put; Open Question 1 seed-data recommendation)
  </read_first>
  <action>
    Create `tests/integration/test_aoe_roundtrip.py` with `pytestmark = pytest.mark.integration`.
    Import the shared boot/skip helpers from `tests.integration.conftest` (do NOT re-copy the
    private helpers). Define a `phys:collectionName`-annotated Turtle fixture by EXTENDING the
    `_PERSON_ONTOLOGY_TTL` shape from `test_execute_endpoint.py` (this is required so the
    imported mapping is queryable at /sparql — do NOT reuse cosmic_coffee.rdf, per Pitfall 3).
    Write a test that: POSTs the fixture to `/mapping/import-owl` (Turtle), gets the bundle,
    POSTs it to `/mapping/export-owl` with `Accept: text/turtle`, and asserts
    `rdflib.Graph.isomorphic()` between the original and re-exported Turtle. Repeat the same
    export→import→re-export cycle with `Accept: application/rdf+xml` / `Content-Type:
    application/rdf+xml`, parsing both sides with rdflib `format="xml"` and asserting
    isomorphism (this row is what Plan 02 unblocks). Skip cleanly when Docker/ArangoDB is
    unavailable via the shared skip helpers.
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_aoe_roundtrip.py -m integration -q 2>&1 | tail -5; RUN_INTEGRATION=1 python -m pytest tests/integration/test_aoe_roundtrip.py -m integration -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_aoe_roundtrip.py` carries `pytest.mark.integration` and imports boot/skip helpers from `tests.integration.conftest`.
    - The fixture contains `phys:collectionName` annotations (extends `_PERSON_ONTOLOGY_TTL`, not cosmic_coffee.rdf).
    - Both a Turtle and an RDF/XML export→import→re-export isomorphism assertion exist (`rdflib.Graph.isomorphic()`).
    - `python -m pytest tests/integration/test_aoe_roundtrip.py -m integration -q` skips cleanly with no Docker (no error/fail).
    - `RUN_INTEGRATION=1 pytest tests/integration/test_aoe_roundtrip.py -m integration -q` exits 0 when ArangoDB is reachable.
  </acceptance_criteria>
  <done>Turtle and RDF/XML OWL round-trips are proven isomorphic through our own /mapping routes, Docker-gated.</done>
</task>

<task type="auto">
  <name>Task 2: SchemaCache activation + ASK/SELECT via /sparql</name>
  <files>tests/integration/test_aoe_roundtrip.py</files>
  <read_first>
    - tests/integration/test_aoe_roundtrip.py (the file from Task 1 — extend it)
    - arango_sparql/schema/cache.py (`SchemaCache` at line 111, `.put()` at line 207 — the deterministic activation API)
    - arango_sparql/service/routes/schema.py (`_resolve_schema_cache()` at line 100 — the process-wide cache /sparql reads from)
    - tests/integration/test_execute_endpoint.py (`_seeded_collection` fixture 132-170 and `_connect_session` 173-191 to reuse for a non-empty SELECT + a true/false ASK)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pattern 2: `_resolve_schema_cache().put(db_name, bundle)` before /connect + /sparql; Open Question 1: seed 1-3 docs so ASK returns both true and false and SELECT returns a non-empty binding)
  </read_first>
  <action>
    Extend the test to make the imported mapping queryable: after import, call
    `_resolve_schema_cache().put(db_name, bundle)` with `db_name = DEFAULT_ARANGO_DB` so
    `/sparql` sees the schema deterministically (import-owl is stateless — RESEARCH.md
    Pattern 2). Seed 1-3 documents into the mapped collection using the `_seeded_collection`
    pattern from `test_execute_endpoint.py`. POST `/connect`, then POST `/sparql` with an ASK
    query (assert the JSON result carries a `boolean` key of type bool — expect at least one
    `true` given the seed and one `false` for a non-existent pattern) and a SELECT query
    (assert 200 + a well-formed SPARQL-Results-JSON `results.bindings` list with the seeded
    row present). Tear down the seeded collection. Keep the whole test Docker-gated.
  </action>
  <verify>
    <automated>RUN_INTEGRATION=1 python -m pytest tests/integration/test_aoe_roundtrip.py -m integration -q 2>&1 | tail -5; python -c "s=open('tests/integration/test_aoe_roundtrip.py').read(); assert 'put(' in s and '/sparql' in s and 'ASK' in s and 'SELECT' in s.upper(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The test calls `SchemaCache.put`/`_resolve_schema_cache().put(...)` before querying `/sparql`.
    - An ASK query asserts a `boolean`-typed result; a SELECT query asserts a non-empty `results.bindings` against seeded data.
    - `RUN_INTEGRATION=1 pytest tests/integration/test_aoe_roundtrip.py -m integration -q` exits 0 with ArangoDB reachable.
    - Seeded collection is dropped in teardown (no cross-test residue).
  </acceptance_criteria>
  <done>The imported OWL mapping is queryable via /sparql — ASK returns booleans and SELECT returns seeded bindings — proving the full AOE own-half contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → docker-compose ArangoDB | test drives real /connect + /sparql against a live DB on host 8532, DB `sparql-to-aql` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | test writes to a shared ArangoDB database | mitigate | Use DB `sparql-to-aql` (never `_system`, enforced by conftest); seed into a dedicated collection dropped in teardown; reuse the proven `_seeded_collection` isolation pattern. |
| T-04-08 | Denial of Service | oversized OWL import during the roundtrip | accept | The `OwlBombError` triple cap (Plan 02) already guards import; this contract test uses a small fixture well under the cap. |
</threat_model>

<verification>
- `RUN_INTEGRATION=1 pytest tests/integration/test_aoe_roundtrip.py -m integration -q` green with ArangoDB up; skips cleanly without Docker.
- Turtle AND RDF/XML isomorphism assertions present; ASK/SELECT via /sparql resolve against seeded data.
- Non-regression: default suite `pytest -m "not integration and not w3c and not eval and not perf" -q` unaffected.
</verification>

<success_criteria>
- REQ-ontoextract-integration proven: our /mapping import/export is round-trip-faithful (Turtle + RDF/XML) and the imported mapping is queryable via /sparql — with no external AOE service.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-04-SUMMARY.md` when done.
</output>
