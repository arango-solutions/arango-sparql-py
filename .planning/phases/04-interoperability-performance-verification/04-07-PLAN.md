---
phase: 04-interoperability-performance-verification
plan: 07
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/perf/test_sparql_protocol_latency.py
  - tests/perf/test_schema_introspect_latency.py
  - tests/perf/test_nl_latency.py
  - tests/perf/test_memory_idle.py
  - tests/perf/test_memory_load.py
  - tests/perf/test_concurrency.py
  - tests/perf/test_first_byte.py
  - tests/perf/LATENCY_REPORT.md
autonomous: false
requirements: [REQ-performance-slos]
must_haves:
  truths:
    - "The 8 report-only §9.4 rows measure their metric and append to LATENCY_REPORT.md but NEVER fail CI"
    - "The /nl-translate row skip-gates on a live LLM key (NL2SPARQL_API_KEY), never committing a secret"
    - "The Docker-dependent rows skip-gate cleanly when ArangoDB is unavailable"
    - "A checked-in LATENCY_REPORT.md holds the human-reviewed measured numbers"
  artifacts:
    - path: "tests/perf/test_sparql_protocol_latency.py"
      provides: "/sparql GET latency row (report-only)"
    - path: "tests/perf/test_nl_latency.py"
      provides: "/nl-translate live-LLM latency row (key-gated, report-only)"
    - path: "tests/perf/LATENCY_REPORT.md"
      provides: "checked-in advisory latency artifact for the 8 report-only rows"
      contains: "p95"
  key_links:
    - from: "tests/perf/test_*.py (report rows)"
      to: "tests/perf/conftest.py append_report"
      via: "non-gating report append"
      pattern: "append_report"
    - from: "tests/perf/test_nl_latency.py"
      to: "NL2SPARQL_API_KEY"
      via: "skip-gate when the key is absent"
      pattern: "NL2SPARQL_API_KEY"
---

<objective>
Deliver the REPORT-NOT-GATE tier of REQ-performance-slos (D-09): the 8 noisy/Docker/LLM §9.4
rows — `/sparql` GET, `/schema/introspect`, `/nl-translate` (live LLM), memory idle, memory
load, concurrency, first-byte — run as a local/on-demand suite that measures each metric and
appends to a checked-in `LATENCY_REPORT.md`, but NEVER gates CI (§9.4's ">25% fails" becomes
advisory for these rows). The live `/nl-translate` row is key-gated and Docker rows skip-gate
cleanly. A human runs the suite (Docker + optional key) and reviews the committed report.

Purpose: These rows need Docker and/or a human-held LLM key, so they cannot live on the per-PR
path. Uses Plan 01's perf scaffolding (`append_report` helper).
Output: 7 report-only test files + `tests/perf/LATENCY_REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@.planning/phases/04-interoperability-performance-verification/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 report-only perf rows (non-gating)</name>
  <files>tests/perf/test_sparql_protocol_latency.py, tests/perf/test_schema_introspect_latency.py, tests/perf/test_nl_latency.py, tests/perf/test_memory_idle.py, tests/perf/test_memory_load.py, tests/perf/test_concurrency.py, tests/perf/test_first_byte.py</files>
  <read_first>
    - tests/perf/conftest.py (Plan 01: `p95`, `append_report`, `_connect_session`, boot/skip access — the helpers every report row uses)
    - tests/integration/conftest.py (shared `try_boot_arangodb_via_compose`, `integration_enabled`, `arangodb_reachable` for the Docker-gated rows)
    - tests/nl2sparql/eval/README.md (the established `NL2SPARQL_API_KEY` key-gating + scripted-default convention the `/nl-translate` row must follow)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/perf/LATENCY_REPORT.md` role-match note; the report rows reuse the perf harness + `append_report`)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Architecture Diagram §E; D-09 row list; §9.4 the 11-row SLO table for the row names/budgets)
  </read_first>
  <action>
    Create the 7 files, each `pytestmark = pytest.mark.perf` and each NON-GATING: measure the
    metric and call `append_report(row_name, measured_p95_ms, budget_ms)` but do NOT `assert`
    against the budget (advisory only, per D-09 — a `budget_ms` may be recorded for reference).
    `test_sparql_protocol_latency.py`: `/sparql` GET latency (Docker-gated). 
    `test_schema_introspect_latency.py`: `/schema/introspect` latency (Docker-gated).
    `test_nl_latency.py`: `/nl-translate` live-LLM latency — skip via
    `pytest.mark.skipif`/`os.environ.get("NL2SPARQL_API_KEY")` when the key is absent (never
    hardcode or log the key; scripted/no-network stays the default). `test_memory_idle.py` /
    `test_memory_load.py`: RSS via stdlib `resource.getrusage` (idle vs under load).
    `test_concurrency.py`: throughput/latency under N concurrent requests. `test_first_byte.py`:
    time-to-first-byte. All Docker-dependent rows skip-gate cleanly through the shared helpers.
    Reuse the perf `p95` helper; no `pytest-benchmark`/numpy/scipy.
  </action>
  <verify>
    <automated>python -m pytest tests/perf -m perf -k "not translate_latency and not execute_overhead" -q 2>&1 | tail -8 && python -c "import glob; files=['tests/perf/test_sparql_protocol_latency.py','tests/perf/test_schema_introspect_latency.py','tests/perf/test_nl_latency.py','tests/perf/test_memory_idle.py','tests/perf/test_memory_load.py','tests/perf/test_concurrency.py','tests/perf/test_first_byte.py']; import os; assert all(os.path.exists(f) for f in files), 'missing report row files'; nl=open('tests/perf/test_nl_latency.py').read(); assert 'NL2SPARQL_API_KEY' in nl, 'nl row not key-gated'; assert all('append_report' in open(f).read() for f in files if 'memory' not in f) or True; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist, carry `pytest.mark.perf`, and NONE asserts a pass/fail against its §9.4 budget (report-only).
    - Each row calls `append_report(...)` to write into `LATENCY_REPORT.md`.
    - `test_nl_latency.py` skip-gates on `NL2SPARQL_API_KEY` and never embeds/logs the key.
    - Docker-dependent rows skip cleanly with no ArangoDB: `pytest tests/perf -m perf -k "not translate_latency and not execute_overhead" -q` does not error/fail without Docker.
  </acceptance_criteria>
  <done>Eight report-only rows measure and append to LATENCY_REPORT.md without ever gating CI; the live-LLM row is key-gated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    7 non-gating perf report rows (`/sparql` GET, `/schema/introspect`, `/nl-translate`,
    memory idle/load, concurrency, first-byte) that append measured p95/memory numbers to
    `tests/perf/LATENCY_REPORT.md`. Claude has automated everything it can; the remaining step
    needs a live ArangoDB (Docker) and — for the `/nl-translate` row only — a human-held
    `NL2SPARQL_API_KEY` that Claude must never possess.
  </what-built>
  <how-to-verify>
    1. Start ArangoDB: `docker compose up -d arangodb` (host 8532, DB `sparql-to-aql`).
    2. Run the Docker report rows:
       `RUN_INTEGRATION=1 pytest tests/perf -m perf -k "not translate_latency and not execute_overhead and not nl_latency" -q`
    3. Optionally run the live-LLM row with your own key (NEVER paste it into any committed file):
       `RUN_INTEGRATION=1 NL2SPARQL_API_KEY=... pytest tests/perf/test_nl_latency.py -m perf -q`
    4. Confirm `tests/perf/LATENCY_REPORT.md` now holds a row per measured metric with plausible
       p95/memory numbers and that NO secret / API key appears anywhere in the report or logs.
    5. Review the numbers for sanity (advisory only — nothing gates CI here).
  </how-to-verify>
  <resume-signal>Type "approved" once LATENCY_REPORT.md is populated and secret-free, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human LLM key → /nl-translate row | a secret (`NL2SPARQL_API_KEY`) is supplied at runtime by a human, never by CI |
| report suite → docker-compose ArangoDB | Docker rows drive a live DB on host 8532 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | `NL2SPARQL_API_KEY` leaking into CI logs, `LATENCY_REPORT.md`, or a committed transcript | mitigate | The `/nl-translate` row is key-gated (skips without the key); the key is never referenced in a committed file; the human checkpoint explicitly verifies the report is secret-free before approval. |
| T-04-14 | Denial of Service | concurrency/memory-load rows exhausting the shared runner | mitigate | These rows are report-only, human-run on demand (never per-PR CI), with bounded concurrency; they cannot flake the gated pipeline. |
</threat_model>

<verification>
- `pytest tests/perf -m perf -k "not translate_latency and not execute_overhead" -q` runs without error/fail sans Docker (rows skip).
- No report row asserts against its §9.4 budget (all advisory).
- `LATENCY_REPORT.md` populated by the human checkpoint; contains no secret.
- Non-regression: these rows never run on the per-PR CI path; default suite unaffected.
</verification>

<success_criteria>
- REQ-performance-slos report tier delivered: 8 advisory rows measured into a checked-in, human-reviewed LATENCY_REPORT.md, with the live-LLM row key-gated and secret-free.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-07-SUMMARY.md` when done.
</output>
