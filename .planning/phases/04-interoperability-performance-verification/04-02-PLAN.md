---
phase: 04-interoperability-performance-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - arango_sparql/translate/owl.py
  - arango_sparql/service/routes/mapping.py
  - tests/translate/test_owl.py
  - tests/test_service_mapping_routes.py
autonomous: true
requirements: [REQ-ontoextract-integration, REQ-thirdparty-tool-compat]
must_haves:
  truths:
    - "turtle_to_mapping / mapping_to_turtle accept a format kwarg covering turtle, RDF/XML, JSON-LD, N-Triples"
    - "POST /mapping/import-owl parses an RDF/XML body when Content-Type: application/rdf+xml"
    - "POST /mapping/export-owl serialises RDF/XML when Accept: application/rdf+xml and sets a matching Content-Type"
    - "The OWL-bomb triple cap still triggers on an oversized RDF/XML body via the unchanged OwlBombError envelope"
  artifacts:
    - path: "arango_sparql/translate/owl.py"
      provides: "rdflib format-dispatch table + format kwarg threaded through parse/serialize"
      contains: "application/rdf+xml"
    - path: "arango_sparql/service/routes/mapping.py"
      provides: "Content-Type sniff + Accept negotiation returning the negotiated rdflib format name"
      contains: "application/rdf+xml"
    - path: "tests/translate/test_owl.py"
      provides: "format-dispatch roundtrip unit tests (turtle/xml/json-ld/nt)"
    - path: "tests/test_service_mapping_routes.py"
      provides: "RDF/XML import+export route test + RDF/XML OWL-bomb triple-cap test"
  key_links:
    - from: "arango_sparql/service/routes/mapping.py"
      to: "arango_sparql/translate/owl.py"
      via: "format kwarg passed to turtle_to_mapping/mapping_to_turtle"
      pattern: "format="
    - from: "mapping.py RDF/XML branch"
      to: "OwlBombError/OwlParseError"
      via: "unchanged HTTPException 422 envelope"
      pattern: "OwlBombError"
---

<objective>
Close the documented-but-unimplemented PRD §11.3/§12.2 contract: `/mapping/import-owl` and
`/mapping/export-owl` (and the underlying `turtle_to_mapping`/`mapping_to_turtle`) hardcode
`format="turtle"` end-to-end. Add an rdflib format-dispatch table (Turtle, RDF/XML, JSON-LD,
N-Triples — all native to installed rdflib 7.6, zero new deps) so RDF/XML round-trips work,
reusing the existing `OwlBombError`/`OwlParseError` → HTTPException(422) envelope UNCHANGED.

Purpose: The RDF/XML rows of the AOE roundtrip (Plan 04) and the Ontology-Playground
roundtrip (Plan 05) are BLOCKED on this. This is a scoped production-code gap-close framed as
finishing a shipped-but-partial contract, not a new feature (RESEARCH.md Open Question 2).
Output: format dispatch in `owl.py` + `mapping.py`; unit + route + security tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add format-dispatch to owl.py and thread the format kwarg</name>
  <files>arango_sparql/translate/owl.py, tests/translate/test_owl.py</files>
  <read_first>
    - arango_sparql/translate/owl.py (the file being modified; `turtle_to_mapping` def at line 191, hardcoded `graph.parse(data=turtle, format="turtle")` at line 242, second parse site at line 576, `mapping_to_turtle` at line 433 with `graph.serialize(format="turtle")` at line 470; `OwlBombError`/`OwlParseError` at lines 116/128; `count_triples` at line 144)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`arango_sparql/translate/owl.py` section: the verbatim `_FORMAT_ALIASES` dict and the "thread a `format: str = "turtle"` kwarg" instruction; triple-cap invariant)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pitfall 1; Security §V5: the cap is applied post-parse to the Graph so it stays format-agnostic)
    - tests/translate/test_owl.py (the self-analog test file to extend)
  </read_first>
  <behavior>
    - Test: `turtle_to_mapping(rdfxml_text, format="xml")` returns a bundle equal to `turtle_to_mapping(turtle_text)` for the same ontology (roundtrip parity across formats).
    - Test: `mapping_to_turtle(bundle, format="xml")` output re-parses under rdflib `format="xml"` and is `isomorphic` to `mapping_to_turtle(bundle)` parsed as turtle.
    - Test: JSON-LD (`format="json-ld"`) and N-Triples (`format="nt"`) round-trip the same bundle.
    - Test: an unknown format string raises `OwlParseError` (not a bare rdflib exception).
    - Test: a graph exceeding the triple cap raises `OwlBombError` regardless of input format.
  </behavior>
  <action>
    Add a module-level `_FORMAT_ALIASES` dict mapping MIME strings to rdflib format names:
    `{"text/turtle": "turtle", "application/x-turtle": "turtle", "application/rdf+xml": "xml",
    "application/ld+json": "json-ld", "application/n-triples": "nt"}` (copy verbatim from
    PATTERNS.md). Add a resolver that accepts either a MIME string or a bare rdflib format
    name (`turtle`/`xml`/`json-ld`/`nt`) and returns the rdflib format name, raising
    `OwlParseError` (code `E_OWL_PARSE`) for anything unrecognised. Thread a
    `format: str = "turtle"` kwarg through `turtle_to_mapping` (line 191, apply at the
    line-242 and line-576 parse sites) and `mapping_to_turtle` (line 433, apply at the
    line-470 serialize site). Do NOT fork per-format function bodies and do NOT move or
    modify the `count_triples(graph)` cap (lines 246-253) — it stays post-parse and thus
    format-agnostic. Do NOT invent new exception classes; reuse `OwlBombError`/`OwlParseError`.
    Extend `tests/translate/test_owl.py` with the behaviors above.
  </action>
  <verify>
    <automated>python -m pytest tests/translate/test_owl.py -q && python -c "s=open('arango_sparql/translate/owl.py').read(); assert 'application/rdf+xml' in s and 'json-ld' in s and '\"nt\"' in s, 'dispatch table incomplete'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `owl.py` contains a format-dispatch dict keyed on turtle/xml/json-ld/nt (MIME + bare names).
    - `turtle_to_mapping` and `mapping_to_turtle` accept a `format=` kwarg defaulting to `"turtle"`.
    - `pytest tests/translate/test_owl.py -q` exits 0 with the new roundtrip + unknown-format + bomb tests passing.
    - No new exception class is defined; unknown format raises `OwlParseError`.
    - The `count_triples` cap call is unchanged (still applied to the parsed Graph).
  </acceptance_criteria>
  <done>owl.py round-trips turtle/xml/json-ld/nt via a single format kwarg; triple cap and error types unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content-Type/Accept format dispatch in the mapping routes</name>
  <files>arango_sparql/service/routes/mapping.py, tests/test_service_mapping_routes.py</files>
  <read_first>
    - arango_sparql/service/routes/mapping.py (the file being modified; `_read_import_body` at line 111, `_wants_turtle_response` at line 200, `import_owl` at line 222 with OwlBombError/OwlParseError handling at lines 253/268, `export_owl` at line 329 with handlers at 370/378 and the roundtrip `parse(data=turtle, format="turtle")` at line 415 + `format="turtle" if wants_turtle else "json"` at line 428)
    - arango_sparql/translate/owl.py (the `_FORMAT_ALIASES` table + threaded format kwarg added in Task 1 — the contract this route layer consumes)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`arango_sparql/service/routes/mapping.py` section: extend `_read_import_body`/`_wants_turtle_response` to return the negotiated format name; error-envelope shape to mirror)
    - tests/test_service_mapping_routes.py (the self-analog route test file; existing OWL-bomb test at lines 326-378, export-bomb at 513-522)
  </read_first>
  <behavior>
    - Test: POST /mapping/import-owl with `Content-Type: application/rdf+xml` and an RDF/XML body returns 200 and a mapping bundle equal to the Turtle-import bundle for the same ontology.
    - Test: POST /mapping/export-owl with `Accept: application/rdf+xml` returns 200, a body that re-parses under rdflib `format="xml"`, and response `Content-Type` containing `application/rdf+xml`.
    - Test: existing Turtle import/export path is byte-for-byte unchanged (default Accept still yields Turtle/JSON as before).
    - Test: an RDF/XML body exceeding `MAPPING_IMPORT_MAX_TRIPLES` returns 422 with `code == "E_OWL_TOO_LARGE"` and the same error envelope shape as the Turtle case.
  </behavior>
  <action>
    Generalise `_read_import_body` (line 111) so the non-JSON branch resolves the request
    `Content-Type` through `owl.py`'s `_FORMAT_ALIASES` and returns the negotiated rdflib
    format name alongside the decoded body (instead of assuming Turtle), then pass that
    format into `turtle_to_mapping(..., format=...)` inside `import_owl`. Generalise
    `_wants_turtle_response` (line 200) into a negotiation helper that returns the rdflib
    format name from the `Accept` header (`application/rdf+xml` → `"xml"`, etc., defaulting
    to Turtle when absent/`*/*`), and in `export_owl` call `mapping_to_turtle(bundle,
    format=negotiated)` and set the response `Content-Type` to the matching MIME
    (`application/rdf+xml` for `xml`). Update the line-415 roundtrip parse and the line-428
    serialize to use the negotiated format. Reuse the EXISTING
    `except OwlBombError`/`except OwlParseError` → `HTTPException(422, {"error":
    _sanitize_error(str(exc)), "code": exc.code})` envelope verbatim for every new format
    branch — do not add a new error shape. Extend `tests/test_service_mapping_routes.py`
    with the behaviors above (add the RDF/XML OWL-bomb assertion mirroring the existing
    `MAPPING_IMPORT_MAX_TRIPLES` test at lines 326-378 but posting `application/rdf+xml`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_service_mapping_routes.py -q && python -c "s=open('arango_sparql/service/routes/mapping.py').read(); assert 'application/rdf+xml' in s, 'no rdf+xml negotiation'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `mapping.py` negotiates `application/rdf+xml` on both Content-Type (import) and Accept (export).
    - `pytest tests/test_service_mapping_routes.py -q` exits 0 including the new RDF/XML import, RDF/XML export, and RDF/XML OWL-bomb tests.
    - The RDF/XML OWL-bomb test asserts `code == "E_OWL_TOO_LARGE"` via the unchanged envelope.
    - Existing Turtle-path tests remain green (no regression).
  </acceptance_criteria>
  <done>Both mapping routes negotiate RDF/XML (and JSON-LD/N-Triples) with the OwlBomb/OwlParse envelope unchanged; RDF/XML bomb defence proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /mapping/import-owl | untrusted RDF/XML/JSON-LD/N-Triples bytes cross into rdflib parsers (NEW format surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | RDF/XML entity-expansion ("billion laughs") via the new `application/rdf+xml` parse path | mitigate | The `count_triples(graph)` cap (`E_OWL_TOO_LARGE`/`OwlBombError`) is applied to the parsed Graph regardless of source format; Task 2 adds an explicit RDF/XML OWL-bomb test asserting the same 422 envelope triggers. rdflib's RDF/XML parser is stdlib `xml.sax`-based (no dangerous external-entity expansion by default). |
| T-04-04 | Tampering | malformed RDF/XML/JSON-LD raising a raw rdflib exception leaking internals | mitigate | Unknown/invalid formats raise the existing `OwlParseError` (`E_OWL_PARSE`), surfaced through the unchanged `_sanitize_error` HTTPException envelope — no raw stack/internal path leaks. |
</threat_model>

<verification>
- `pytest tests/translate/test_owl.py tests/test_service_mapping_routes.py -q` green.
- RDF/XML import→export round-trips isomorphically; RDF/XML OWL-bomb returns 422 `E_OWL_TOO_LARGE`.
- Non-regression: `pytest -m "not integration and not w3c and not eval and not perf" -q` green; the deterministic SPARQL→AQL transpiler package is untouched (only OWL import/export I/O changed).
- W3C DAWG query-eval coverage ≥ 96.4% unaffected (no transpiler file touched).
</verification>

<success_criteria>
- RDF/XML (plus JSON-LD, N-Triples) import and export work through a single format kwarg.
- Existing Turtle behaviour and the OwlBomb/OwlParse envelope are byte-unchanged.
- RDF/XML OWL-bomb defence has an asserting test.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-02-SUMMARY.md` when done.
</output>
