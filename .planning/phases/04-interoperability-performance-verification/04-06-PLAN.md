---
phase: 04-interoperability-performance-verification
plan: 06
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/perf/test_translate_latency.py
  - tests/perf/test_execute_overhead.py
  - tests/perf/baseline.json
autonomous: true
requirements: [REQ-performance-slos]
must_haves:
  truths:
    - "The three CI-gated perf rows (/translate cold, /translate warm, /execute overhead) run fully in-process with zero Docker via the _FakeArangoClient double"
    - "Each row measures p95 with statistics.quantiles and fails if p95 > baseline * 1.25"
    - "tests/perf/baseline.json is checked in with the three CI-gated p95 rows"
  artifacts:
    - path: "tests/perf/test_translate_latency.py"
      provides: "translate cold + warm p95 gate"
      contains: "quantiles"
    - path: "tests/perf/test_execute_overhead.py"
      provides: "execute-overhead p95 gate (AQL stubbed to RETURN 1)"
      contains: "fake_client_factory"
    - path: "tests/perf/baseline.json"
      provides: "checked-in p95 baseline for the three CI-gated rows"
      contains: "execute_overhead_p95_ms"
  key_links:
    - from: "tests/perf/test_execute_overhead.py"
      to: "tests/perf/conftest.py fake_client_factory"
      via: "monkeypatched ArangoClient so /execute is dependency-free"
      pattern: "fake_client_factory"
    - from: "tests/perf/test_*_latency.py"
      to: "tests/perf/baseline.json"
      via: "load_baseline() + 1.25 tolerance gate"
      pattern: "baseline"
---

<objective>
Deliver the CI-GATED tier of REQ-performance-slos (D-08): the three fast, deterministic,
in-process rows — `/translate` cold, `/translate` warm, `/execute` overhead (AQL pinned to
`RETURN 1` via the `_FakeArangoClient` double so it is genuinely Docker-free) — each measuring
p95 via `statistics.quantiles` and failing if p95 > checked-in baseline * 1.25 (§9.4's
generous 25% tolerance to survive shared-runner jitter). Baseline captured and committed to
`tests/perf/baseline.json`.

Purpose: These rows fit the per-PR fast path (no Docker, no LLM). Pitfall 2: without the fake
double, `/execute` silently reaches a live DB. Uses Plan 01's perf scaffolding.
Output: `tests/perf/test_translate_latency.py`, `test_execute_overhead.py`, `baseline.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@.planning/phases/04-interoperability-performance-verification/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI-gated translate + execute p95 perf tests</name>
  <files>tests/perf/test_translate_latency.py, tests/perf/test_execute_overhead.py</files>
  <read_first>
    - tests/perf/conftest.py (Plan 01: `fake_client_factory`, `p95`, `load_baseline`, `_connect_session` helpers this test consumes)
    - tests/test_service_sparql_routes.py (the `_FakeArangoClient`/`_FakeDb`/`_FakeCursor` double + `_connect_session` + the `/translate` and `/execute` request shapes at the route-test call sites)
    - arango_sparql/service/routes/sparql.py (line 265 `session.db.aql.execute(...)` — the real DB call the fake double must stub; confirms why the monkeypatch is mandatory)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/perf/test_translate_latency.py`/`test_execute_overhead.py` section: the vetted N=120, warmup-discard [20:], `p95 <= baseline * 1.25` sketch; Pitfall 2 warning)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pattern 3; Open Question 3: N=100-200 samples, discard first 10-20 as warmup; §9.4 `/execute overhead` = translate+dispatch EXCLUDING AQL exec)
  </read_first>
  <action>
    Create both files with `pytestmark = pytest.mark.perf`. In `test_execute_overhead.py`, use
    the `fake_client_factory` fixture to monkeypatch `arango_sparql.service.ArangoClient` so
    `/connect` then `/translate` then `/execute` run with zero real I/O (Pitfall 2 — the row is
    defined as translate+dispatch EXCLUDING AQL exec, so stubbing is correct, not a workaround).
    Drive a `TestClient(app)`, `_connect_session`, then loop ~120 iterations of the target
    request timing each with `time.perf_counter()` in ms, discard the first 20 as warmup,
    compute p95 via the `p95` helper, and assert `p95 <= load_baseline()["execute_overhead_p95_ms"]
    * 1.25` (skip the assertion gracefully when the baseline key is absent so the first capture
    run does not fail). In `test_translate_latency.py`, measure two rows: `/translate` COLD
    (fresh schema each iteration) and `/translate` WARM (reused cached schema), gating each
    against `translate_cold_p95_ms` / `translate_warm_p95_ms` * 1.25. AQL for the execute row is
    pinned to a trivial query so the fake cursor returns instantly. No `pytest-benchmark`, no
    numpy/scipy.
  </action>
  <verify>
    <automated>python -m pytest tests/perf/test_translate_latency.py tests/perf/test_execute_overhead.py -m perf -q 2>&1 | tail -8 && python -c "s1=open('tests/perf/test_execute_overhead.py').read(); s2=open('tests/perf/test_translate_latency.py').read(); assert 'fake_client_factory' in s1, 'execute row not stubbed (Pitfall 2)'; assert 'pytest_benchmark' not in s1+s2 and 'import numpy' not in s1+s2; assert '1.25' in s1 and '1.25' in s2; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files carry `pytest.mark.perf`.
    - `test_execute_overhead.py` monkeypatches `ArangoClient` via `fake_client_factory` (no Docker, no `RUN_INTEGRATION`).
    - Each row computes p95 via the stdlib `p95`/`statistics.quantiles` helper and gates at `* 1.25`.
    - `/translate` has distinct cold and warm rows.
    - Neither file imports `pytest_benchmark`, `numpy`, or `scipy`.
    - `pytest tests/perf/test_translate_latency.py tests/perf/test_execute_overhead.py -m perf -q` runs without Docker (passes or skips-on-missing-baseline, never errors).
  </acceptance_criteria>
  <done>Three CI-gated perf rows run fully in-process with a 25% p95 tolerance gate; /execute is stubbed via the fake double.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and commit the CI-gated baseline.json</name>
  <files>tests/perf/baseline.json</files>
  <read_first>
    - tests/nl2sparql/eval/baseline.json (the checked-in-baseline convention analog: `generated_at` timestamp + nested results)
    - tests/perf/conftest.py (the `load_baseline` shape the tests read)
    - tests/perf/test_translate_latency.py, tests/perf/test_execute_overhead.py (the exact baseline keys the gates reference: `translate_cold_p95_ms`, `translate_warm_p95_ms`, `execute_overhead_p95_ms`)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/perf/baseline.json` section: the exact top-level shape `{"generated_at": ..., "rows": {...}}`; only the 3 CI-gated rows belong here, never the 8 report rows)
  </read_first>
  <action>
    Run the two perf test files a few times in capture mode to produce measured p95 numbers on
    this machine, then write `tests/perf/baseline.json` with a `generated_at` ISO timestamp and
    a `rows` object holding exactly `translate_cold_p95_ms`, `translate_warm_p95_ms`, and
    `execute_overhead_p95_ms` set to a stable central value across the capture runs (avoid
    baking in a single jitter spike). Do NOT include any of the 8 report-only rows here (they
    live in LATENCY_REPORT.md, Plan 07). Re-run the perf tests against the committed baseline to
    confirm they now PASS the `* 1.25` gate.
  </action>
  <verify>
    <automated>python -c "import json; b=json.load(open('tests/perf/baseline.json')); r=b['rows']; assert set(r)>={'translate_cold_p95_ms','translate_warm_p95_ms','execute_overhead_p95_ms'}, r; assert all(isinstance(v,(int,float)) and v>0 for v in r.values()); assert 'generated_at' in b; print('baseline ok', r)" && python -m pytest tests/perf/test_translate_latency.py tests/perf/test_execute_overhead.py -m perf -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `tests/perf/baseline.json` has a `generated_at` timestamp and a `rows` object with exactly the three CI-gated keys, all positive numbers.
    - No report-only rows appear in `baseline.json`.
    - After the baseline is committed, `pytest tests/perf/test_translate_latency.py tests/perf/test_execute_overhead.py -m perf -q` exits 0 (all three rows within the `* 1.25` gate).
  </acceptance_criteria>
  <done>A stable checked-in baseline.json exists for the three CI-gated rows and the perf gate passes against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perf test → in-process app | no external I/O — `_FakeArangoClient` stubs the only DB call, so no trust boundary is actually crossed at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Denial of Service | perf loops (~120 iterations) exhausting CI runner resources | mitigate | In-process only, tiny fixtures, AQL stubbed to an instant fake cursor; bounded iteration count; no Docker/LLM in the CI-gated tier so runtime stays in the per-PR budget. |
| T-04-12 | Tampering | a jitter-inflated baseline masking a real regression | mitigate | Capture a stable central value across multiple runs; keep the 25% tolerance generous-but-bounded per §9.4/D-08 so a genuine >25% regression still fails. |
</threat_model>

<verification>
- `pytest tests/perf/test_translate_latency.py tests/perf/test_execute_overhead.py -m perf -q` green against the committed baseline, with NO Docker and NO `RUN_INTEGRATION`.
- `/execute` row provably stubbed (`fake_client_factory` present); no `pytest-benchmark`/numpy/scipy.
- Non-regression: default suite `pytest -m "not integration and not w3c and not eval and not perf" -q` unaffected.
</verification>

<success_criteria>
- REQ-performance-slos CI-gated tier delivered: three in-process p95 rows gate at 25% against a checked-in baseline with zero Docker dependency.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-06-SUMMARY.md` when done.
</output>
