---
phase: 04-interoperability-performance-verification
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - tests/integration/test_sparqlwrapper_smoke.py
  - tests/integration/test_ontology_playground_roundtrip.py
autonomous: true
requirements: [REQ-thirdparty-tool-compat]
must_haves:
  truths:
    - "SPARQLWrapper (a real urllib HTTP client) drives SELECT + ASK + Service Description against our /sparql over a real bound socket"
    - "The cosmic_coffee.rdf RDF/XML fixture round-trips /mapping/import-owl → /mapping/export-owl with rdflib isomorphism"
    - "Both tests skip-gate cleanly when Docker/ArangoDB is unavailable"
  artifacts:
    - path: "tests/integration/test_sparqlwrapper_smoke.py"
      provides: "SPARQLWrapper smoke over a background uvicorn server"
      min_lines: 60
      contains: "SPARQLWrapper"
    - path: "tests/integration/test_ontology_playground_roundtrip.py"
      provides: "file-based RDF/XML OWL roundtrip via /mapping routes"
      min_lines: 40
      contains: "isomorphic"
  key_links:
    - from: "tests/integration/test_sparqlwrapper_smoke.py"
      to: "uvicorn.Server(bound 127.0.0.1)"
      via: "background daemon thread giving SPARQLWrapper a real socket"
      pattern: "uvicorn"
    - from: "tests/integration/test_ontology_playground_roundtrip.py"
      to: "tests/fixtures/cosmic_coffee.rdf"
      via: "RDF/XML fixture import/export"
      pattern: "cosmic_coffee"
---

<objective>
Deliver the AUTOMATED half of REQ-thirdparty-tool-compat (D-05/D-06): a SPARQLWrapper smoke
test (SELECT + ASK + Service Description fetch) driven over a REAL bound HTTP socket via a
background `uvicorn` server (SPARQLWrapper is a real `urllib` client — `TestClient` has no
socket), and a file-based Ontology-Playground roundtrip that imports/exports the vendored
`cosmic_coffee.rdf` (RDF/XML) through our `/mapping` routes and asserts rdflib isomorphism.
Both Docker-gated on a live ArangoDB.

Purpose: These are the two automatable, high-value tools PRD §11.1 names. Playground is
file-based (no browser); the RDF/XML path depends on Plan 02, the fixture + SPARQLWrapper dep
on Plan 01.
Output: `tests/integration/test_sparqlwrapper_smoke.py`, `test_ontology_playground_roundtrip.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@.planning/phases/04-interoperability-performance-verification/04-01-PLAN.md
@.planning/phases/04-interoperability-performance-verification/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPARQLWrapper smoke over a background uvicorn server</name>
  <files>tests/integration/test_sparqlwrapper_smoke.py</files>
  <read_first>
    - tests/integration/test_execute_endpoint.py (boot/skip + `_connect_session` + `_seeded_collection` to reuse so the bound server has a real session+schema to query)
    - tests/integration/conftest.py (shared `try_boot_arangodb_via_compose`, `integration_enabled`, `arangodb_reachable`, `DEFAULT_ARANGO_*`)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/integration/test_sparqlwrapper_smoke.py` section: the verified live-uvicorn-thread fixture sketch + SPARQLWrapper client pattern; anti-pattern — never point SPARQLWrapper at TestClient)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Pattern 4 uvicorn thread — `port=0`, `while not server.started`, `server.servers[0].sockets[0].getsockname()`, `server.should_exit`; Code Examples SPARQLWrapper SELECT/ASK; Assumption A2 — ASK JSON carries `{"boolean": bool}`)
  </read_first>
  <action>
    Create `tests/integration/test_sparqlwrapper_smoke.py` with `pytestmark =
    pytest.mark.integration`. Add a fixture that binds a real `uvicorn.Server(uvicorn.Config(
    app, host="127.0.0.1", port=0, log_level="warning"))` in a daemon thread, waits on
    `server.started`, reads the actual bound port from
    `server.servers[0].sockets[0].getsockname()`, and tears down via `server.should_exit=True`
    + `thread.join`. Reuse the boot/skip + `_connect_session` + `_seeded_collection` machinery
    so a session with a queryable schema exists. Using `SPARQLWrapper(f"http://127.0.0.1:
    {port}/sparql")` with `setMethod(POST)` + `setReturnFormat(JSON)`: run a SELECT (assert
    `results.bindings` present and non-empty against seeded data), an ASK (assert the result
    carries a `boolean` key of type bool), and a Service Description fetch (an empty GET to
    `/sparql` returns a service-description document). Do NOT point SPARQLWrapper at
    `TestClient` (no real socket). Skip cleanly without Docker.
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_sparqlwrapper_smoke.py -m integration -q 2>&1 | tail -5; RUN_INTEGRATION=1 python -m pytest tests/integration/test_sparqlwrapper_smoke.py -m integration -q 2>&1 | tail -5; python -c "s=open('tests/integration/test_sparqlwrapper_smoke.py').read(); assert 'uvicorn' in s and 'SPARQLWrapper' in s and 'boolean' in s and 'TestClient' not in s.replace('# TestClient',''); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Test uses a background `uvicorn.Server` on `127.0.0.1:0` (real socket) — not `TestClient` — as the SPARQLWrapper target.
    - SELECT, ASK (asserting a `boolean` bool), and Service Description fetch are all exercised.
    - `python -m pytest tests/integration/test_sparqlwrapper_smoke.py -m integration -q` skips cleanly with no Docker.
    - `RUN_INTEGRATION=1 pytest tests/integration/test_sparqlwrapper_smoke.py -m integration -q` exits 0 with ArangoDB reachable.
  </acceptance_criteria>
  <done>SPARQLWrapper drives SELECT+ASK+Service Description against our /sparql over a real bound port, Docker-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Ontology Playground file-based RDF/XML roundtrip</name>
  <files>tests/integration/test_ontology_playground_roundtrip.py</files>
  <read_first>
    - tests/integration/conftest.py (boot/skip helpers) and tests/test_service_sparql_routes.py (`client.post("/mapping/import-owl", ...)` route-call idiom + `_connect_session`)
    - tests/fixtures/cosmic_coffee.rdf (the vendored RDF/XML fixture from Plan 01 — the input to round-trip)
    - arango_sparql/service/routes/mapping.py (the RDF/XML Content-Type/Accept negotiation added by Plan 02)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`tests/integration/test_ontology_playground_roundtrip.py` section: the import (`Content-Type: application/rdf+xml`) → export (`Accept: application/rdf+xml`) → `rdflib.Graph.isomorphic()` sketch; Pitfall 3 — do NOT reuse this fixture for the AOE test)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Architecture Diagram §C: file-based only, no live SPARQL needed)
  </read_first>
  <action>
    Create `tests/integration/test_ontology_playground_roundtrip.py` with `pytestmark =
    pytest.mark.integration`. Read `tests/fixtures/cosmic_coffee.rdf` bytes, POST them to
    `/mapping/import-owl` with `Content-Type: application/rdf+xml`, take the returned bundle,
    POST it to `/mapping/export-owl` with `Accept: application/rdf+xml`, then parse the
    original fixture and the re-exported body both with rdflib `format="xml"` and assert
    `Graph.isomorphic()`. This is pure OWL fidelity — no `/sparql`, no `phys:` annotations
    required (Pitfall 3: keep separate from the AOE fixture). Docker-gate via the shared
    boot/skip helpers (a `/connect` session is needed for the authenticated `/mapping`
    routes). Skip cleanly without Docker.
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_ontology_playground_roundtrip.py -m integration -q 2>&1 | tail -5; RUN_INTEGRATION=1 python -m pytest tests/integration/test_ontology_playground_roundtrip.py -m integration -q 2>&1 | tail -5; python -c "s=open('tests/integration/test_ontology_playground_roundtrip.py').read(); assert 'cosmic_coffee' in s and 'application/rdf+xml' in s and 'isomorphic' in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Test imports `cosmic_coffee.rdf` as `application/rdf+xml` and exports as `application/rdf+xml`.
    - Asserts `rdflib.Graph.isomorphic()` between original and re-exported RDF/XML.
    - Does NOT query `/sparql` and does NOT require `phys:` annotations (Pitfall 3 kept).
    - `RUN_INTEGRATION=1 pytest tests/integration/test_ontology_playground_roundtrip.py -m integration -q` exits 0 with ArangoDB reachable; skips cleanly otherwise.
  </acceptance_criteria>
  <done>The Ontology-Playground RDF/XML fixture round-trips isomorphically through our /mapping routes, Docker-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPARQLWrapper client → bound /sparql socket | real HTTP over 127.0.0.1 loopback into our service |
| vendored RDF/XML fixture → rdflib parser | untrusted-shaped RDF/XML crosses into the import path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | background uvicorn server bound to a socket | mitigate | Bind only `127.0.0.1` (loopback) on an ephemeral `port=0`; tear down via `should_exit` + `thread.join` so no server lingers past the test. |
| T-04-10 | Denial of Service | cosmic_coffee.rdf RDF/XML import (entity expansion) | mitigate | Fixture is a vendored, pinned-SHA static file (Plan 01) parsed through the OwlBomb-capped import path (Plan 02); no live fetch. |
| T-04-SC | Tampering | SPARQLWrapper dependency | mitigate | Approved in RESEARCH.md legitimacy audit; installed via the pinned Plan 01 dev dep; no runtime network beyond loopback. |
</threat_model>

<verification>
- `RUN_INTEGRATION=1 pytest tests/integration/test_sparqlwrapper_smoke.py tests/integration/test_ontology_playground_roundtrip.py -m integration -q` green with ArangoDB up; both skip cleanly without Docker.
- SPARQLWrapper SELECT+ASK+Service Description all succeed; Playground RDF/XML roundtrip isomorphic.
- Non-regression: default suite unaffected; W3C DAWG coverage untouched.
</verification>

<success_criteria>
- REQ-thirdparty-tool-compat automated half proven: SPARQLWrapper and Ontology Playground both pass automated smoke/roundtrip tests, Docker-gated.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-05-SUMMARY.md` when done.
</output>
