---
phase: 04-interoperability-performance-verification
plan: 08
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/howto/protege.md
  - docs/howto/yasgui.md
  - docs/howto/arq.md
  - docs/howto/sparqlwrapper.md
  - docs/howto/ontology-playground.md
autonomous: false
requirements: [REQ-thirdparty-tool-compat]
must_haves:
  truths:
    - "Protégé and YASGUI each have a docs/howto recipe with a RECORDED smoke transcript (SELECT + ASK + Service Description)"
    - "The Protégé recipe drives the endpoint headless via Apache Jena rsparql --service (NOT arq)"
    - "sparqlwrapper.md and ontology-playground.md document the automated tests' invocations as reproducible recipes"
    - "No JVM/browser image is added to CI"
  artifacts:
    - path: "docs/howto/protege.md"
      provides: "Protégé recipe + recorded rsparql transcript"
      contains: "rsparql"
    - path: "docs/howto/yasgui.md"
      provides: "YASGUI browser recipe + recorded transcript"
    - path: "docs/howto/arq.md"
      provides: "Apache Jena rsparql invocation recipe + transcript"
      contains: "rsparql --service"
    - path: "docs/howto/sparqlwrapper.md"
      provides: "SPARQLWrapper recipe mirroring the automated smoke test"
    - path: "docs/howto/ontology-playground.md"
      provides: "Ontology Playground file-based roundtrip recipe"
  key_links:
    - from: "docs/howto/protege.md"
      to: "our /sparql endpoint"
      via: "rsparql --service headless driver"
      pattern: "rsparql --service"
    - from: "docs/howto/index.md"
      to: "the five recipe files"
      via: "recipe index links"
      pattern: "protege"
---

<objective>
Deliver the DOCUMENTED-MANUAL half of REQ-thirdparty-tool-compat (D-07): `docs/howto/` recipes
for the JVM/browser-heavy tools — Protégé (driven headless via Apache Jena `rsparql --service`,
NOT `arq`, per RESEARCH.md) and YASGUI (browser widget) — each with a RECORDED smoke transcript
(SELECT + ASK + Service Description fetch), plus reproducible recipes for the two automated
tools (SPARQLWrapper, Ontology Playground). NO JVM/browser image in CI.

Purpose: These tools are too heavy to automate per §13.1's nightly/on-demand posture, so they
get documented recipes + a human-recorded transcript instead of a CI test. Uses Plan 01's
`docs/howto/index.md` anchor.
Output: `docs/howto/{protege,yasgui,arq,sparqlwrapper,ontology-playground}.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interoperability-performance-verification/04-CONTEXT.md
@.planning/phases/04-interoperability-performance-verification/04-RESEARCH.md
@.planning/phases/04-interoperability-performance-verification/04-PATTERNS.md
@.planning/phases/04-interoperability-performance-verification/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the five docs/howto recipes</name>
  <files>docs/howto/protege.md, docs/howto/yasgui.md, docs/howto/arq.md, docs/howto/sparqlwrapper.md, docs/howto/ontology-playground.md</files>
  <read_first>
    - docs/howto/index.md (Plan 01: the recipe index these files are linked from — keep names consistent)
    - .planning/phases/04-interoperability-performance-verification/04-RESEARCH.md (Code Examples: the verified `rsparql --service <url> --query <q> --results JSON` invocations for SELECT and ASK; SPARQLWrapper SELECT/ASK snippet; Assumption A1 rsparql-not-arq; A3 YASGUI package caveat)
    - .planning/phases/04-interoperability-performance-verification/04-PATTERNS.md (`No Analog Found` section: draft from PRD §11.2-§11.4 prose using a "Prerequisites / Connect / SELECT / ASK / Service Description / Transcript" template)
    - .planning/phases/04-interoperability-performance-verification/04-CONTEXT.md (D-07: Protégé via Apache Jena rsparql headless, NOT arq; recorded transcript required; no JVM/browser image in CI)
  </read_first>
  <action>
    Create the five recipe files using a consistent template: Prerequisites / Connect (point the
    tool at our running `/sparql`, host per docker-compose) / SELECT / ASK / Service Description
    fetch / Transcript (a fenced block reserved for the recorded output — filled in Task 2 for
    protege.md and yasgui.md). `protege.md` and `arq.md`: use `rsparql --service <our /sparql>
    --query '<q>' --results JSON` (the actual Homebrew `jena` binary — NOT `arq`, per RESEARCH.md
    A1); arq.md documents the raw rsparql invocations, protege.md frames them as the headless
    driver behind Protégé's SPARQL panel. `yasgui.md`: browser-widget setup pointing at our
    endpoint (note the YASGUI package caveat from A3 as a maintenance flag). `sparqlwrapper.md`:
    mirror the exact queries from the automated `test_sparqlwrapper_smoke.py` so the recipe is a
    reproducible manual companion. `ontology-playground.md`: document the file-based RDF/XML
    import→export roundtrip against `/mapping`. State explicitly in protege.md/yasgui.md that no
    JVM/browser image is added to CI (documented-manual only). Do NOT commit any secret.
  </action>
  <verify>
    <automated>python -c "import os; files=['docs/howto/protege.md','docs/howto/yasgui.md','docs/howto/arq.md','docs/howto/sparqlwrapper.md','docs/howto/ontology-playground.md']; assert all(os.path.exists(f) for f in files), 'missing recipe'; assert 'rsparql --service' in open('docs/howto/arq.md').read(); assert 'rsparql' in open('docs/howto/protege.md').read() and 'arq' not in open('docs/howto/protege.md').read().replace('arq.md','').replace('rsparql',''); assert 'SPARQLWrapper' in open('docs/howto/sparqlwrapper.md').read(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All five recipe files exist under `docs/howto/`.
    - `arq.md` and `protege.md` use `rsparql --service` (Apache Jena) — NOT `arq` — as the headless driver.
    - Each recipe covers SELECT, ASK, and Service Description fetch against our `/sparql`.
    - `sparqlwrapper.md` mirrors the automated test's queries; `ontology-playground.md` documents the RDF/XML `/mapping` roundtrip.
    - protege.md/yasgui.md state no JVM/browser image is added to CI; no secret is committed.
  </acceptance_criteria>
  <done>Five reproducible howto recipes exist with an empty transcript block awaiting the human recording.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Five `docs/howto/` recipes with a reserved transcript block. Claude has drafted the
    recipes and the exact `rsparql`/SPARQLWrapper invocations; the remaining step needs the
    JVM (Apache Jena, `brew install jena`) and a browser (YASGUI) that cannot run in CI, so a
    human records the smoke transcript against a running service.
  </what-built>
  <how-to-verify>
    1. Start the service + ArangoDB (`docker compose up -d arangodb`; run the app so `/sparql`
       is reachable).
    2. Protégé/`rsparql`: run the SELECT, ASK, and an empty-GET Service Description fetch from
       `docs/howto/arq.md` / `protege.md` against your `/sparql` URL; copy the actual terminal
       output into the transcript block of `protege.md` (and `arq.md` if desired).
    3. YASGUI: point a YASGUI instance at `/sparql`, run the same SELECT + ASK + Service
       Description, and paste the observed results into `yasgui.md`'s transcript block.
    4. Confirm each transcript shows a successful SELECT (bindings), ASK (boolean), and Service
       Description document, and that no API key or secret appears in any transcript.
  </how-to-verify>
  <resume-signal>Type "approved" once protege.md and yasgui.md carry recorded, secret-free transcripts, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external tool (Protégé/YASGUI/rsparql) → /sparql | third-party clients connect to our HTTP surface; documented, not run in CI |
| recorded transcript → committed docs | human-pasted output enters the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Information Disclosure | recorded transcript accidentally containing a secret or internal URL | mitigate | The checkpoint explicitly verifies transcripts are secret-free; recipes target a local `/sparql` (no credentials in query strings). |
| T-04-16 | Server-Side Request Forgery | a Service Description / tool-driven fetch pointed at an internal endpoint | accept | Documented-manual recipes only, run by a human against their own local service; no automated CI fetch of arbitrary endpoints. The existing service SSRF guards (Phase 3) remain the runtime control. |
</threat_model>

<verification>
- All five recipes exist; Protégé/arq use `rsparql --service` (not `arq`).
- protege.md + yasgui.md carry human-recorded SELECT + ASK + Service Description transcripts (secret-free) after the checkpoint.
- No JVM/browser image added to CI; no secret committed.
- Non-regression: docs-only; no test/code/CI-image change.
</verification>

<success_criteria>
- REQ-thirdparty-tool-compat documented half delivered: Protégé + YASGUI recipes with recorded transcripts, plus reproducible SPARQLWrapper/Playground companions, no CI image cost.
</success_criteria>

<output>
Create `.planning/phases/04-interoperability-performance-verification/04-08-SUMMARY.md` when done.
</output>
