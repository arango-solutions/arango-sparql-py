---
phase: 07.4
slug: nl-sparql-predicate-schema-convention-grounding
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-24
---

# Phase 07.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/nl2sparql/ -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~60 seconds (CI tier; live eval sweeps are `RUN_EVAL`-gated + credentials-gated, run separately) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/nl2sparql/ -q`
- **After every plan wave:** Run `uv run pytest -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | NL-ACC-02 | — | N/A | unit | `{command}` | ❌ W0 | ⬜ pending |

*Planner fills this table per task. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Existing `tests/nl2sparql/eval/` + grounding test infrastructure covers all phase requirements — no new framework install.

*Planner confirms Wave 0 needs during planning.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live CK25 McNemar accuracy lift vs 07.3 baseline | NL-ACC-02 | Requires OpenAI credentials + `RUN_EVAL` (cost, non-CI) | `RUN_EVAL=1 uv run pytest tests/nl2sparql/eval/ -k ck25-predicate-grounded` then compare McNemar p<0.05, 0 regressions |
| QALD-9-plus directional generalization signal | NL-ACC-02 | First live large-schema run; credentials-gated, directional (not a hard gate) | `RUN_EVAL=1` QALD predicate-grounded arm; record MDE via `power.py` |

*Deterministic recall guards + shape-classification purity are automated in the CI tier.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
