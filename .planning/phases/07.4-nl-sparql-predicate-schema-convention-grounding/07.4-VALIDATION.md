---
phase: 07.4
slug: nl-sparql-predicate-schema-convention-grounding
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-24
updated: 2026-07-24
---

# Phase 07.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/nl2sparql/ -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~60 seconds (CI tier; live eval sweeps are `RUN_EVAL`-gated + credentials-gated, run separately) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/nl2sparql/ -q`
- **After every plan wave:** Run `uv run pytest -q` (+ `python -m pytest tests/w3c/test_coverage_gate.py -q`)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.4-01-01 | 01 | 1 | NL-ACC-02 | T-07.4-01 | Every rendered predicate label sanitized via existing `_sanitize_label` | unit (sibling repo) | `cd /Users/plosiewicz/Desktop/arango-query-core && python -m pytest tests -k "predicate" -q` | ❌ W0 (creates engine tests) | ⬜ pending |
| 07.4-01-02 | 01 | 1 | NL-ACC-02 | T-07.4-01 | Predicate block composed post-cache-boundary (D-07) | unit (sibling repo) | `cd /Users/plosiewicz/Desktop/arango-query-core && python -m pytest tests -k "predicate or system_prompt or compose" -q` | ❌ W0 | ⬜ pending |
| 07.4-01-03 | 01 | 1 | NL-ACC-02 | — | N/A (docs) | source assert | `grep -c "NL-ACC-02" .planning/REQUIREMENTS.md` | ✓ | ⬜ pending |
| 07.4-02-01 | 02 | 2 | NL-ACC-02 | T-07.4-01, T-07.4-02 | Byte-identical wording; PREDICATE_DUMP_THRESHOLD bounds dump | unit | `uv run pytest tests/nl2sparql/test_adapter.py tests/nl2sparql/test_pipeline.py -q` | ✓ | ⬜ pending |
| 07.4-02-02 | 02 | 2 | NL-ACC-02 | T-07.4-SC | git-ls-remote-verified pin, no registry install | integration | `uv run pytest tests/nl2sparql/ -k "predicate or grounding or adapter" -q` | ✓ | ⬜ pending |
| 07.4-03-01 | 03 | 3 | NL-ACC-02 | T-07.4-D8 | Function-local pyoxigraph import (tests/-confined) | integration (offline) | `uv run python -c "from tests.nl2sparql.eval.grounding_index_builder import build_predicate_index; ttl=open('tests/nl2sparql/eval/vendored/ck25/raw/prod-inst-schema.ttl').read(); build_predicate_index(ttl); print('ok')"` | ❌ W0 (creates builder) | ⬜ pending |
| 07.4-03-02 | 03 | 3 | NL-ACC-02 | — | Deterministic offline shape purity | unit | `uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -k predicate -q` | ❌ W0 (creates tests) | ⬜ pending |
| 07.4-03-03 | 03 | 3 | NL-ACC-02 | — | Honest QALD shape-distribution report (OQ1/A3) | integration (offline) | `uv run python -c "from tests.nl2sparql.eval.grounding_index_builder import build_predicate_index; ttl=open('tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl').read(); idx=build_predicate_index(ttl); n=len(list(idx.retrieve('', 10**6))); assert n>40, n; print(n)"` | ❌ W0 | ⬜ pending |
| 07.4-04-01 | 04 | 4 | NL-ACC-02 | T-07.4-03 | D-05 capture adds only `c.actual`, no secret; gate on shared_ontology | integration (scripted, no network) | `RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-ck25-predicate-grounded'); print(r.pass_rate)"; RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-qald9plus-predicate-grounded'); print(r.pass_rate)"` | ❌ W0 (creates configs) | ⬜ pending |
| 07.4-04-02 | 04 | 4 | NL-ACC-02 | T-07.4-01 | D-07 cache boundary + cross-adapter parity for seam 7 | unit/SC-gate | `uv run pytest tests/nl2sparql/test_grounding_engine_prompt.py tests/nl2sparql/test_grounding_prompt_parity.py -k predicate -q` | ❌ W0 (extends tests) | ⬜ pending |
| 07.4-04-03 | 04 | 4 | NL-ACC-02 | — | Always-on offline predicate-recall guard | unit | `uv run pytest tests/nl2sparql/eval/test_grounding_recall.py -k predicate -q` | ❌ W0 (extends test) | ⬜ pending |
| 07.4-05-01 | 05 | 5 | NL-ACC-02 | T-07.4-03 | No secret in runbook | source assert | `grep -c "predicate-grounded" tests/nl2sparql/eval/README.md` | ✓ | ⬜ pending |
| 07.4-05-02 | 05 | 5 | NL-ACC-02 | T-07.4-03, T-07.4-04 | Key human-held, never committed; provenance recorded | manual (human-run, credentialed) | human-check (paste both-arm aggregates + per-case verdicts + corpus_sha + temperature) | N/A — always manual | ⬜ pending |
| 07.4-05-03 | 05 | 5 | NL-ACC-02 | T-07.4-04 | Reproducible provenance; W3C non-regression re-run | manual fold-in + automated gate | `grep -c "predicate-grounded" tests/nl2sparql/eval/baseline.json && uv run python -c "import json; json.load(open('tests/nl2sparql/eval/baseline.json')); print('valid json')"; python -m pytest tests/w3c/test_coverage_gate.py -q` | ✓ (gate exists) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. Live sweeps (07.4-05) are RUN_EVAL + credentials-gated and human-run.*

---

## Wave 0 Requirements

- [x] Existing `tests/nl2sparql/eval/` + grounding test infrastructure covers all phase requirements — no new framework install.
- [x] All net-new test files/functions (predicate shape tests, QALD shape-distribution smoke, predicate SC-gate, predicate parity, predicate-recall guard) are authored by the task that first needs them within its own plan — no task's `<automated>` verify depends on a test a later plan creates. The one non-existent target the checker flagged (a `predicate_grounded` test in `test_eval.py`) has been removed; 07.4-04-01 now invokes the runner directly (07.3-05 precedent).

*All MISSING references are created in-plan; no separate pre-wave scaffolding is required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live CK25 McNemar accuracy lift vs 07.3 baseline (HARD GATE) | NL-ACC-02 | Requires OpenAI-compatible credentials + `RUN_EVAL` (cost, non-CI); agent must never hold the key | 07.4-05 §10 runbook: run `openai-gpt4o-mini-ck25-predicate-grounded` + a fresh same-session `openai-gpt4o-mini-ck25-grounded` arm; paired McNemar p<0.05, 0 regressions vs 14/49 |
| QALD-9-plus directional generalization signal | NL-ACC-02 | First live large-schema run; credentials-gated, directional (NOT a gate) | 07.4-05: run `openai-gpt4o-mini-qald9plus` (zero-shot, pre-existing) + `openai-gpt4o-mini-qald9plus-predicate-grounded`; record MDE via `power.py` |

*Deterministic recall guards, shape-classification purity, the QALD shape-distribution smoke check, and the W3C ≥96.4% coverage gate are all automated in the CI tier.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (07.4-05-02 is the sole manual human-run checkpoint, by design — credentialed live sweep)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s (CI tier)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved
