---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
plan: 05
type: execute
wave: 5
depends_on: ["07.4-04"]
files_modified:
  - tests/nl2sparql/eval/README.md
  - tests/nl2sparql/eval/baseline.json
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [NL-ACC-02]
user_setup:
  - service: openai-compatible LLM
    why: "Live CK25 hard-gate sweep + QALD directional run require a real generator; the agent must never hold the key"
    env_vars:
      - name: NL2SPARQL_API_KEY
        source: "Provider dashboard (OpenAI-compatible). Note: NL2SPARQL_API_KEY, NOT OPENAI_API_KEY — Phase 06.2-04 Pitfall 1"
must_haves:
  truths:
    - "The README documents the §10 predicate-grounded sweep runbook (both arms, temperature/corpus_sha provenance, manual human-reviewed fold-in, non-regression re-confirm) mirroring §9"
    - "The CK25 predicate-grounded vs the 07.3 entity-grounded baseline (14/49) confirmatory McNemar is recorded with p<0.05 and zero regressions (HARD GATE)"
    - "The first-ever live QALD-9-plus predicate-grounded (and zero-shot) numbers are recorded as a directional generalization signal (NOT a gate)"
    - "baseline.json carries the new predicate-grounded entries as SEPARATE per-corpus entries, never blended; the confirmatory pairing uses a same-session run, not a stale historical entry"
    - "NL-ACC-02 is closed in REQUIREMENTS.md via the significant-lift path (or the human-accepted-documented-null path if the gate returns null), with numbers matching baseline.json"
  artifacts:
    - path: "tests/nl2sparql/eval/README.md"
      provides: "§10 predicate-grounding sweep runbook"
      contains: "predicate-grounded"
    - path: "tests/nl2sparql/eval/baseline.json"
      provides: "CK25 + QALD predicate-grounded confirmatory/directional entries"
      contains: "predicate-grounded"
    - path: ".planning/REQUIREMENTS.md"
      provides: "NL-ACC-02 closure text + [x]/traceability update"
      contains: "NL-ACC-02"
  key_links:
    - from: "human-run live sweep"
      to: "baseline.json predicate-grounded entry"
      via: "manual human-reviewed fold-in (never CI-auto-regenerated)"
      pattern: "predicate-grounded"
    - from: "baseline.json confirmatory_test"
      to: "REQUIREMENTS.md NL-ACC-02 closure"
      via: "McNemar p + zero-regressions match"
      pattern: "confirmatory_test"
---

<objective>
Run the credentialed live sweeps, record the results, and close NL-ACC-02.

This plan is human-run (credentialed, `RUN_EVAL`-gated, costs money) — `autonomous: false`. The
agent prepares the runbook and the exact commands; the human executes them with the key the agent
must never hold, pastes back aggregate + per-case verdicts + corpus_sha + temperature; the agent
folds the human-reviewed numbers into baseline.json and closes the requirement.

**Hard gate (D-03):** CK25 `openai-gpt4o-mini-ck25-predicate-grounded` vs the 07.3 baseline
`openai-gpt4o-mini-ck25-grounded` (14/49), paired McNemar **p<0.05, zero regressions**, run
same-session against a fresh entity-grounded arm (never the stale committed entry — the 07.3-06
discipline). **Directional (D-03, NOT a gate):** the first-ever live QALD-9-plus run — both the
already-existing `openai-gpt4o-mini-qald9plus` zero-shot arm (Pitfall 4: run it, do not author it)
and the new `openai-gpt4o-mini-qald9plus-predicate-grounded` arm — with MDE reported via power.py.
Record D-04's honest ceiling. If the CK25 gate returns null, close via the documented-null path
(the NL-FEW-02 precedent), never by hiding it.
Output: README §10, baseline.json entries, NL-ACC-02 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md
@.planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-VERIFICATION.md
@tests/nl2sparql/eval/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README §10 predicate-grounding sweep runbook</name>
  <files>tests/nl2sparql/eval/README.md</files>
  <read_first>
    - tests/nl2sparql/eval/README.md (analog: §9 entity-grounding runbook — design, temperature provenance, run-both-arms, manual fold-in, non-regression re-confirm)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Validation Architecture: exact commands; D-03 gate shape; D-04 honest ceiling; Pitfall 4 qald zero-shot exists)
  </read_first>
  <action>
    Add a §10 section mirroring §9's structure. Document: (a) the design (seam-7 predicate block,
    mechanical shape derivation, no hand-curation); (b) the exact human-run commands for the four
    live arms — the CK25 predicate-grounded arm, a fresh same-session CK25 entity-grounded arm
    (openai-gpt4o-mini-ck25-grounded) for the confirmatory pairing, the QALD zero-shot arm
    (openai-gpt4o-mini-qald9plus, already existing — run, do not create), and the QALD
    predicate-grounded arm — all via RUN_EVAL=1 NL2SPARQL_API_KEY=... uv run ..., capturing per-case
    verdicts + corpus_sha (git log -1 --format=%h -- <corpus.yml>) + temperature=0.1; (c) the CK25
    hard-gate criterion (paired McNemar p<0.05, zero regressions vs 14/49, using the fresh
    same-session entity-grounded arm, never the stale committed entry); (d) QALD framed as
    directional-only with MDE via power.py; (e) D-04's honest ceiling (~14 -> ~20-25, not 35);
    (f) the manual human-reviewed fold-in into baseline.json (never CI-auto-regenerated) and the
    non-regression re-confirm (W3C >=96.4%, scripted stays CI default). Do NOT commit any secret.
  </action>
  <verify>
    <automated>grep -c "predicate-grounded" tests/nl2sparql/eval/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README has a §10 section naming all four live arms and the exact `RUN_EVAL=1 NL2SPARQL_API_KEY=...` command shape
    - §10 states the CK25 hard-gate criterion (McNemar p<0.05 vs 14/49, zero regressions, same-session fresh arm) and frames QALD as directional-only
    - §10 records D-04's honest ceiling and the manual-fold-in + non-regression discipline
    - §10 documents the env var as NL2SPARQL_API_KEY (Pitfall 1 name), not OPENAI_API_KEY
  </acceptance_criteria>
  <done>§10 runbook present, mirrors §9, all four arms + hard-gate/directional framing + honest ceiling + manual-fold-in documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human runs the credentialed live sweeps (CK25 hard gate + QALD directional)</name>
  <files>tests/nl2sparql/eval/baseline.json</files>
  <what-built>
    Task 1's README §10 provides the exact commands. Everything scripted/deterministic is already
    green (Plans 03-04). The only remaining step needs a real generator key the agent must never
    hold, so the human runs it.
  </what-built>
  <action>
    HUMAN-RUN (agent must never hold the key). The human executes the credentialed live sweeps per
    README §10 and pastes the results back; the agent does NOT run any RUN_EVAL live command itself.
    Follow the how-to-verify steps below exactly.
  </action>
  <how-to-verify>
    1. Ensure env is current: `uv sync` then confirm `uv run python -c "from arango_query_core.nl.grounding import PredicateIndex"` exits 0.
    2. Run the CK25 arms (temperature 0.1) with your key, per README §10:
       - `RUN_EVAL=1 NL2SPARQL_API_KEY=... uv run python -c "from tests.nl2sparql.eval.runner import run, write_report; r=run('openai-gpt4o-mini-ck25-predicate-grounded'); write_report(r); print('pred', r.pass_rate); [print(c.name, c.passed) for c in r.cases]"`
       - a FRESH same-session entity-grounded arm: same command with `run('openai-gpt4o-mini-ck25-grounded')`
    3. Run the QALD arms (directional): `run('openai-gpt4o-mini-qald9plus')` (zero-shot, already exists) and `run('openai-gpt4o-mini-qald9plus-predicate-grounded')`.
    4. Capture corpus_sha per corpus: `git log -1 --format=%h -- tests/nl2sparql/eval/vendored/ck25/corpus.yml` and the qald9plus one.
    5. Paste back: CK25 both arms' aggregate pass_rate + full per-case passed verdicts; QALD both arms' aggregate; the two corpus_sha values; confirm temperature=0.1; confirm no secret is being committed.
  </how-to-verify>
  <verify>
    <human-check>Human pastes CK25 both-arm aggregates + per-case verdicts, QALD both-arm aggregates, corpus_sha values, temperature=0.1, and confirms no secret committed.</human-check>
  </verify>
  <resume-signal>Paste CK25 (both arms) + QALD (both arms) aggregates, CK25 per-case verdicts, corpus_sha values, temperature. Then type "folded" for the agent to fold in.</resume-signal>
  <done>Human has run all four live arms and pasted back both-corpus aggregates + CK25 per-case verdicts + corpus_sha + temperature; no secret committed.</done>
</task>

<task type="auto">
  <name>Task 3: Fold live numbers into baseline.json + close NL-ACC-02</name>
  <files>tests/nl2sparql/eval/baseline.json, .planning/REQUIREMENTS.md</files>
  <read_first>
    - tests/nl2sparql/eval/baseline.json (analog: openai-gpt4o-mini-ck25-grounded entry + confirmatory_test block shape; separate per-corpus entries)
    - .planning/phases/07.3-nl-to-sparql-entity-instance-grounding/07.3-VERIFICATION.md (truth 6: confirmatory_test provenance shape — McNemar b/c/p, bootstrap CI, corpus_sha, temperature, zero regressions)
    - .planning/REQUIREMENTS.md (NL-ACC-01 closure text as the shape for NL-ACC-02's closure)
  </read_first>
  <action>
    Compute the paired McNemar (via runner.py::paired_mcnemar) on the CK25 predicate-grounded vs the
    FRESH same-session entity-grounded per-case verdicts the human pasted (never the stale committed
    14/49 entry). Add a new openai-gpt4o-mini-ck25-predicate-grounded entry to baseline.json kept
    SEPARATE from the existing ck25 entries, carrying a confirmatory_test block: fresh entity-grounded
    arm pass_rate, predicate-grounded pass_rate, McNemar b/c/p_value, bootstrap paired_delta + ci_95,
    regressed_1_to_0 list, corpus_sha, temperature=0.1. Add the QALD openai-gpt4o-mini-qald9plus
    (zero-shot) and openai-gpt4o-mini-qald9plus-predicate-grounded numbers as separate directional
    entries with achieved_mde from power.py (no gate). Record D-04's honest ceiling in the entry.
    Then close NL-ACC-02 in REQUIREMENTS.md: mark [x] with closure text matching baseline.json numbers
    exactly (McNemar p, b/c, zero regressions), and set the traceability row to Complete. If the CK25
    gate returned null (p>=0.05), do NOT mark it a passed confirmatory test — close via the
    human-accepted-documented-null path (NL-FEW-02 precedent): record the honest null with exact b/c/p
    and the MDE, state the wording is provisional, and name the documented follow-up (expand render
    detail on all predicates).
  </action>
  <verify>
    <automated>grep -c "predicate-grounded" tests/nl2sparql/eval/baseline.json && uv run python -c "import json; json.load(open('tests/nl2sparql/eval/baseline.json')); print('valid json')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "predicate-grounded" tests/nl2sparql/eval/baseline.json` >= 2 (CK25 + QALD predicate-grounded entries, kept separate from existing entries)
    - baseline.json remains valid JSON; the CK25 entry has a confirmatory_test block with McNemar b/c/p_value, corpus_sha, temperature, regressed_1_to_0
    - REQUIREMENTS.md NL-ACC-02 marked `[x]` (or documented-null closure) with numbers matching baseline.json; traceability row `Complete`
    - The confirmatory pairing uses the same-session fresh entity-grounded arm, NOT the committed 14/49 entry (stated in the closure text)
  </acceptance_criteria>
  <done>Live numbers folded into baseline.json as separate entries with full provenance; NL-ACC-02 closed via the significant-lift or documented-null path with numbers matching baseline.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-held API key -> live generator | Credential the agent must never hold or log |
| Generated SPARQL + eval report -> committed baseline.json | Model output + aggregate stats written to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-03 | Information Disclosure | live sweep + baseline.json fold-in | mitigate | NL2SPARQL_API_KEY is human-held, never in agent context, never committed; baseline.json stores aggregate pass-rates + per-case verdicts + McNemar stats only, no key/header/secret; checkpoint resume-signal explicitly requires the human to confirm no secret is committed |
| T-07.4-04 | Repudiation (unreproducible / fabricated numbers) | confirmatory_test provenance | mitigate | Record corpus_sha + temperature + b/c/p + bootstrap CI + regressed list so the gate is reproducible and auditable (07.3-06 discipline); confirmatory pairing uses a same-session fresh arm, never a stale committed entry |
| T-07.4-02 | Denial of Service (cost blowup) | live QALD 514-case run | accept | Cost is a few dollars on gpt-4o-mini (D-03); dump/retrieve threshold bounds prompt size; a bounded, human-authorized one-time spend |
</threat_model>

<verification>
- README §10 documents all four arms, the hard-gate/directional split, honest ceiling, manual fold-in.
- Human runs the credentialed sweeps; agent never holds the key.
- baseline.json carries separate CK25 + QALD predicate-grounded entries with full provenance; valid JSON.
- NL-ACC-02 closed with numbers matching baseline.json (significant-lift or documented-null).
- Non-regression re-confirm noted: W3C >=96.4%, scripted stays CI default.
</verification>

<success_criteria>
- CK25 hard gate recorded: McNemar p<0.05, zero regressions vs 14/49 (or an honest documented null).
- QALD first-ever live numbers recorded as directional (not a gate) with MDE.
- baseline.json entries separate + reproducible; NL-ACC-02 closed; traceability Complete.
</success_criteria>

<output>
Create `.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-05-SUMMARY.md` when done. Record the CK25 gate outcome (p, b/c, regressions), the QALD directional numbers + MDE, and the closure path taken.
</output>
