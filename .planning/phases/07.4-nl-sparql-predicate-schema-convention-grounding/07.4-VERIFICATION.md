---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
verified: 2026-07-27T23:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 07.4: NL→SPARQL Predicate/Schema-Convention Grounding Verification Report

**Phase Goal:** Extend the grounding seam from instance-entity grounding to predicate/schema-convention
grounding (walk the OWL/RDFS TBox, surface schema predicates with label/domain/range/shape, inject them
via a language-agnostic engine seam so the model binds to real predicates and follows schema
conventions), and prove a statistically-graded execution-graded CK25 accuracy lift over the 07.3
entity-grounded baseline.

**Verified:** 2026-07-27T23:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Verification Framing (read before the tables below)

NL-ACC-02 has a designed two-outcome acceptance (NL-FEW-02 precedent): a significant positive lift
closes it as a confirmatory pass, OR a rigorously-run, honestly-recorded NULL closes it via the
documented-null path. The empirical result here IS a null (additive arm 10/49 vs fresh entity-alone
12/49, McNemar p=0.6875; standalone arm 7/49 vs the same fresh 12/49, p=0.1250). This is treated as the
phase's designed acceptable outcome, not a failure — the goal-backward question this report answers is
(a) was the seam-7 machinery actually built and is it live, and (b) was the CK25 experiment that produced
the null actually valid (not confounded by the CR-01 dump-mode defect the code review caught).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Seam 7 (`predicate_index`/`predicate_prompt_section`) is a real, language-agnostic member of `QueryLanguageAdapter` in `arango_query_core` | VERIFIED | `arango_query_core/nl/seams.py:125,131` define both members on the `Protocol`; importable and present (checked directly against the sibling checkout, pin `b669320…`). |
| 2 | `PredicateIndex` mirrors `LabelIndex` (caller-owned, no memoization, shared token-substring scorer) | VERIFIED | `grounding.py:80-129` (`_token_substring_retrieve`, shared by both classes), `grounding.py:261-` (`PredicateIndex.retrieve`/`format_prompt_section`) mirror `LabelIndex` shape exactly. |
| 3 | Engine composes the predicate block after the entity block, inside `_system_prompt`, post-cache-boundary | VERIFIED | `engine.py:120-136`: entity block composed first (line 131), predicate block second (lines 134-136), both inside `_system_prompt`; `predicate_k` field (line 72) sits alongside `grounding_k`. |
| 4 | NL-ACC-02 exists in REQUIREMENTS.md mirroring NL-ACC-01's acceptance shape | VERIFIED | `.planning/REQUIREMENTS.md:54`; traceability row `NL-ACC-02 \| Phase 07.4 \| Complete`; Coverage `Unmapped: 0`. |
| 5 | Both SPARQL adapters implement seam 7 with byte-identical rendered output, dump-vs-retrieve mode selection (D-01) | VERIFIED | `adapter.py:144-178`, `engine_adapter.py:292-334` — executable logic (mode selection, `dump=is_dump` kwarg) byte-identical; `tests/nl2sparql/test_grounding_prompt_parity.py` passes (4 tests). Diff between the two files' predicate blocks shows only comment text differs. |
| 6 | CR-01 (dump-mode defect: widening `k` never actually dumped the schema) was fixed upstream, not merely documented around | VERIFIED | `arango_query_core/nl/grounding.py:86,101-111,126` — `dump: bool = False` kwarg genuinely bypasses the `if hits:` zero-hit filter (`if hits or dump:`); both adapters pass `dump=is_dump` (`adapter.py:178`, `engine_adapter.py:334`); pin bumped to `b66932046a102898e8fff205a7ddcbedfb2c896e` at both `pyproject.toml` extras sites (verified — not the old `8adc0de`/`c6ae5e1`). |
| 7 | The CK25 lift hypothesis was tested on a VALID (unconfounded) experiment and the honest null result recorded, closing NL-ACC-02 | VERIFIED | `baseline.json` additive-arm and standalone-arm entries carry the re-fold numbers (10/49 p=0.6875; 7/49 p=0.1250) run against the fixed dump-mode pin, explicitly marked "SUPERSEDES" the confounded 07.4-05 numbers (13/49-vs-13/49 p=1.0; 5/49 p=0.0156). Independently recomputed both McNemar results via `runner.py::paired_mcnemar` against the embedded per-case dicts — b=2/c=4/p=0.6875 (additive) and b=1/c=6/p=0.125 (standalone) match exactly. `cr_01_confounded_note` is marked SUPERSEDED, not left "provisional pending re-run." REQUIREMENTS.md closure text matches baseline.json numbers verbatim and says "CLOSED... on a VALID experiment," never "provisional." |
| 8 | Non-regression guardrails hold: W3C DAWG ≥96.4%, transpiler package unchanged, scripted configs remain CI default | VERIFIED | `pytest tests/w3c/test_coverage_gate.py` → 1 passed; `git diff --stat -- arango_sparql/translate/` empty (no commits touched it this phase); `RUN_EVAL=1 pytest tests/nl2sparql/eval/test_eval.py -k ci_gate_only_ever_runs_scripted` → 1 passed; full suite `1751 passed, 797 skipped, 23 xfailed` (matches SUMMARY claim exactly). |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `arango_query_core/nl/grounding.py` (sibling repo) | `GroundedPredicate`/`PredicateIndex` + shared scorer + real `dump=True` bypass | VERIFIED | Present at pin `b669320…`; `dump` kwarg threaded through `_token_substring_retrieve` → `retrieve` → `format_prompt_section`; docstring explicitly documents the CR-01 fix. |
| `arango_query_core/nl/seams.py` (sibling repo) | Seam-7 Protocol members | VERIFIED | `predicate_index`/`predicate_prompt_section` on `QueryLanguageAdapter`. |
| `arango_query_core/nl/engine.py` (sibling repo) | Predicate block composed after entity block | VERIFIED | `_system_prompt` lines 120-136. |
| `arango_sparql/nl2sparql/adapter.py` | Seam-7 implementation, `dump=is_dump` wiring | VERIFIED | Lines 144-178. |
| `arango_sparql/nl2sparql/engine_adapter.py` | Seam-7 implementation, byte-identical executable logic | VERIFIED | Lines 292-334; parity test green. |
| `pyproject.toml` | Pin at `b66932046a102898e8fff205a7ddcbedfb2c896e`, both `nl`/`nl[dense]` extras | VERIFIED | Confirmed via grep, both lines 65 and 75. |
| `tests/nl2sparql/eval/baseline.json` | Valid re-fold CK25 numbers, `cr_01_confounded_note` superseded | VERIFIED | Confirmed via direct JSON read + independent McNemar recomputation. |
| `.planning/REQUIREMENTS.md` | NL-ACC-02 `[x]`, closure text matches baseline.json, no "provisional" language | VERIFIED | Line 54; traceability row Complete; Coverage Unmapped: 0. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `NLQueryEngine._system_prompt` | `adapter.predicate_index()`/`predicate_prompt_section()` | seam-7 composition after entity block | WIRED | `engine.py:134-136`. |
| `adapter.py`/`engine_adapter.py` `predicate_prompt_section` | `PredicateIndex.retrieve(dump=...)` | `dump=is_dump` kwarg | WIRED | Both adapters compute `is_dump = 0 < total <= PREDICATE_DUMP_THRESHOLD` and pass it through; genuinely bypasses the zero-hit filter (verified in `grounding.py:126`). |
| Human-run live sweep | `baseline.json` predicate-grounded entries | manual human-reviewed fold-in (07.4-06 re-fold) | WIRED | Numbers in `baseline.json` match REQUIREMENTS.md closure text exactly; per-case dicts independently recomputed to the same b/c/p. |
| `baseline.json` confirmatory_test | REQUIREMENTS.md NL-ACC-02 closure | McNemar p + zero-regressions match | WIRED | Verbatim numeric match confirmed (10/49, p=0.6875, b=2/c=4; 7/49, p=0.125, b=1/c=6). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Scripted CK25 predicate-grounded plumbing gate | `run('scripted-ck25-predicate-grounded')` | pass_rate=1.0, 49/49 | PASS |
| Scripted QALD-9-plus predicate-grounded plumbing gate | `run('scripted-qald9plus-predicate-grounded')` | pass_rate=1.0, 514/514 | PASS |
| Scripted CK25 additive (entity+predicate) plumbing gate | `run('scripted-ck25-grounded-predicate-grounded')` | pass_rate=1.0, 49/49 | PASS |
| Independent recompute of additive-arm McNemar | `paired_mcnemar(entity_alone_fresh_cases, additive_arm_cases)` | (b=2, c=4, p=0.6875) | PASS — matches recorded |
| Full test suite | `pytest -q` | 1751 passed, 797 skipped, 23 xfailed | PASS — matches SUMMARY claim |
| W3C DAWG coverage gate | `pytest tests/w3c/test_coverage_gate.py -q` | 1 passed | PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No `TBD`/`FIXME`/`XXX` found in any file touched this phase (both repos) | — | none |
| `tests/nl2sparql/eval/grounding_index_builder.py:263-302` | WR-01 (07.4-REVIEW.md) | multi-valued `rdfs:domain`/`range`/`label` silently collapsed to one value | INFO | Eval-only harness, zero occurrences in either vendored corpus today; documented in REVIEW.md as a WARNING, not a phase-goal blocker — does not affect NL-ACC-02's disposition. |
| `tests/nl2sparql/eval/grounding_index_builder.py:283-292` | WR-02 (07.4-REVIEW.md) | unvalidated IRI interpolation into SPARQL query text | INFO | Pre-existing pattern (shared with `build_label_index`), eval-only, no untrusted input reaches this path today. |

Both WR-01/WR-02 are pre-existing-pattern, eval-harness-only warnings the code review correctly
classified below CRITICAL; they do not block the phase goal (seam-7 built + valid null recorded) and
were left open by design (07.4-REVIEW.md), consistent with the phase's scope.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|--------------|--------------|--------------|--------|----------|
| NL-ACC-02 | 07.4-01, 02, 03, 04, 05 (06 is a gap-closure/CR-01 fix, not a new requirements-bearing plan) | Predicate/schema-convention grounding lift, execution-graded | SATISFIED (documented-null closure on a valid experiment) | `.planning/REQUIREMENTS.md:54`; `baseline.json` re-fold entries; independently recomputed McNemar stats match. |

No orphaned requirements: REQUIREMENTS.md Coverage block shows `Unmapped: 0`, and NL-ACC-02 is the
phase's sole declared requirement across all plans (05's frontmatter declares it; 06 is an undeclared
gap-closure plan correctly not re-claiming the requirement id, consistent with 07.4-02's precedent of
not prematurely closing a requirement it only plumbs).

### Human Verification Required

None. All observable truths, artifacts, and key links were verifiable directly against the codebase
(sibling-repo source at the pinned commit, this repo's adapters, `pyproject.toml`, `baseline.json`,
`REQUIREMENTS.md`) plus reproducible test/computation runs. The credentialed live sweep itself was
necessarily human-run (agent must never hold `NL2SPARQL_API_KEY`), but its *output* — the per-case
verdicts folded into `baseline.json` — was independently re-verified by recomputing the paired McNemar
statistic from the embedded per-case dicts and confirming an exact match to the recorded b/c/p values,
which is the strongest verification available short of re-running the live LLM calls.

### Gaps Summary

None. All 8 derived must-haves (roadmap goal decomposed: seam-7 built + wired, dump-mode fix real,
experiment valid, honest null recorded, non-regression held) verified directly against the codebase.
The code-review BLOCKER (CR-01) was confirmed fixed at the root cause (real `dump=True` escape hatch in
the shared upstream scorer, not a superficial patch), the pin was confirmed bumped to the fixed commit
at both extras sites, and the confounded numbers were confirmed overwritten (not merely annotated) with
a validly re-run, statistically re-verified null. The two REVIEW.md WARNING items (WR-01/WR-02) are
scoped to the eval-only TBox-walking harness, do not affect production code or the NL-ACC-02 disposition,
and were correctly left open as documented, non-blocking findings.

---

*Verified: 2026-07-27T23:00:00Z*
*Verifier: Claude (gsd-verifier)*
