---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
plan: 06
subsystem: nl2sparql
tags: [arango-query-core, grounding, predicate-index, dump-mode, cr-01, code-review-fix]

# Dependency graph
requires:
  - phase: 07.4-nl-sparql-predicate-schema-convention-grounding
    provides: seam-7 PredicateIndex/predicate_prompt_section (07.4-01/02), both adapters wired to D-01 dump-vs-retrieve threshold (07.4-02), NL-ACC-02 documented-null closure (07.4-05)
provides:
  - Real PredicateIndex dump mode upstream (arango_query_core.nl.grounding, dump=True kwarg on retrieve()/format_prompt_section()) that bypasses the shared scorer's zero-hit filter
  - Both arango-sparql-py adapters (adapter.py, engine_adapter.py) wired to pass dump=is_dump byte-identically
  - Provisional/confounded annotations on the existing NL-ACC-02 documented-null disposition (REQUIREMENTS.md, baseline.json, eval README) pending a re-run
affects: [07.4-followup-live-sweep, any future phase touching PredicateIndex/seam-7]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Genuine 'dump' retrieval mode as an explicit boolean kwarg threaded through a shared scorer, rather than trying to fake it by widening k against a filter that silently drops zero-hit items"
    - "Annotate a confounded/provisional finding in-place (additive JSON key, inline doc note) rather than reversing or deleting the prior recorded verdict — the re-run, not this bug-fix plan, decides the final disposition"

key-files:
  created: []
  modified:
    - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py
    - /Users/plosiewicz/Desktop/arango-query-core/tests/test_nl_grounding.py
    - pyproject.toml
    - uv.lock
    - arango_sparql/nl2sparql/adapter.py
    - arango_sparql/nl2sparql/engine_adapter.py
    - .planning/REQUIREMENTS.md
    - tests/nl2sparql/eval/README.md
    - tests/nl2sparql/eval/baseline.json

key-decisions:
  - "dump is a keyword-only bool threaded through _token_substring_retrieve -> PredicateIndex.retrieve -> PredicateIndex.format_prompt_section, default False, so LabelIndex/entity grounding (seam 6) is byte-behavior-identical (it never passes dump)"
  - "dump=True only removes the zero-hit filter; it does not override k as a cap, and real hits still rank before zero-hit items (same (hits, -best_len) sort key) -- verified with a fixture where dump=True + a smaller k still truncates, and a partial-match question still ranks the real hit first"
  - "Rewrote the upstream test that misleadingly asserted 'k >= len(predicates) returns all predicates' -- it only passed because its fixture question happened to token-match every predicate; the corrected version proves the same k has no effect on a zero-overlap question absent dump=True"
  - "NL-ACC-02's existing documented-null closure (07.4-05) is left in place, not reversed -- annotated as provisional/confounded in REQUIREMENTS.md, baseline.json (new cr_01_confounded_note key), and the eval README, since only a fresh credentialed live sweep (a separate human checkpoint) can settle whether the CR-01 fix changes the verdict"
  - "No live/credentialed RUN_EVAL sweep executed by this plan, per explicit instruction -- that re-run is a human checkpoint outside this plan's scope"

requirements-completed: []

duration: 32min
completed: 2026-07-27
---

# Phase 07.4 Plan 06: Real predicate dump mode (CR-01 gap-closure fix) Summary

**Upstream `arango_query_core.nl.grounding.PredicateIndex` gains a genuine `dump=True` bypass for its zero-hit token filter (commit `b669320`, pushed to both remotes), re-pinned into arango-sparql-py, and wired into both adapters — closing code-review BLOCKER CR-01 where widening `k` alone never actually dumped CK25's full 30-predicate schema.**

## Performance

- **Duration:** ~32 min
- **Started:** 2026-07-27T14:23:00Z (approx, following 07.4-REVIEW.md's commit)
- **Completed:** 2026-07-27T21:54:32Z
- **Tasks:** 4 (upstream TDD fix, pin bump, adapter wiring, offline verification + doc annotation)
- **Files modified:** 9 (2 upstream, 7 in this repo)

## Accomplishments

- Root-caused and fixed the actual bug behind CR-01: `_token_substring_retrieve`'s `if hits:` guard dropped every zero-overlap predicate regardless of `k`, so "dump mode" (widening `k` to the total predicate count) was a no-op — a real CK25 question only ever surfaced 1–8 of 30 predicates.
- Added a real `dump: bool = False` escape hatch upstream in `arango_query_core.nl.grounding`, TDD RED→GREEN, with `LabelIndex`/entity grounding (seam 6) proven byte-behavior-unchanged.
- Rewrote the misleading upstream unit test that only "proved" k-alone dumping because its fixture question happened to token-match every predicate in the set.
- Pushed the fix to both `arthurkeen` and `origin` remotes (SHA `b66932046a102898e8fff205a7ddcbedfb2c896e`), verified fetchable via `git ls-remote` on both before re-pinning.
- Bumped this repo's pin at both `pyproject.toml` extras sites (`nl` and `nl[dense]`), re-locked with `uv lock`, re-synced with `uv sync --all-extras`, confirmed `PredicateIndex` importable.
- Wired `predicate_prompt_section` in both `adapter.py` and `engine_adapter.py` to pass `dump=is_dump`, keeping the two adapters' rendered output and executable logic byte-identical (comments still legitimately differ, as they did pre-existing).
- Re-ran all required offline verification: both scripted plumbing gates (CK25 predicate-grounded, CK25 grounded+predicate-grounded, QALD-9-plus predicate-grounded — all `pass_rate=1.0`), the cross-adapter parity test, the engine prompt test, the full `tests/nl2sparql/` suite (179 passed / 603 skipped, unchanged from the review's baseline), and the W3C coverage gate (green). `arango_sparql/translate/` diff is empty.
- Annotated (not reversed) the existing NL-ACC-02 documented-null closure as provisional/confounded in `REQUIREMENTS.md`, `baseline.json`, and the eval README, since the recorded null ran against the broken dump mode and needs re-confirmation by a fresh live sweep.
- Did **not** run any credentialed/live `RUN_EVAL` sweep — that re-run is a separate human checkpoint, per explicit instruction.

## Task Commits

Upstream (`arango-query-core`, pushed to both `arthurkeen` and `origin` remotes as `b669320`):

1. **Add failing tests for real PredicateIndex dump mode** — `3411c1b` (test)
2. **Add real PredicateIndex dump mode (CR-01 fix)** — `b669320` (feat, includes a post-rebase ruff-format amend)

This repo (`arango-sparql-py`):

3. **Bump arango-query-core pin to b669320** — `7599de3` (chore) — `pyproject.toml` (both extras sites), `uv.lock`
4. **Wire both adapters to real predicate dump mode** — `1e35107` (fix) — `arango_sparql/nl2sparql/adapter.py`, `arango_sparql/nl2sparql/engine_adapter.py`
5. **Flag NL-ACC-02 documented-null as provisional pending re-run** — `72c9933` (docs) — `.planning/REQUIREMENTS.md`, `tests/nl2sparql/eval/README.md`, `tests/nl2sparql/eval/baseline.json`

**Plan metadata:** (this commit, following SUMMARY.md creation)

## Files Created/Modified

- `/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py` — added `dump: bool = False` kwarg to `_token_substring_retrieve`, `PredicateIndex.retrieve`, `PredicateIndex.format_prompt_section`; corrected the stale class docstring claim that widening `k` alone achieves dump mode
- `/Users/plosiewicz/Desktop/arango-query-core/tests/test_nl_grounding.py` — new/rewritten tests proving the dump contract (zero-overlap question + `dump=True` returns everything; `dump=False` still filters; `LabelIndex` unaffected)
- `pyproject.toml` — both `arango-query-core` git pins (`nl`, `nl[dense]` extras) bumped `8adc0de...` → `b669320...`
- `uv.lock` — relocked against the new pin
- `arango_sparql/nl2sparql/adapter.py` — `predicate_prompt_section` now computes `is_dump` and passes `dump=is_dump`
- `arango_sparql/nl2sparql/engine_adapter.py` — identical wiring, byte-identical executable logic to `adapter.py`
- `.planning/REQUIREMENTS.md` — NL-ACC-02 entry gets a provisional/confounded footnote (checkbox/table status left untouched — this plan does not close or reopen the requirement)
- `tests/nl2sparql/eval/README.md` — §10 dump-mode section gets a CR-01 correction note
- `tests/nl2sparql/eval/baseline.json` — `phase07_4_predicate_grounding_sweep` gets an additive `cr_01_confounded_note` key (existing recorded numbers/verdicts unchanged)

## Decisions Made

- Threaded `dump` as a keyword-only bool through the shared scorer rather than adding a separate dump-only method — keeps `LabelIndex` and `PredicateIndex` on the exact same code path, with `dump=False` as the default preserving byte-identical prior behavior for both.
- Used a question with no shared domain token (`"what is the price"`, omitting "product") when testing that dump-mode ranking still favors real hits — an earlier draft used a question containing "product," which both the price and category fixture predicates matched via their shared `domain="Product"`, making the assertion fragile/coincidental rather than a clean test of the ranking rule.
- Rebased the two new upstream commits onto a `style: ruff format` commit that landed on `main` between the initial fetch and the push (both remotes had already advanced past the previously-recorded `8adc0de` pin) rather than force-pushing — this is the correct, non-destructive way to publish onto a shared branch that moved.
- Chose to annotate rather than reverse the existing NL-ACC-02 documented-null closure: the plan's instructions were explicit that no live sweep should run in this session, and unilaterally flipping a requirement's closed status without new experimental evidence would be a bigger claim than the fix itself supports.

## Deviations from Plan

**1. [Rule 1 - Bug] Fixed a flaky dump-mode ranking assertion before it was ever committed**
- **Found during:** writing the upstream `test_predicate_retrieve_dump_true_returns_every_predicate_regardless_of_hits` test
- **Issue:** the question `"what is the price of the product"` gave both `_price_predicate()` and `_category_predicate()` a real token hit (both share `domain="Product"`), and the tiebreak (shorter matching field wins) put the category predicate first, not price — the test's own premise ("the only real hit ranks first") was false for that fixture
- **Fix:** switched the ranking assertion to `"what is the price"` (no "product" token), which unambiguously hits only the price predicate
- **Files modified:** `/Users/plosiewicz/Desktop/arango-query-core/tests/test_nl_grounding.py` (never committed with the broken assertion — caught and fixed pre-commit)
- **Commit:** `3411c1b`

**2. [Rule 3 - Blocking] Rebased local upstream commits onto a remote-only formatting commit**
- **Found during:** `git push arthurkeen main` (first attempt)
- **Issue:** both `arthurkeen` and `origin` remotes had advanced to a `style: ruff format` commit (`57d639e`) past the locally-checked-out `8adc0de`, rejecting the push as a non-fast-forward
- **Fix:** `git rebase arthurkeen/main`, re-ran `ruff format` on the touched file (one line needed re-wrapping to satisfy the now-current formatter), amended the still-unpushed feat commit, re-ran the full upstream test suite (103 passed), then pushed
- **Files modified:** `/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py` (formatting only)
- **Verification:** `pytest tests/` (103 passed), `ruff check .` / `ruff format --check .` clean
- **Committed in:** `b669320`

---

**Total deviations:** 2 auto-fixed (1 bug caught pre-commit, 1 blocking git-workflow issue). Both routine; no scope creep.
**Impact on plan:** None on scope — both were resolved before any commit landed with the defect.

## Issues Encountered

None beyond the two deviations above.

## User Setup Required

None — no external service configuration required. The credentialed live CK25/QALD re-sweep this fix motivates is intentionally NOT part of this plan; it is a separate human checkpoint (see Next Phase Readiness).

## Next Phase Readiness

- **Ready:** the real dump-mode fix is live, pinned, wired into both adapters, and verified offline (scripted plumbing gates, parity test, full `tests/nl2sparql/` suite, W3C gate all green; `arango_sparql/translate/` untouched).
- **Blocker/concern (human checkpoint required):** the NL-ACC-02 documented-null verdict recorded in `baseline.json`/`REQUIREMENTS.md` (07.4-05) was measured against the broken dump mode and is now flagged provisional/confounded. A fresh credentialed live sweep — `openai-gpt4o-mini-ck25-predicate-grounded` (standalone) and the additive `openai-gpt4o-mini-ck25-grounded-predicate-grounded` arm, both re-paired against a same-session fresh entity-alone arm exactly as 07.4-05 did — is needed to settle whether the real dump mode changes the verdict. This plan deliberately does not run it (no `NL2SPARQL_API_KEY` held by the agent).
- No other blockers identified.

## Self-Check: PASSED

- All key files confirmed present on disk (this repo + sibling `arango-query-core` checkout).
- All commit hashes confirmed in `git log` (this repo: `7599de3`, `1e35107`, `72c9933`, `bdc8ef3`; `arango-query-core`: `3411c1b`, `b669320`).
- Both remotes' `main` confirmed at `b66932046a102898e8fff205a7ddcbedfb2c896e` via `git ls-remote`.

---
*Phase: 07.4-nl-sparql-predicate-schema-convention-grounding*
*Completed: 2026-07-27*
