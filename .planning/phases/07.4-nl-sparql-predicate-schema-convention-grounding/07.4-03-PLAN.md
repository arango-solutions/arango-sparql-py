---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
plan: 03
type: execute
wave: 3
depends_on: ["07.4-02"]
files_modified:
  - tests/nl2sparql/eval/grounding_index_builder.py
  - tests/nl2sparql/eval/test_grounding_index_builder.py
autonomous: true
requirements: [NL-ACC-02]
must_haves:
  truths:
    - "build_predicate_index() walks the TBox (function-local pyoxigraph) and returns a PredicateIndex with label/domain/range/kind/shape per predicate"
    - "The corrected 3-way shape rule classifies mechanically: value_object requires the range class's own (exact-domain-match) properties be ALL datatype; category_instance = zero own children; linked_entity = has an object-property child; literal = datatype predicate"
    - "No hand-authored per-schema hints: no 'price'/'hasCategory' string literals in the classification logic (D-02)"
    - "pyoxigraph stays confined to tests/ via function-local imports (D-08)"
    - "The mechanical builder runs on QALD's 250-property DBpedia subset (retrieve path exercised) and its shape-distribution is reported honestly (RESEARCH OQ1 resolved; A3 de-risked before the live QALD sweep)"
  artifacts:
    - path: "tests/nl2sparql/eval/grounding_index_builder.py"
      provides: "build_predicate_index(ontology_ttl) sibling to build_label_index"
      contains: "def build_predicate_index"
    - path: "tests/nl2sparql/eval/test_grounding_index_builder.py"
      provides: "shape-derivation precision/purity tests (Price/ProductCategory/Manager/literal)"
      contains: "value_object"
  key_links:
    - from: "build_predicate_index"
      to: "arango_query_core.nl.grounding.GroundedPredicate / PredicateIndex"
      via: "function-local import at return"
      pattern: "from arango_query_core.nl.grounding import"
    - from: "build_predicate_index"
      to: "tests.helpers.oxi"
      via: "function-local pyoxigraph import"
      pattern: "from tests.helpers.oxi import"
---

<objective>
Add the eval-only TBox predicate-index builder `build_predicate_index()` — the inverse walk to
07.3's `build_label_index()` — and its shape-derivation precision/purity unit tests, applying the
RESEARCH-corrected 3-way shape rule mechanically (D-02).

Purpose: turn a corpus's `ontology:` Turtle TBox into a `PredicateIndex` of `GroundedPredicate`s
with mechanically-derived usage shapes, so the eval harness (Plan 04) can inject the seam-7 block.
The corrected rule (exact-domain-match, all-datatype-children) is the crux: it must classify
`pv:price`→value_object and `pv:hasCategory`→category_instance while correctly NOT firing
value_object on `pv:hasManager`→`pv:Manager` (whose child `pv:hasDirectReport` is an
ObjectProperty) — the exact false positive RESEARCH discovered and fixed.
Output: `build_predicate_index()` + discriminating shape tests, all offline/deterministic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_predicate_index() — two-pass TBox walk + corrected 3-way shape rule</name>
  <files>tests/nl2sparql/eval/grounding_index_builder.py</files>
  <read_first>
    - tests/nl2sparql/eval/grounding_index_builder.py (analog: build_label_index full file — module docstring/confinement lines 1-20, function-local pyoxigraph import lines 107-108/141, SPARQL f-string style lines 110-119, CR-02 label normalization lines 127-139, _expand_predicate/_PREFIXES lines 31-53)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Pattern 1 corrected 3-way rule + Manager/hasDirectReport walkthrough; Pattern 2 two-pass query; Anti-Patterns: no subClassOf walk, no hardcoded price/hasCategory)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (grounding_index_builder.py section: confinement + query-style + label-normalization patterns to reuse)
    - tests/nl2sparql/eval/vendored/ck25/raw/prod-inst-schema.ttl (the real TBox the rule was verified against)
  </read_first>
  <behavior>
    - Test (in Task 2): pv:price (range pv:Price; children pv:amount+pv:currency, both datatype) -> shape "value_object", shape_detail carries the child (label, range) pairs.
    - Test: pv:hasCategory (range pv:ProductCategory; zero own children) -> shape "category_instance".
    - Test: pv:hasManager (range pv:Manager; child pv:hasDirectReport is an ObjectProperty) -> shape "linked_entity", NOT "value_object" (the false-positive guard).
    - Test: a datatype property (e.g. pv:name) -> shape "literal".
  </behavior>
  <action>
    Add `build_predicate_index(ontology_ttl: str) -> PredicateIndex` as a sibling function to
    build_label_index in the same file. Import `load_store_from_string`/`oxi_query` from
    tests.helpers.oxi and `GroundedPredicate`/`PredicateIndex` from arango_query_core.nl.grounding
    **function-locally** (never module top-level — D-08 confinement). Copy build_label_index's
    module-docstring confinement framing for the new function's docstring. Run the two-pass query
    from RESEARCH Pattern 2: Pass 1 selects every owl:ObjectProperty/owl:DatatypeProperty with its
    own rdfs:label/rdfs:domain/rdfs:range (BIND kind "object"/"datatype"); Pass 2 runs once per
    distinct object-property range class C selecting that class's children (properties with an
    EXPLICITLY declared rdfs:domain == C — exact match only, NEVER walk rdfs:subClassOf). Apply the
    corrected 3-way shape rule: datatype property -> "literal"; object property whose range class C
    has non-empty children ALL of kind datatype -> "value_object" (shape_detail = child (label,range)
    pairs); C has zero children -> "category_instance"; C has children including >=1 object property
    -> "linked_entity". Reuse the CR-02 label normalization
    (`.split('"^^')[0].strip('"').split('"@')[0]`) exactly and the existing `_expand_predicate`
    helper for any prefixed IRI. Do NOT hardcode "price"/"hasCategory" anywhere — the mechanical
    rule is the sole source of classification.
  </action>
  <verify>
    <automated>uv run python -c "from tests.nl2sparql.eval.grounding_index_builder import build_predicate_index; ttl=open('tests/nl2sparql/eval/vendored/ck25/raw/prod-inst-schema.ttl').read(); idx=build_predicate_index(ttl); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_predicate_index" tests/nl2sparql/eval/grounding_index_builder.py` == 1
    - No module-level pyoxigraph import: `grep -n "^import pyoxigraph\|^from pyoxigraph\|^from tests.helpers.oxi" tests/nl2sparql/eval/grounding_index_builder.py` returns nothing (imports are function-local)
    - No subClassOf walk in the shape logic: `grep -c "subClassOf" tests/nl2sparql/eval/grounding_index_builder.py` == 0
    - No hardcoded convention strings in build_predicate_index: `grep -v '^\s*#' tests/nl2sparql/eval/grounding_index_builder.py | grep -ciE '"price"|"hascategory"|hasCategory'` == 0
    - The verify command prints `ok` (builder runs against the real CK25 TBox without crashing)
  </acceptance_criteria>
  <done>build_predicate_index walks the CK25 TBox with function-local pyoxigraph, classifies shapes via the corrected exact-domain-match / all-datatype-children rule, zero hardcoded conventions, zero subClassOf walk.</done>
</task>

<task type="auto">
  <name>Task 2: Shape-derivation precision/purity tests (Price / ProductCategory / Manager / literal)</name>
  <files>tests/nl2sparql/eval/test_grounding_index_builder.py</files>
  <read_first>
    - tests/nl2sparql/eval/test_grounding_index_builder.py (analog: full file — small hand-rolled Turtle fixture + builder-driven assertions, lines 31-93)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (test_grounding_index_builder.py section: the four discriminating fixtures to encode)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Wave 0 Gap #1: Price/ProductCategory/Manager discriminating fixture set)
  </read_first>
  <action>
    Add sibling test functions using a small hand-rolled TBox fixture (not an instance graph) that
    reproduces the four discriminating cases, driving them through build_predicate_index and
    asserting the resulting GroundedPredicate.shape: (1) a Price-shaped object property whose range
    class has ONLY datatype-property children -> assert shape == "value_object" and shape_detail
    non-empty; (2) a ProductCategory-shaped object property whose range class has ZERO own children
    -> assert shape == "category_instance"; (3) a Manager-shaped object property whose range class
    has an ObjectProperty child (e.g. hasDirectReport) -> assert shape == "linked_entity" and
    explicitly assert shape != "value_object" (the false-positive guard RESEARCH found and fixed);
    (4) a datatype property -> assert shape == "literal". Keep the tests offline/deterministic
    (no RUN_EVAL, no network), matching this file's existing placement discipline.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -k predicate -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -k predicate -q` exits 0
    - Test source asserts all four shapes AND explicitly asserts the Manager case is NOT value_object
    - Tests carry no `pytest.mark.eval` / no RUN_EVAL gate (always-on CI tier)
  </acceptance_criteria>
  <done>Four discriminating shape cases (value_object/category_instance/linked_entity-guard/literal) encoded and green, offline.</done>
</task>

<task type="auto">
  <name>Task 3: QALD DBpedia shape-distribution smoke check (resolves RESEARCH OQ1; Assumption A3 guard)</name>
  <files>tests/nl2sparql/eval/test_grounding_index_builder.py</files>
  <read_first>
    - tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl (the 250-property DBpedia subset — the retrieve-path fixture, ~6x over PREDICATE_DUMP_THRESHOLD)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Open Questions OQ1 — run the mechanical builder against dbpedia_subset.ttl and report shape-distribution counts HONESTLY; a "0 value_object, N category_instance" result is a valid finding, NOT a bug; Assumption A3 — corrected rule generalizes to DBpedia is unverified before this)
  </read_first>
  <action>
    Add an offline test function that runs `build_predicate_index()` against
    `tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl` and (a) asserts it returns a
    PredicateIndex with >0 predicates and that the predicate count exceeds PREDICATE_DUMP_THRESHOLD
    (40) — proving the mechanical builder runs on a real large DBpedia-style schema and exercises
    the retrieve (not dump) path; (b) computes the shape-distribution counts
    (value_object / category_instance / linked_entity / literal) and records them (print +
    record in the SUMMARY). Do NOT assert specific per-shape counts — per OQ1 a result of
    "0 value_object" on DBpedia is an expected, honest finding (DBpedia's broad/shallow ontology
    style differs from CK25's structured product vocabulary), not a failure to chase. Keep it
    offline/deterministic (no RUN_EVAL, no network). This closes RESEARCH OQ1 and gives A3 an
    early, pre-live-sweep signal.
  </action>
  <verify>
    <automated>uv run python -c "from tests.nl2sparql.eval.grounding_index_builder import build_predicate_index; ttl=open('tests/nl2sparql/eval/vendored/qald9plus/dbpedia_subset.ttl').read(); idx=build_predicate_index(ttl); n=len(list(idx.retrieve('', 10**6))); assert n>40, n; print('qald predicates:', n)"; uv run pytest tests/nl2sparql/eval/test_grounding_index_builder.py -k "predicate and qald" -q</automated>
  </verify>
  <acceptance_criteria>
    - build_predicate_index runs against dbpedia_subset.ttl without crashing and returns >40 predicates (retrieve path exercised on a real fixture)
    - The test computes and records the four shape-distribution counts; it does NOT hard-assert specific per-shape counts (OQ1: a 0-value_object DBpedia result is a valid honest finding)
    - The actual QALD shape-distribution counts are recorded in the SUMMARY, and RESEARCH.md OQ1 is marked RESOLVED pointing at this task's SUMMARY
    - Test is offline (no RUN_EVAL / no pytest.mark.eval)
  </acceptance_criteria>
  <done>Mechanical builder proven to run on QALD's 250-property DBpedia subset (retrieve path), shape-distribution counts recorded honestly, RESEARCH OQ1 resolved before the live QALD sweep (A3 de-risked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ontology Turtle text -> in-memory RDF store | TBox parsed by pyoxigraph inside tests/ only |
| TBox label text -> GroundedPredicate.label -> LLM prompt | labels flow toward the prompt via the builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-01 | Tampering (prompt injection) | build_predicate_index label capture | mitigate | Labels are sanitized at render time by PredicateIndex.format_prompt_section (Plan 01 _sanitize_label); the builder normalizes literal syntax (CR-02) but the C0/length mitigation lives in the shared renderer, not duplicated here |
| T-07.4-D8 | Elevation (packaging boundary breach) | pyoxigraph import path | mitigate | Every pyoxigraph-touching import stays function-local (D-08); grep gate asserts no module-level pyoxigraph import so arango_sparql/arango_query_core never gain a transitive pyoxigraph path |
</threat_model>

<verification>
- build_predicate_index runs against the real CK25 TBox and returns a PredicateIndex.
- Corrected 3-way rule verified by the four discriminating shape tests (incl. the Manager false-positive guard).
- No hardcoded conventions, no subClassOf walk, no module-level pyoxigraph import.
- build_predicate_index runs on QALD's DBpedia subset (>40 predicates, retrieve path); shape-distribution counts recorded; RESEARCH OQ1 resolved.
</verification>

<success_criteria>
- `build_predicate_index()` present, function-local imports only.
- Shape tests green offline; Manager case asserted NOT value_object.
- Grep gates: 0 subClassOf, 0 hardcoded price/hasCategory, 0 module-level pyoxigraph import.
</success_criteria>

<output>
Create `.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-03-SUMMARY.md` when done.
</output>
