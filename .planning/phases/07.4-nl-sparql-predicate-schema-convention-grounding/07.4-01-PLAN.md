---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py
  - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/seams.py
  - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/engine.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [NL-ACC-02]
must_haves:
  truths:
    - "Seam 7 (predicate_index / predicate_prompt_section) is a real language-agnostic member of QueryLanguageAdapter in arango_query_core"
    - "PredicateIndex mirrors LabelIndex (caller-owned, no memoization, retrieve/format_prompt_section contract) and reuses one shared token-substring scorer"
    - "The engine composes the predicate block after the entity block, inside _system_prompt (post-cache-boundary), never in grammar_prompt_section"
    - "NL-ACC-02 exists in REQUIREMENTS.md mirroring NL-ACC-01's acceptance shape"
  artifacts:
    - path: "/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py"
      provides: "GroundedPredicate dataclass + PredicateIndex + extracted _token_substring_retrieve shared helper + two-tier renderer"
      contains: "class PredicateIndex"
    - path: "/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/seams.py"
      provides: "Seam 7 Protocol members on QueryLanguageAdapter"
      contains: "def predicate_index"
    - path: "/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/engine.py"
      provides: "NLQueryEngine._system_prompt predicate block after entities + predicate_k field"
      contains: "predicate_index()"
    - path: ".planning/REQUIREMENTS.md"
      provides: "NL-ACC-02 requirement entry + coverage/traceability rows"
      contains: "NL-ACC-02"
  key_links:
    - from: "NLQueryEngine._system_prompt"
      to: "adapter.predicate_index() + adapter.predicate_prompt_section()"
      via: "seam-7 composition after the entity block"
      pattern: "predicate_prompt_section"
---

<objective>
Add a new language-agnostic **seam 7** (predicate/schema-convention grounding) to the shared
`arango_query_core` engine, structurally mirroring the seam-6 (entity grounding) pattern shipped
in Phase 07.3, and author the new **NL-ACC-02** requirement it satisfies.

This plan is sibling-repo + requirements-doc work ONLY. It does NOT bump this repo's
`arango-query-core` pin (Plan 02 owns that, atomically with the adapters — Pitfall 1). The old
pin has no seam 7, so this repo stays conformant until Plan 02.

Purpose: establish the engine contract (`GroundedPredicate`/`PredicateIndex`, seam-7 Protocol
members, engine composition ordering) that all downstream SPARQL-side and eval-harness work
consumes, and register NL-ACC-02 as the phase's requirement of record (D-06, D-07, D-09).
Output: a pushed `arango-query-core` commit carrying seam 7; NL-ACC-02 in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroundedPredicate + PredicateIndex + extracted shared scorer in engine grounding.py</name>
  <files>/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py</files>
  <read_first>
    - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py (analog: GroundedEntity lines 62-72, LabelIndex.__init__/from_items/retrieve lines 75-120, format_prompt_section lines 122-156, _sanitize_label lines 39-59)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (grounding.py section: dataclass + retrieval + renderer patterns to mirror)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Pattern 3 GroundedPredicate/PredicateIndex sketch; Pattern 5 two-tier render)
  </read_first>
  <behavior>
    - Test: PredicateIndex.retrieve(question, k) returns predicates whose label/domain/range tokens substring-match the question, capped at k, identical ranking to LabelIndex on equivalent inputs (shared scorer).
    - Test: PredicateIndex.retrieve with k >= len(predicates) returns all predicates (dump mode is just a large k; the class does not decide the mode).
    - Test: format_prompt_section returns "" when no matches; when matches exist it emits a terse `domain -> range [kind]` line for every predicate, and an expanded bracketed shape tag + example-triple line ONLY for shape=="value_object" and shape=="category_instance".
    - Test: every rendered label passes through the existing _sanitize_label (control-char strip, whitespace collapse, 200-char cap) — a label containing a newline + "ignore previous instructions" is neutralized.
  </behavior>
  <action>
    Add a frozen dataclass `GroundedPredicate` with fields `iri: str`, `label: str`,
    `kind: str` ("object"|"datatype"), `domain: str`, `range: str`, `shape: str`
    ("value_object"|"category_instance"|"linked_entity"|"literal"),
    `shape_detail: tuple[tuple[str, str], ...] = ()`. Extract LabelIndex's existing
    token-substring ranking body into a module-private helper
    `_token_substring_retrieve(items, get_labels, question, k)` and call it from BOTH
    `LabelIndex.retrieve` and a new `PredicateIndex.retrieve` — do not hand-roll a second
    scorer. `PredicateIndex` mirrors LabelIndex's `__init__(self, predicates)`,
    `from_items` classmethod, and `retrieve(self, question, k)` shape exactly, substituting
    GroundedPredicate. Add `PredicateIndex.format_prompt_section(self, question, k, *, header, instruction)`
    keeping the "empty string on no matches" contract and the caller-supplied header/instruction
    wording split; add the two-tier rendering logic (terse line for all; expanded shape tag +
    example triple only for value_object/category_instance). Call the existing `_sanitize_label`
    on every label rendered from a predicate/class (do NOT write a second sanitizer). Mirror the
    module docstring framing ("caller-owned data, no memoization at this layer, retrieve/
    format_prompt_section contract, returns '' so caller can omit the section").
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/arango-query-core && python -m pytest tests -k "predicate" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from arango_query_core.nl.grounding import GroundedPredicate, PredicateIndex"` (run in the sibling repo env) exits 0
    - `grep -c "def _token_substring_retrieve" /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/grounding.py` == 1 (single shared scorer, not duplicated)
    - New engine-side unit tests for PredicateIndex retrieve/render/sanitize pass (pytest exits 0)
    - No second sanitizer: `grep -c "_CONTROL_CHARS_RE\|def _sanitize" grounding.py` shows the sanitizer defined once, reused
  </acceptance_criteria>
  <done>GroundedPredicate + PredicateIndex importable from arango_query_core.nl.grounding; one shared token-substring scorer; two-tier renderer sanitizes every label; engine-side unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Seam 7 Protocol members + engine _system_prompt composition + push commit</name>
  <files>/Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/seams.py, /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/engine.py</files>
  <read_first>
    - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/seams.py (analog: seam 6 lines 67-123; enumerated seam list lines 10-16; import line 32)
    - /Users/plosiewicz/Desktop/arango-query-core/arango_query_core/nl/engine.py (analog: _system_prompt lines 116-130; grounding_k field line 68; class docstring lines 55-63)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (seams.py + engine.py sections: exact members and composition block to add)
  </read_first>
  <action>
    In seams.py: add seam-7 Protocol members `predicate_index(self) -> PredicateIndex | None`
    and `predicate_prompt_section(self, question: str, index: PredicateIndex, k: int = 20) -> str`
    to `QueryLanguageAdapter`, mirroring seam 6's docstrings (index or None to run ungrounded;
    renderer adapter-owned wording, machinery in PredicateIndex). Add `PredicateIndex` to the
    `from .grounding import LabelIndex` import. Add a `7. the predicate/schema-convention
    grounding index.` line to the enumerated seam list at the top of the file. In engine.py:
    add a `predicate_k: int = 20` dataclass field on `NLQueryEngine` next to `grounding_k`, extend
    the class docstring the same way grounding_k is documented (retrieved per question, rendered
    AFTER the entity section, post-cache-boundary), and extend `_system_prompt` to append the
    predicate block AFTER the entity block (ordering: grammar -> few-shot -> entities ->
    predicates), calling `self.adapter.predicate_index()` then, if non-None and the returned block
    is non-empty, `self.adapter.predicate_prompt_section(question, predicates, k=self.predicate_k)`.
    Add an engine-side composition unit test asserting the predicate block appears after the entity
    block and only inside `_system_prompt`. Commit and push to the pinned remote
    (github.com/ArthurKeen/arango-query-core, and the arango-solutions mirror if present), then
    record the pushed SHA in this plan's SUMMARY for Plan 02 to consume.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/arango-query-core && python -m pytest tests -k "predicate or system_prompt or compose" -q && git -C /Users/plosiewicz/Desktop/arango-query-core log -1 --format=%H</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from arango_query_core.nl.seams import QueryLanguageAdapter; assert hasattr(QueryLanguageAdapter,'predicate_index') and hasattr(QueryLanguageAdapter,'predicate_prompt_section')"` (sibling env) exits 0
    - Engine composition test asserts predicate block ordering (after entities) and absence from grammar_prompt_section; pytest exits 0
    - `git -C /Users/plosiewicz/Desktop/arango-query-core status --porcelain` empty (committed) and `git ls-remote https://github.com/ArthurKeen/arango-query-core <SHA>` resolves (pushed)
    - SUMMARY records the pushed 40-char SHA
  </acceptance_criteria>
  <done>Seam 7 is a mandatory QueryLanguageAdapter member; engine composes the predicate block after entities inside _system_prompt; commit pushed and SHA recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Author NL-ACC-02 into REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (analog: NL-ACC-01 acceptance shape line 53; coverage block lines 131-137; traceability table lines 97-129)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Phase Requirements table: NL-ACC-02 description)
  </read_first>
  <action>
    Add an unchecked `NL-ACC-02` bullet to the `### NL→SPARQL Quality (ACTIVE workstream)` section
    immediately after NL-ACC-01 (line 53), mirroring NL-ACC-01's acceptance shape: mechanically-
    derived TBox predicate index (label + domain + range + object-vs-datatype + usage-shape)
    injected as a new engine seam-7 prompt block (D-06/D-07); prove a statistically significant,
    execution-graded CK25 lift over the NL-ACC-01 entity-grounded baseline
    (`baseline.json:openai-gpt4o-mini-ck25-grounded` = 14/49), McNemar p<0.05, zero regressions;
    directional QALD-9-plus generalization check (not a gate); no CK25-specific hand-curation
    (mechanical schema walk only, D-02); scripted configs stay the CI default; W3C DAWG ≥96.4% and
    the SPARQL→AQL transpiler package unchanged (D-08). Add a `| NL-ACC-02 | Phase 07.4 | Pending |`
    row to the traceability table, and update the Coverage counts (v1 NL requirement count +1;
    Mapped +1; Unmapped stays 0).
  </action>
  <verify>
    <automated>grep -c "NL-ACC-02" /Users/plosiewicz/dev/arango-sparql-py/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NL-ACC-02" .planning/REQUIREMENTS.md` >= 2 (requirement bullet + traceability row)
    - The NL-ACC-02 bullet text names: seam-7 predicate block, mechanical TBox derivation (no hand-curation), CK25 McNemar p<0.05 vs 14/49 baseline, zero regressions, QALD directional, W3C ≥96.4%, transpiler untouched
    - Coverage block "Unmapped: 0" preserved
  </acceptance_criteria>
  <done>NL-ACC-02 authored mirroring NL-ACC-01; traceability + coverage updated; Unmapped stays 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBox label text -> LLM prompt | `rdfs:label`/`rdfs:comment` strings authored in an ontology cross into the model prompt via the predicate renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-01 | Tampering (prompt injection) | PredicateIndex.format_prompt_section (grounding.py) | mitigate | Call the EXISTING `_sanitize_label` (strip C0 control chars, collapse whitespace, cap 200) on every predicate/class label before it enters the block — the shipped T-07.3-01 mitigation extended to predicate labels; do not write a second sanitizer |
| T-07.4-02 | Denial of Service (prompt/cost blowup) | PredicateIndex.retrieve | mitigate | retrieve(k) caps candidates; the dump-vs-retrieve threshold that bounds large schemas lands in Plan 02 (adapter/caller level), not this class |
</threat_model>

<verification>
- Sibling-repo suite green for the new predicate classes and engine composition test.
- Seam 7 importable and present on QueryLanguageAdapter; engine composes the block after entities.
- NL-ACC-02 present in REQUIREMENTS.md; coverage Unmapped: 0.
- This repo's pin is UNCHANGED this plan (Plan 02 owns the atomic bump).
</verification>

<success_criteria>
- `GroundedPredicate`/`PredicateIndex` importable from `arango_query_core.nl.grounding`; one shared token-substring scorer.
- `predicate_index`/`predicate_prompt_section` are members of `QueryLanguageAdapter`.
- `NLQueryEngine._system_prompt` renders the predicate block after the entity block, post-cache-boundary.
- Engine commit pushed; SHA recorded for Plan 02.
- NL-ACC-02 authored in REQUIREMENTS.md.
</success_criteria>

<output>
Create `.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-01-SUMMARY.md` when done. MUST record the pushed arango-query-core commit SHA.
</output>
