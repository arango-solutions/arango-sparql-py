---
phase: 07.4-nl-sparql-predicate-schema-convention-grounding
plan: 04
type: execute
wave: 4
depends_on: ["07.4-02", "07.4-03"]
files_modified:
  - tests/nl2sparql/eval/runner.py
  - tests/nl2sparql/eval/configs.yml
  - tests/nl2sparql/test_grounding_engine_prompt.py
  - tests/nl2sparql/test_grounding_prompt_parity.py
  - tests/nl2sparql/eval/test_grounding_recall.py
autonomous: true
requirements: [NL-ACC-02]
must_haves:
  truths:
    - "runner.py reads an additive predicate_grounding: config key, builds the predicate index ONCE before the per-case loop, gated on shared_ontology (NOT data_ttl), and threads predicate_index/predicate_k into NlPipeline"
    - "write_report() JSON payload captures per-case generated SPARQL (D-05) via the already-populated CaseResult.actual"
    - "The predicate block lands in the engine-composed prompt, absent from grammar_prompt_section (D-07 SC-gate), and is byte-identical across both adapters (parity)"
    - "A deterministic predicate-recall guard runs offline in the always-on CI tier; scripted-ck25-predicate-grounded is a plumbing gate that does not crash"
    - "Every pre-existing config stays byte-identical (additive, default-preserving)"
  artifacts:
    - path: "tests/nl2sparql/eval/runner.py"
      provides: "predicate_grounding read + build-once + passthrough + D-05 one-key write_report fix"
      contains: "build_predicate_index"
    - path: "tests/nl2sparql/eval/configs.yml"
      provides: "scripted/openai-gpt4o-mini ck25 + qald9plus -predicate-grounded configs"
      contains: "predicate_grounding"
    - path: "tests/nl2sparql/test_grounding_engine_prompt.py"
      provides: "predicate-block SC-gate (D-07)"
      contains: "Known schema predicates"
    - path: "tests/nl2sparql/test_grounding_prompt_parity.py"
      provides: "predicate cross-adapter byte-identity gate"
      contains: "predicate_prompt_section"
    - path: "tests/nl2sparql/eval/test_grounding_recall.py"
      provides: "deterministic predicate-recall guard (offline CI tier)"
      contains: "predicate"
  key_links:
    - from: "runner.py::run()"
      to: "build_predicate_index(shared_ontology) -> NlPipeline(predicate_index=...)"
      via: "build-once before the per-case loop, gated on shared_ontology"
      pattern: "if predicate_cfg and shared_ontology"
    - from: "write_report()"
      to: "CaseResult.actual"
      via: "one-key dict-literal addition"
      pattern: "\"sparql\": c.actual"
---

<objective>
Wire the seam-7 predicate index into the eval harness (additive, default-preserving), capture
generated SPARQL per case (D-05), and add the offline SC-gate / parity / predicate-recall
guards.

Purpose: make `predicate_grounding:` runnable end-to-end (config -> build_predicate_index ->
seam 7 -> engine prompt -> judge) in the scripted CI tier without live credentials, prove the
cache-boundary + cross-adapter invariants for seam 7 hold, and give the lever always-on
deterministic regression protection. Three Pitfalls are load-bearing here: gate on
`shared_ontology` NOT `data_ttl` (Pitfall 5 — QALD has no data_path); D-05 is a one-key fix not
new plumbing (Pitfall 3); do NOT re-author `openai-gpt4o-mini-qald9plus`, it already exists
(Pitfall 4).
Output: runner + configs + SC-gate + parity + recall guard, all green offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md
@.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runner.py predicate_grounding read + build-once + D-05 capture + configs.yml entries</name>
  <files>tests/nl2sparql/eval/runner.py, tests/nl2sparql/eval/configs.yml</files>
  <read_first>
    - tests/nl2sparql/eval/runner.py (analog: grounding_cfg/grounding_index build-once block lines 540-567; per-case pipeline wiring lines 574-583; write_report() payload lines 717-721; CaseResult.actual populated at line 594)
    - tests/nl2sparql/eval/configs.yml (analog: scripted-ck25-grounded / openai-gpt4o-mini-ck25-grounded block lines 92-142; scripted-qald9plus / openai-gpt4o-mini-qald9plus lines 52-79)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (runner.py + configs.yml sections: exact additive blocks; Pitfalls 3/4/5)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-RESEARCH.md (Pitfall 3 D-05 one-key fix; Pitfall 4 qald zero-shot exists; Pitfall 5 gate on shared_ontology)
  </read_first>
  <action>
    In runner.py, mirror the grounding_cfg block: read `predicate_cfg = config.get("predicate_grounding", {})`,
    `predicate_k = predicate_cfg.get("k", 0)`, `predicate_index = None`; then, gated on
    `if predicate_cfg and shared_ontology:` (NOT data_ttl — Pitfall 5), function-locally import
    `build_predicate_index` from tests.nl2sparql.eval.grounding_index_builder and build
    `predicate_index = build_predicate_index(shared_ontology)` ONCE, before the
    `for case in corpus["cases"]:` loop (same build-once discipline as few_shot_index/grounding_index).
    Add `predicate_k=predicate_k, predicate_index=predicate_index` to the per-case `NlPipeline(...)`
    construction. Apply the D-05 fix: in write_report()'s JSON payload cases list, add
    `"sparql": c.actual` to the per-case dict literal (CaseResult.actual is already populated at
    line ~594 — one key, not new plumbing). In configs.yml, add four net-new entries mirroring the
    grounded block's shape but with a `predicate_grounding:` block instead of `grounding:`:
    `scripted-ck25-predicate-grounded` and `openai-gpt4o-mini-ck25-predicate-grounded`
    (corpus vendored/ck25/corpus.yml, judge execution, `predicate_grounding: {k: 40}` — CK25's 30
    dump under PREDICATE_DUMP_THRESHOLD); `scripted-qald9plus-predicate-grounded` and
    `openai-gpt4o-mini-qald9plus-predicate-grounded` (corpus vendored/qald9plus/corpus.yml,
    judge canonical, `predicate_grounding: {k: 20}` — QALD's 250 retrieve). Add a comment block
    framing these as Phase 07.4 additive. Do NOT add or modify `openai-gpt4o-mini-qald9plus`
    (Pitfall 4 — it already exists as the zero-shot live config).
  </action>
  <verify>
    <automated>RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-ck25-predicate-grounded'); print('ck25-pred pass_rate', r.pass_rate)"; RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-qald9plus-predicate-grounded'); print('qald-pred pass_rate', r.pass_rate)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "build_predicate_index" tests/nl2sparql/eval/runner.py` >= 1 and the build call precedes the `for case in corpus\[.cases.\]` loop (line-order check)
    - `grep -c "if predicate_cfg and shared_ontology" tests/nl2sparql/eval/runner.py` == 1 (gated on shared_ontology, NOT data_ttl)
    - `grep -c '"sparql": c.actual' tests/nl2sparql/eval/runner.py` == 1 (D-05 one-key capture)
    - `grep -c "predicate_grounding:" tests/nl2sparql/eval/configs.yml` == 4 (four net-new entries)
    - `grep -c "openai-gpt4o-mini-qald9plus:" tests/nl2sparql/eval/configs.yml` == 1 (the pre-existing zero-shot config is untouched, not duplicated)
    - `RUN_EVAL=1 python -c "from tests.nl2sparql.eval.runner import run; r=run('scripted-ck25-predicate-grounded'); print(r.pass_rate)"` completes without error (plumbing gate: config->build_predicate_index->seam 7->prompt->judge does not crash; treated as plumbing evidence only, NOT accuracy — mirrors 07.3-05 Task 2 / Pitfall 5) and likewise for `run('scripted-qald9plus-predicate-grounded')`
    - Do NOT use a `pytest -k` filter against `test_eval.py` (it matches zero predicate_grounded tests and risks tripping `test_ci_gate_only_ever_runs_scripted`'s static self-parse)
    - Default-preserving is asserted separately by the untouched pre-existing `test_eval.py` suite run at wave-merge (`python -m pytest tests/nl2sparql/eval/test_eval.py -q`), not via a `-k` filter here
  </acceptance_criteria>
  <done>predicate_grounding read + build-once (shared_ontology-gated) + pipeline passthrough + D-05 one-key capture in runner.py; four additive configs in configs.yml; scripted plumbing gate green; pre-existing configs byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Predicate SC-gate (D-07) + cross-adapter parity gate</name>
  <files>tests/nl2sparql/test_grounding_engine_prompt.py, tests/nl2sparql/test_grounding_prompt_parity.py</files>
  <read_first>
    - tests/nl2sparql/test_grounding_engine_prompt.py (analog: full file — entity SC-gate fixture + assertions lines 27-68)
    - tests/nl2sparql/test_grounding_prompt_parity.py (analog: full file — cross-adapter byte-identity lines 45-59)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (both test sections: sibling test shapes to add)
  </read_first>
  <action>
    In test_grounding_engine_prompt.py, add a sibling test
    `test_predicates_render_in_engine_prompt_not_standalone_builder`: build a hand-rolled
    GroundedPredicate/PredicateIndex, construct SparqlAdapter with `predicate_index=index`, build
    NLQueryEngine with `predicate_k=...`, and assert the seam-7 header string
    (`## Known schema predicates`) appears in `engine._system_prompt(question, "")` and is ABSENT
    from `adapter.grammar_prompt_section("")` (D-07 cache boundary extended to seam 7). In
    test_grounding_prompt_parity.py, add
    `test_predicate_prompt_section_byte_identical_across_adapters`: build a hand-rolled
    PredicateIndex fixture, call `.predicate_prompt_section(question, index, k=20)` on BOTH
    SparqlAdapter and legacy SparqlLanguageAdapter, and assert the two outputs are equal.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/test_grounding_engine_prompt.py tests/nl2sparql/test_grounding_prompt_parity.py -k predicate -q</automated>
  </verify>
  <acceptance_criteria>
    - SC-gate test asserts `"## Known schema predicates" in engine._system_prompt(...)` AND `not in adapter.grammar_prompt_section("")`
    - Parity test asserts `language_output == engine_output` for predicate_prompt_section
    - `uv run pytest tests/nl2sparql/test_grounding_engine_prompt.py tests/nl2sparql/test_grounding_prompt_parity.py -k predicate -q` exits 0
  </acceptance_criteria>
  <done>Predicate block proven engine-composed (not in grammar section) and byte-identical across both adapters.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic predicate-recall guard (offline CI tier)</name>
  <files>tests/nl2sparql/eval/test_grounding_recall.py</files>
  <read_first>
    - tests/nl2sparql/eval/test_grounding_recall.py (analog: full file — deterministic gold-IRI recall pattern lines 29-48, always-on CI-tier placement)
    - .planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-PATTERNS.md (test_grounding_recall.py section: predicate-recall analog)
    - tests/nl2sparql/eval/vendored/ck25/corpus.yml (gold SPARQL to scan for predicate mentions)
  </read_first>
  <action>
    Add a sibling test that walks CK25's gold SPARQL for predicate local-name mentions (regex over
    `pv:[A-Za-z]+` in each `case["expected"]`), builds a PredicateIndex from the corpus's
    `shared_ontology` via build_predicate_index, and asserts retrieval recall of gold predicates
    meets a floor (assert `total > 0` first so the guard is non-vacuous, then `recall >= <floor>`
    with the measured value recorded in the assertion message). Keep it offline and always-on
    (no RUN_EVAL, no pytest.mark.eval), matching this file's placement discipline. Choose the floor
    from the observed recall (report the measured number in the SUMMARY); do not set an aspirational
    floor above what the mechanical builder actually achieves.
  </action>
  <verify>
    <automated>uv run pytest tests/nl2sparql/eval/test_grounding_recall.py -k predicate -q</automated>
  </verify>
  <acceptance_criteria>
    - Test builds a PredicateIndex from `shared_ontology` (not from an instance graph) and asserts `total > 0` before the recall assertion (non-vacuous)
    - No RUN_EVAL / pytest.mark.eval gate (always-on CI tier)
    - `uv run pytest tests/nl2sparql/eval/test_grounding_recall.py -k predicate -q` exits 0; measured recall recorded in SUMMARY
  </acceptance_criteria>
  <done>Offline predicate-recall guard green in the always-on CI tier; measured recall recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated SPARQL -> eval report JSON | Model output captured to disk (D-05) |
| Ontology Turtle -> predicate index -> LLM prompt | schema flows into the prompt in the eval tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-03 | Information Disclosure | write_report() D-05 payload | mitigate | Only `c.actual` (the generated SPARQL string) is added; no API key, header, or credential is in scope for the payload; capture is eval-tier only (never a CI-default artifact with secrets), consistent with the existing report writer which already stores name/passed/elapsed_ms |
| T-07.4-02 | Denial of Service (prompt/cost blowup) | configs.yml predicate_grounding k | mitigate | CK25 k=40 dumps 30 (bounded); QALD k=20 retrieves from 250 (bounded); PREDICATE_DUMP_THRESHOLD (Plan 02) enforces the routing |
| T-07.4-01 | Tampering (prompt injection) | seam-7 render path in eval | mitigate | Rendering still routes through the sanitizing PredicateIndex.format_prompt_section (Plan 01); no new unsanitized label path added here |
</threat_model>

<verification>
- Scripted plumbing gate (scripted-ck25-predicate-grounded) runs the full chain without crashing.
- Pre-existing configs byte-identical (default-preserving).
- SC-gate + parity + predicate-recall guards green offline.
- D-05 capture adds only `c.actual`; no secrets.
</verification>

<success_criteria>
- runner.py: predicate_grounding read (shared_ontology-gated), build-once, passthrough, D-05 one-key capture.
- configs.yml: 4 additive entries; zero-shot QALD config untouched.
- SC-gate, parity, predicate-recall guards green offline; scripted plumbing gate green under RUN_EVAL=1.
</success_criteria>

<output>
Create `.planning/phases/07.4-nl-sparql-predicate-schema-convention-grounding/07.4-04-SUMMARY.md` when done. Record the measured predicate-recall number + the scripted plumbing gate pass_rate.
</output>
